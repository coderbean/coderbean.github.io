<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Aria2 GUI最佳实践</title>
    <url>/best-practices-of-aria2.html</url>
    <content><![CDATA[<p>目前用的爽的下载工具，通过搭配  Chrome 和 自己写的一些脚本达到不输迅雷的用户体验。</p>
<blockquote>
<p>本文在 MacOS 下可以通过所有步骤，其他平台需要自行修改相关步骤</p>
</blockquote>
<span id="more"></span>
<h2 id="Aria2-下载"><a href="#Aria2-下载" class="headerlink" title="Aria2 下载"></a>Aria2 下载</h2><p>推荐使用 <a href="https://brew.sh/index_zh-cn">Homebrew</a> 安装</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">brew install aria2</span><br></pre></td></tr></table></figure>
<h2 id="配置-Aria2"><a href="#配置-Aria2" class="headerlink" title="配置 Aria2"></a>配置 Aria2</h2><p>将下面的内容复制到 <code>/Users/用户名/.aria2/aria2.conf</code> 里面，用户名是你 Mac 的用户名。<br>同时建立session文件<br><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">touch ~/.aria2/aria2.session</span><br></pre></td></tr></table></figure></p>
<p>文中注释了一些选项含义，所以的含义需要看这个网址获取 <a href="https://aria2.github.io/manual/en/html/aria2c.html?highlight=download#cmdoption-on-bt-download-complete">aria2c(1) — aria2 1.34.0 documentation</a></p>
<p>不建议修改下面的除了 用户文件夹之外的内容，这些内容都是经过实践得出的比较优秀的配置，下载速度较快</p>
<p>需要修改下面的用户名 jacob 改为你自己的用户名方可使用。</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="comment">## &#x27;#&#x27;开头为注释内容, 选项都有相应的注释说明, 根据需要修改 ##</span></span><br><span class="line"><span class="comment">## 被注释的选项填写的是默认值, 建议在需要修改时再取消注释  ##</span></span><br><span class="line"><span class="comment">## 添加了@和默认启用的选项都是系统需要调用的，请不要随意改动否则可能无法正常运行</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">## 日志</span></span><br><span class="line"><span class="comment">#log-level=info</span></span><br><span class="line"><span class="comment">#log=/Users/jacob/.aria2/aria2.log</span></span><br><span class="line"></span><br><span class="line"><span class="comment">## 文件保存相关 ##</span></span><br><span class="line"><span class="comment"># 文件的保存路径(可使用绝对路径或相对路径), 默认: 当前启动位置</span></span><br><span class="line"><span class="comment"># 此项 OS X 无法使用 $HOME 及 ~/ 设置路径  建议使用 /users/用户名/downloads</span></span><br><span class="line"><span class="attr">dir</span>=<span class="string">/Users/jacob/Downloads</span></span><br><span class="line"><span class="comment"># 启用磁盘缓存, 0为禁用缓存, 需1.16以上版本, 默认:16M</span></span><br><span class="line"><span class="meta">disk-cache</span>=<span class="string">0M</span></span><br><span class="line"><span class="comment"># 文件预分配方式, 能有效降低磁盘碎片, 默认:prealloc</span></span><br><span class="line"><span class="comment"># 预分配所需时间: none &lt; falloc ? trunc &lt; prealloc</span></span><br><span class="line"><span class="comment"># falloc和trunc则需要文件系统和内核支持</span></span><br><span class="line"><span class="comment"># NTFS建议使用falloc, EXT3/4建议trunc, MAC 下需要注释此项</span></span><br><span class="line"><span class="comment"># file-allocation=none</span></span><br><span class="line"><span class="comment"># 断点续传</span></span><br><span class="line"><span class="attr">continue</span>=<span class="string">true</span></span><br><span class="line"><span class="comment"># 最大重试次数, 设置为0表示不限制重试次数, 默认:5</span></span><br><span class="line"><span class="meta">max-tries</span>=<span class="string">5</span></span><br><span class="line"><span class="comment"># 设置重试等待的秒数, 默认:0</span></span><br><span class="line"><span class="meta">retry-wait</span>=<span class="string">5</span></span><br><span class="line"><span class="comment">## 下载连接相关 ##</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 最大同时下载任务数, 运行时可修改, 默认:5</span></span><br><span class="line"><span class="meta">max-concurrent-downloads</span>=<span class="string">20</span></span><br><span class="line"><span class="comment"># 同一服务器连接数, 添加时可指定, 默认:1</span></span><br><span class="line"><span class="meta">max-connection-per-server</span>=<span class="string">16</span></span><br><span class="line"><span class="comment"># 最小文件分片大小, 添加时可指定, 取值范围1M -1024M, 默认:20M</span></span><br><span class="line"><span class="comment"># 假定size=10M, 文件为20MiB 则使用两个来源下载; 文件为15MiB 则使用一个来源下载</span></span><br><span class="line"><span class="meta">min-split-size</span>=<span class="string">1024K</span></span><br><span class="line"><span class="comment"># 单个任务最大线程数, 添加时可指定, 默认:5</span></span><br><span class="line"><span class="attr">split</span>=<span class="string">16</span></span><br><span class="line"><span class="comment"># 整体下载速度限制, 运行时可修改, 默认:0</span></span><br><span class="line"><span class="meta">max-overall-download-limit</span>=<span class="string">0</span></span><br><span class="line"><span class="comment"># 单个任务下载速度限制, 默认:0</span></span><br><span class="line"><span class="meta">max-download-limit</span>=<span class="string">0</span></span><br><span class="line"><span class="comment"># 整体上传速度限制, 运行时可修改, 默认:0</span></span><br><span class="line"><span class="meta">max-overall-upload-limit</span>=<span class="string">0</span></span><br><span class="line"><span class="comment"># 单个任务上传速度限制, 默认:0</span></span><br><span class="line"><span class="meta">max-upload-limit</span>=<span class="string">0</span></span><br><span class="line"><span class="comment"># 禁用IPv6, 默认:false</span></span><br><span class="line"><span class="meta">disable-ipv6</span>=<span class="string">false</span></span><br><span class="line"><span class="comment">#运行覆盖已存在文件</span></span><br><span class="line"><span class="meta">allow-overwrite</span>=<span class="string">true</span></span><br><span class="line"><span class="comment">#自动重命名</span></span><br><span class="line"><span class="meta">auto-file-renaming</span>=<span class="string">true</span></span><br><span class="line"></span><br><span class="line"><span class="comment">## 进度保存相关 ##</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 从会话文件中读取下载任务</span></span><br><span class="line"><span class="meta">input-file</span>=<span class="string">/Users/jacob/.aria2/aria2.session</span></span><br><span class="line"><span class="comment"># 在Aria2退出时保存`错误/未完成`的下载任务到会话文件</span></span><br><span class="line"><span class="meta">save-session</span>=<span class="string">/Users/jacob/.aria2/aria2.session</span></span><br><span class="line"><span class="comment"># 定时保存会话, 0为退出时才保存, 需1.16.1以上版本, 默认:0</span></span><br><span class="line"><span class="meta">save-session-interval</span>=<span class="string">30</span></span><br><span class="line"></span><br><span class="line"><span class="comment">## RPC相关设置 ##</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 启用RPC, 默认:false</span></span><br><span class="line"><span class="meta">enable-rpc</span>=<span class="string">true</span></span><br><span class="line"><span class="comment"># 允许所有来源, 默认:false</span></span><br><span class="line"><span class="meta">rpc-allow-origin-all</span>=<span class="string">true</span></span><br><span class="line"><span class="comment"># 允许非外部访问, 默认:false</span></span><br><span class="line"><span class="meta">rpc-listen-all</span>=<span class="string">true</span></span><br><span class="line"><span class="comment"># 事件轮询方式, 取值:[epoll, kqueue, port, poll, select], 不同系统默认值不同</span></span><br><span class="line"><span class="comment">#event-poll=select</span></span><br><span class="line"><span class="comment"># RPC监听端口, 端口被占用时可以修改, 默认:6800</span></span><br><span class="line"><span class="comment"># 使用本客户端请勿修改此项</span></span><br><span class="line"><span class="meta">rpc-listen-port</span>=<span class="string">6800</span></span><br><span class="line"><span class="comment"># 设置的RPC授权令牌, v1.18.4新增功能, 取代 --rpc-user 和 --rpc-passwd 选项</span></span><br><span class="line"><span class="meta">rpc-secret</span>=<span class="string">maria.rpc.2018</span></span><br><span class="line"></span><br><span class="line"><span class="comment">## BT/PT下载相关 ##</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 当下载的是一个种子(以.torrent结尾)时, 自动开始BT任务, 默认:true</span></span><br><span class="line"><span class="comment">#follow-torrent=true</span></span><br><span class="line"><span class="comment"># BT监听端口, 当端口被屏蔽时使用, 默认:6881-6999</span></span><br><span class="line"><span class="meta">listen-port</span>=<span class="string">51413</span></span><br><span class="line"><span class="comment"># 单个种子最大连接数, 默认:55</span></span><br><span class="line"><span class="comment">#bt-max-peers=55</span></span><br><span class="line"><span class="comment"># 打开DHT功能, PT需要禁用, 默认:true</span></span><br><span class="line"><span class="meta">enable-dht</span>=<span class="string">true</span></span><br><span class="line"><span class="comment"># 打开IPv6 DHT功能, PT需要禁用</span></span><br><span class="line"><span class="comment">#enable-dht6=false</span></span><br><span class="line"><span class="comment"># DHT网络监听端口, 默认:6881-6999</span></span><br><span class="line"><span class="comment">#dht-listen-port=6881-6999</span></span><br><span class="line"><span class="comment"># 本地节点查找, PT需要禁用, 默认:false</span></span><br><span class="line"><span class="meta">bt-enable-lpd</span>=<span class="string">true</span></span><br><span class="line"><span class="comment"># 种子交换, PT需要禁用, 默认:true</span></span><br><span class="line"><span class="meta">enable-peer-exchange</span>=<span class="string">true</span></span><br><span class="line"><span class="comment"># 每个种子限速, 对少种的PT很有用, 默认:50K</span></span><br><span class="line"><span class="comment">#bt-request-peer-speed-limit=50K</span></span><br><span class="line"><span class="comment"># 客户端伪装, PT需要</span></span><br><span class="line"><span class="meta">peer-id-prefix</span>=<span class="string">-TR2770-</span></span><br><span class="line"><span class="meta">user-agent</span>=<span class="string">Transmission/2.77</span></span><br><span class="line"><span class="comment"># 当种子的分享率达到这个数时, 自动停止做种, 0为一直做种, 默认:1.0</span></span><br><span class="line"><span class="meta">seed-ratio</span>=<span class="string">0</span></span><br><span class="line"><span class="comment"># 强制保存会话, 即使任务已经完成, 默认:false</span></span><br><span class="line"><span class="comment"># 较新的版本开启后会在任务完成后依然保留.aria2文件</span></span><br><span class="line"><span class="comment">#force-save=false</span></span><br><span class="line"><span class="comment"># BT校验相关, 默认:true</span></span><br><span class="line"><span class="comment">#bt-hash-check-seed=true</span></span><br><span class="line"><span class="comment"># 继续之前的BT任务时, 无需再次校验, 默认:false</span></span><br><span class="line"><span class="meta">bt-seed-unverified</span>=<span class="string">true</span></span><br><span class="line"><span class="comment"># 保存磁力链接元数据为种子文件(.torrent文件), 默认:false</span></span><br><span class="line"><span class="meta">bt-save-metadata</span>=<span class="string">true</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># bt-tracker数据来自https://github.com/ngosang/trackerslist/blob/master/trackers_all_udp.txt</span></span><br><span class="line"></span><br><span class="line"><span class="meta">bt-tracker</span>=<span class="string">udp://tracker.coppersurfer.tk:6969/announce,udp://tracker.open-internet.nl:6969/announce,udp://tracker.leechers-paradise.org:6969/announce,udp://tracker.internetwarriors.net:1337/announce,udp://9.rarbg.to:2710/announce,udp://9.rarbg.me:2710/announce,udp://tracker.opentrackr.org:1337/announce,http://tracker3.itzmx.com:6961/announce,http://tracker1.itzmx.com:8080/announce,udp://exodus.desync.com:6969/announce,udp://explodie.org:6969/announce,udp://ipv4.tracker.harry.lu:80/announce,udp://denis.stalker.upeer.me:6969/announce,udp://tracker.torrent.eu.org:451/announce,udp://tracker.tiny-vps.com:6969/announce,udp://open.demonii.si:1337/announce,http://vps02.net.orel.ru:80/announce,http://re-tracker.uz:80/announce,http://open.acgnxtracker.com:80/announce,udp://tracker4.itzmx.com:2710/announce,</span></span><br><span class="line"></span><br><span class="line"><span class="comment">## 一些脚本</span></span><br><span class="line"><span class="comment">#下载完成后字段执行操作</span></span><br><span class="line"><span class="meta">on-bt-download-complete</span>=<span class="string">/Users/jacob/.aria2/aria2_on_bt_download_complete.sh</span></span><br><span class="line"><span class="meta">on-download-complete</span>=<span class="string">/Users/jacob/.aria2/aria2_on_download_complete.sh</span></span><br><span class="line"><span class="meta">on-download-error</span>=<span class="string">/Users/jacob/.aria2/aria2_on_download_error.sh</span></span><br><span class="line"><span class="meta">on-download-pause</span>=<span class="string">/Users/jacob/.aria2/aria2_on_download_pause.sh</span></span><br><span class="line"><span class="meta">on-download-start</span>=<span class="string">/Users/jacob/.aria2/aria2_on_download_start.sh</span></span><br><span class="line"><span class="meta">on-download-stop</span>=<span class="string">/Users/jacob/.aria2/aria2_on_download_stop.sh</span></span><br></pre></td></tr></table></figure>
<h2 id="调试-Aria2"><a href="#调试-Aria2" class="headerlink" title="调试 Aria2"></a>调试 Aria2</h2><p>Aria2 启动可能会有问题，我们需要调试成功之后才然后设置开机启动，否则无法查看错误信息，</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">/usr/local/bin/aria2c --conf-path=/Users/jacob/.aria2/aria2.conf</span><br></pre></td></tr></table></figure>
<p>通过上面的命令可以看到提示，如果没有错误就可以进入下一步了。如果有错误根据错误提示解决问题</p>
<h2 id="配置-Aria2-开机启动"><a href="#配置-Aria2-开机启动" class="headerlink" title="配置 Aria2 开机启动"></a>配置 Aria2 开机启动</h2><p>aira2 每次都要自己启动，十分麻烦，但是 aria2 资源占用很低，适合在后台常驻，所以可以设置为开机启动。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">cd ~/Library/LaunchAgents</span><br><span class="line">touch com.aria2c.plist</span><br></pre></td></tr></table></figure>
<p>将下面的内容放入 <code>com.aria2c.plist</code></p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">plist</span> <span class="meta-keyword">PUBLIC</span> <span class="meta-string">&quot;-//Apple//DTD PLIST 1.0//EN&quot;</span> <span class="meta-string">&quot;http://www.apple.com/DTDs/PropertyList-1.0.dtd&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">plist</span> <span class="attr">version</span>=<span class="string">&quot;1.0&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dict</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">key</span>&gt;</span>KeepAlive<span class="tag">&lt;/<span class="name">key</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">true</span>/&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">key</span>&gt;</span>Label<span class="tag">&lt;/<span class="name">key</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">string</span>&gt;</span>com.aria2c<span class="tag">&lt;/<span class="name">string</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">key</span>&gt;</span>ProgramArguments<span class="tag">&lt;/<span class="name">key</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">array</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">string</span>&gt;</span>/usr/local/bin/aria2c<span class="tag">&lt;/<span class="name">string</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">string</span>&gt;</span>--conf-path=/Users/jacob/.aria2/aria2.conf<span class="tag">&lt;/<span class="name">string</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">array</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">key</span>&gt;</span>RunAtLoad<span class="tag">&lt;/<span class="name">key</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">true</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dict</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">plist</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>加载配置</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">launchctl load ~/Library/LaunchAgents/com.aria2c.plist</span><br></pre></td></tr></table></figure>
<p>此时 通过 <code>ps aux | grep &quot;aria2&quot;</code>可以看到 aria2 已经启动，如果没有启动可以注销Mac用户后登陆即可看到 aria2 在运行中。以后 aira2 就可以开机启动了。</p>
<h2 id="GUI-推荐"><a href="#GUI-推荐" class="headerlink" title="GUI 推荐"></a>GUI 推荐</h2><p>Aria2 有很多 GUI，这个推荐一个 Chrome 插件用作 GUI， Chrome 的功能我们可以让他看起来像一个独立的应用。</p>
<p>安装 <a href="https://chrome.google.com/webstore/detail/aria2-for-chrome/mpkodccbngfoacfalldjimigbofkhgjn">Aria2 for Chrome - Chrome 网上应用店</a></p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1g1wmsowtn4j30he0i076a.jpg" alt="image-20190409195941716"></p>
<p>右键插件图标进入选项，设置 rpc 网址为 <code>http://token:maria.rpc.2018@localhost:6800/jsonrpc</code></p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1g1wmu6com6j30yt0u0n1f.jpg" alt="image-20190409200110273"></p>
<p>然后单击插件图标即可看到提示 Aria2 已连接的提示</p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1g1wmvewqh5j30y40rota2.jpg" alt="image-20190409200222405"></p>
<p>如果这里无法连接，需要在 GUI 内部设置token等信息</p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1g1xep6hxkvj31tm0mujw0.jpg" alt="image-20190410120509073"></p>
<p>下面我们可以让这个插件变成一个应用，如果你不感兴趣可以直接看下一节。</p>
<p>右键点击 插件 图标，点击 打开AriaNG 文字，进入页面</p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1g1wmyn8cffj30hs0fywfp.jpg" alt="image-20190409200528048"></p>
<p>然后将这个页面导出为应用</p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1g1wmzyuwz3j311x0u0tha.jpg" alt="image-20190409200644372"></p>
<p>此时在mac的应用里就会有一个 AriaNG 应用了，图标可以自行定制，如何修改图标自己搜索下吧</p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1g1wn0qhed3j30i80f0tag.jpg" alt="image-20190409200728757"></p>
<p>效果如下</p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1g1xenq065vj31880u0k3r.jpg" alt="image-20190410120344344"></p>
<h2 id="使用-Aria2-下载磁力链接"><a href="#使用-Aria2-下载磁力链接" class="headerlink" title="使用 Aria2 下载磁力链接"></a>使用 Aria2 下载磁力链接</h2><p>普通下载不必多讲，按照插件和 GUI 的页面可以很容易的进行下载，这里说说如何下载 磁力链接。Aria2 磁力链接的速度会很慢，甚至可能无法下载，此时我们可以通过修改 配置中的 bt-tracker 选项来获得高速下载，速度不输迅雷。</p>
<p>可以通过复制 <a href="https://raw.githubusercontent.com/ngosang/trackerslist/master/trackers_best.txt">https://raw.githubusercontent.com/ngosang/trackerslist/master/trackers_best.txt</a>  的内容然后修改一下格式，配置给 Aria2，因为 trackers 一直在变化我也写了一个python3 的脚本来辅助更新。内容如下</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> urllib.request</span><br><span class="line"><span class="keyword">import</span> urllib.parse</span><br><span class="line"></span><br><span class="line">path = <span class="string">&#x27;/Users/jacob/.aria2/aria2.conf&#x27;</span></span><br><span class="line"></span><br><span class="line">url = <span class="string">&#x27;https://raw.githubusercontent.com/ngosang/trackerslist/master/trackers_best.txt&#x27;</span></span><br><span class="line">f = urllib.request.urlopen(url)</span><br><span class="line">s = f.read().decode(<span class="string">&#x27;utf-8&#x27;</span>)</span><br><span class="line">bt_tracker = s.replace(<span class="string">&#x27;\n\n&#x27;</span>, <span class="string">&#x27;,&#x27;</span>)</span><br><span class="line"></span><br><span class="line">data = <span class="string">&#x27;&#x27;</span></span><br><span class="line"><span class="keyword">with</span> <span class="built_in">open</span>(path, <span class="string">&#x27;r+&#x27;</span>) <span class="keyword">as</span> f:</span><br><span class="line">    <span class="keyword">for</span> line <span class="keyword">in</span> f.readlines():</span><br><span class="line">        <span class="keyword">if</span> line.find(<span class="string">&#x27;bt-tracker&#x27;</span>) == <span class="number">0</span>:</span><br><span class="line">            line = <span class="string">&#x27;bt-tracker=%s&#x27;</span> % bt_tracker + <span class="string">&#x27;\n&#x27;</span></span><br><span class="line"></span><br><span class="line">        data += line</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> <span class="built_in">open</span>(path, <span class="string">&#x27;r+&#x27;</span>) <span class="keyword">as</span> f:</span><br><span class="line">    f.writelines(data)</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>通过运行上面的脚本可以一键更新 bt-tracker 内容，注意配置修改之后必须要重启 Aria2 才可以生效。可以通过 kill 进程来实现，我们的开机启动配置会自动重启一个 Aria 进程。</p>
<h2 id="配置使用下载完成后通知"><a href="#配置使用下载完成后通知" class="headerlink" title="配置使用下载完成后通知"></a>配置使用下载完成后通知</h2><p>先看看效果：</p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1g1xemfqc4dj30jk03uq3w.jpg" alt="image-20190410120230775"></p>
<p>上面的插件是没有实现下载完成后通知的，这样其实有些尴尬，每次都要去看看写没写完，可以通过 Aria2 自带的 钩子函数来实现。对应在 conf 里的配置是这些</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="comment">## 一些脚本</span></span><br><span class="line"><span class="comment">#下载完成后字段执行操作</span></span><br><span class="line"><span class="meta">on-bt-download-complete</span>=<span class="string">/Users/jacob/.aria2/aria2_on_bt_download_complete.sh</span></span><br><span class="line"><span class="meta">on-download-complete</span>=<span class="string">/Users/jacob/.aria2/aria2_on_download_complete.sh</span></span><br><span class="line"><span class="meta">on-download-error</span>=<span class="string">/Users/jacob/.aria2/aria2_on_download_error.sh</span></span><br><span class="line"><span class="meta">on-download-pause</span>=<span class="string">/Users/jacob/.aria2/aria2_on_download_pause.sh</span></span><br><span class="line"><span class="meta">on-download-start</span>=<span class="string">/Users/jacob/.aria2/aria2_on_download_start.sh</span></span><br><span class="line"><span class="meta">on-download-stop</span>=<span class="string">/Users/jacob/.aria2/aria2_on_download_stop.sh</span></span><br></pre></td></tr></table></figure>
<p>这些脚本的内容如下</p>
<p>/Users/jacob/.aria2/aria2_on_bt_download_complete.sh</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">/usr/local/bin/terminal-notifier -title &quot;Aria2&quot; -subtitle &quot;BT下载完成&quot; -message &quot;&quot; -activate com.google.Chrome.app.Default-hpjnfnejkleckpmhmjppopcbnedoocmg  -appIcon /Users/jacob/Applications/Chrome\ Apps.localized/Default\ hpjnfnejkleckpmhmjppopcbnedoocmg.app/Contents/Resources/app.icns</span><br></pre></td></tr></table></figure>
<p>/Users/jacob/.aria2/aria2_on_download_complete.sh</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">/usr/local/bin/terminal-notifier -title &quot;Aria2&quot; -subtitle &quot;下载完成&quot; -message $msg -execute &quot;/usr/bin/open $3&quot; -appIcon /Users/jacob/Applications/Chrome\ Apps.localized/Default\ hpjnfnejkleckpmhmjppopcbnedoocmg.app/Contents/Resources/app.icns</span><br></pre></td></tr></table></figure>
<p>/Users/jacob/.aria2/aria2_on_download_error.sh</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">var=$3</span><br><span class="line">msg=$&#123;var##*/&#125;</span><br><span class="line">/usr/local/bin/terminal-notifier -title &quot;Aria2&quot; -subtitle &quot;下载失败&quot; -message $msg -activate com.google.Chrome.app.Default-hpjnfnejkleckpmhmjppopcbnedoocmg  -appIcon /Users/jacob/Applications/Chrome\ Apps.localized/Default\ hpjnfnejkleckpmhmjppopcbnedoocmg.app/Contents/Resources/app.icns</span><br></pre></td></tr></table></figure>
<p>/Users/jacob/.aria2/aria2_on_download_pause.sh</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">var=$3</span><br><span class="line">msg=$&#123;var##*/&#125;</span><br><span class="line">/usr/local/bin/terminal-notifier -title &quot;Aria2&quot; -subtitle &quot;暂停下载&quot; -message $msg -activate com.google.Chrome.app.Default-hpjnfnejkleckpmhmjppopcbnedoocmg  -appIcon /Users/jacob/Applications/Chrome\ Apps.localized/Default\ hpjnfnejkleckpmhmjppopcbnedoocmg.app/Contents/Resources/app.icns</span><br></pre></td></tr></table></figure>
<p>/Users/jacob/.aria2/aria2_on_download_start.sh</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span>=$<span class="number">3</span></span><br><span class="line">msg=$&#123;var##*/&#125;</span><br><span class="line">/usr/local/bin/terminal-notifier -title <span class="string">&quot;Aria2&quot;</span> -subtitle <span class="string">&quot;开始下载&quot;</span> -message $msg -activate com.google.Chrome.app.Default-hpjnfnejkleckpmhmjppopcbnedoocmg  -appIcon /Users/jacob/Applications/Chrome\ Apps.localized/Default\ hpjnfnejkleckpmhmjppopcbnedoocmg.app/Contents/Resources/app.icns</span><br></pre></td></tr></table></figure>
<p>使用这些脚本需要安装软件 <a href="https://github.com/julienXX/terminal-notifier">julienXX/terminal-notifier: Send User Notifications on macOS from the command-line.</a></p>
<p>安装方式 <code>brew install terminal-notifier</code></p>
<p>appIcon 就是通知时候的图标，可以自定义。这样就实现了下载完成后通知，并且点击通知就能够打开下载的文件。terminal-notifier 一定要使用全路径名，否则无法生效。</p>
<h2 id="Aria2-满速下载百度云文件"><a href="#Aria2-满速下载百度云文件" class="headerlink" title="Aria2 满速下载百度云文件"></a>Aria2 满速下载百度云文件</h2><p>安装 Chrome 插件<a href="https://github.com/acgotaku/BaiduExporter">acgotaku/BaiduExporter: Assistant for Baidu to export download links to aria2/aria2-rpc</a></p>
<p>安装方式看 GitHub 的 readme， 然后配置本地 Aria2</p>
<p>设置在这里：</p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1g1wo1bmaqpj310s0cmq53.jpg" alt="image-20190409204236064"></p>
<p>配置 URL 为 <code>http://token:maria.rpc.2018@localhost:6800/jsonrpc</code></p>
<p>以后使用 导出下载即可获得满速下载。</p>
]]></content>
      <categories>
        <category>工具</category>
      </categories>
      <tags>
        <tag>tools</tag>
      </tags>
  </entry>
  <entry>
    <title>JVM 探究（一）：JVM内存模型概念模型</title>
    <url>/jmm-of-jvm.html</url>
    <content><![CDATA[<p>作为一个 Java 程序员，不了解 Java 内存模型就不能写出能够充分利用内存的代码。本文通过对 Java 内存模型的介绍，让读者能够了解 Java 的内存的分配情况，适合 Java 初学者或者对 JMM 不熟悉的同学。后面的博客会针对每个部分做更加深入的解释。<br><span id="more"></span></p>
<h2 id="Java-内存模型"><a href="#Java-内存模型" class="headerlink" title="Java 内存模型"></a>Java 内存模型</h2><p>首先通过下图对于 Java 内存模型有一个整体的认识，然后针对不同的区域的作用和存储的内容做进一步的解释。</p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcly1fnvc6jtby5j30r50l9wh2.jpg" alt="Java内存模型"></p>
<h3 id="PC（程序计数器）"><a href="#PC（程序计数器）" class="headerlink" title="PC（程序计数器）"></a>PC（程序计数器）</h3><p>这里的 PC 不是 Personal Computer，而是 Program Counter Register，从名字就可以看出来，这是一个寄存器，用来存储需要执行的指令地址。</p>
<blockquote>
<p>程序计数器（Program Counter (PC)）是在电脑处理器中的一个寄存器，用来指示电脑下一步要运行的指令序列。—WikiPedia</p>
</blockquote>
<p>PC 和其他 JVM 内存区域最大的区别是：</p>
<blockquote>
<p><strong>“此内存区域是唯一一个在Java虚拟机规范中没有规定任何OutOfMemoryError情况的区域。</strong>”<br>摘录来自: 周志明. “深入理解Java虚拟机：JVM高级特性与最佳实践（第2版）”。 iBooks.</p>
</blockquote>
<p>像上面的图片一样，PC 是每个线程私有的，对于 Java 方法而言，PC 中存储的是正在执行的虚拟机字节码的内存地址；对于 Native 方法来说，PC 中的值为空（Undefined）。</p>
<h3 id="Java-虚拟机栈和本地方法栈"><a href="#Java-虚拟机栈和本地方法栈" class="headerlink" title="Java 虚拟机栈和本地方法栈"></a>Java 虚拟机栈和本地方法栈</h3><h4 id="虚拟机栈"><a href="#虚拟机栈" class="headerlink" title="虚拟机栈"></a>虚拟机栈</h4><p>无论是在大学的 Java 编程课堂上，还是我们在学习过程编码过程，经常会出现 StackOverFlow，甚至目前最大的技术问答社区的名字也是 StackOverFlow。Java 语言中会产生栈溢出的就是这块内存区域，当你的程序中设置了超过 JVM 规定的递归深度的时候就会触发这个异常。类似 JMM 的其他内存区域，如果虚拟机栈在动态扩展的时候无法申请到足够的内存也会报OOM异常。</p>
<p>Java 语言中每一个方法的执行都对应着一个栈帧（Stack Frame）的创建，栈帧中存储的是局部变量、方法出口等信息，因此对于一个方法的执行而言，所能够使用到的内存是在编译期间就能够完全确定的，在运行期间不会发生变化。在栈帧中，局部变量空间成为 Slot，除了 double 和 long 占有 2 个 slot 外，其他基本数据类型和对象引用都占用 1 个 slot 空间</p>
<h4 id="本地方法栈"><a href="#本地方法栈" class="headerlink" title="本地方法栈"></a>本地方法栈</h4><p>本地方法栈和虚拟机栈最大的区别就是虚拟机栈是为执行 Java 字节码服务的，而本地方法栈是为了虚拟机使用到的 Native 方法服务的。除此之外，Java 虚拟机规范并没有针对本地方法栈的实现做具体规定。在 HotSpot 虚拟机中，本地方法栈和虚拟机栈是共用同一块内存的，不做具体区分。同样，本地方法栈也会产生 OOM 异常和 StackOverFlow 异常。</p>
<h3 id="Java-堆"><a href="#Java-堆" class="headerlink" title="Java 堆"></a>Java 堆</h3><blockquote>
<p>“The heap is the runtime data area from which memory for all class instances and arrays is allocated。” —Java虚拟机规范</p>
</blockquote>
<p>Java 虚拟机规范规定所有的实例对象和数组都应该分配到 Java 堆中。<br>说的通俗一点就是所有 new 出来的对象和数组都会放到该区域，由于现在的收集器都采用分代收集算法，所以在 Java 堆中又分了新生代和老年代，新生代有做了详细的区分。该区域的大小可以通过 JVM 参数 <code>-Xmx</code> 和 <code>-Xms</code> 来设置。</p>
<h4 id="直接内存"><a href="#直接内存" class="headerlink" title="直接内存"></a>直接内存</h4><p>在 JDK1.4 中引入了 NIO，可以通过 Native 方法直接在堆外分配内存，然后通过在堆中存储的引用来对这块内存区域做操作。<strong>注意</strong> 这块区域并不会在 <code>-Xmx</code>和 <code>-Xms</code> 设置的大小之内，因此在设置 JVM 参数的时候要注意考虑这块内存区域，避免设置的内存区域总额大于物理内存</p>
<h3 id="方法区"><a href="#方法区" class="headerlink" title="方法区"></a>方法区</h3><p>Method Area 又叫 NonHeap，也是线程共有的内存区域，用来存：</p>
<ul>
<li>类信息</li>
<li>常量</li>
<li>静态变量</li>
<li>字符串常量池</li>
</ul>
<p>在 JDK1.7 中已经将字符串常量池移出永久代，在 Java8 中更是之内取消了永久代，而是使用了元空间（MetaSpace）来存储这些信息，从而永久代的大小不需要再指定，只要不超出物理内存的限制就不会产生 OOM 异常</p>
<h4 id="运行时常量池"><a href="#运行时常量池" class="headerlink" title="运行时常量池"></a>运行时常量池</h4><p>运行时常量池主要用来存储类的版本、字段、方法、接口等描述信息。常量池（Constant Pool Table）用来存储各种字面量和符号引用。String 的 <code>intern()</code> 方法就是在运行期间将对象放到常量池中的。此部分也会出现 OOM 异常。</p>
<h2 id="JDK8-以及之后的模型"><a href="#JDK8-以及之后的模型" class="headerlink" title="JDK8 以及之后的模型"></a>JDK8 以及之后的模型</h2><p><img src="https://raw.githubusercontent.com/PicGoBed/PicBed/master/2019-07-26-NrXELZ.jpg" alt="pic"></p>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>JVM</tag>
        <tag>tech</tag>
      </tags>
  </entry>
  <entry>
    <title>Atom插件和主题推荐</title>
    <url>/theme-and-plugin-of-atom.html</url>
    <content><![CDATA[<p>介绍本人使用<code>Atom</code>编辑器使用的插件和主题配置，推荐给大家使用，同时也给自己做个备份。 <span id="more"></span></p>
<h1 id="theme"><a href="#theme" class="headerlink" title="theme"></a>theme</h1><p>下面是一些废话，可以跳过</p>
<blockquote>
<p>说起这个主题，真的是一波三折。Atom 默认的 <code>One Dark</code> 主题用的太久了，有点腻了，然后就寻思着换个主题，看了很多，没有特别有感觉的，直到在某国外网站上看到了这个推荐 <a href="https://myawesomemac.com/10-best-atom-themes-of-2017/">10 Best Atom themes of 2017</a>，感觉这个 <a href="https://atom.io/themes/native-ui">Native UI</a> 甚是满足我的审美，于是乎赶紧下载了下来，然后发现切换到该主题后 Atom 巨卡无比，本来打算放弃了，可是想了想设置里面应该可以关闭半透明效果应该能够让 Atom 不那么力不从心。机智的我终于看了设置里面的两个选项看了半天，愣是没发现这个 <code>User Interface</code> 是半透明效果的开关。甚是蛋疼。</p>
</blockquote>
<p>主题推荐：<a href="https://myawesomemac.com/10-best-atom-themes-of-2017/">10 Best Atom themes of 2017</a><br>我最推荐的主题：<a href="https://atom.io/themes/native-ui">Native UI</a></p>
<p>大概是这个样子：<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1fnqxdj3niij31kw0zi7ai.jpg" alt=""></p>
<p>安装方法：</p>
<ol>
<li>cmd + ,</li>
<li>install</li>
<li>Search packages(右侧选定 Themes)</li>
<li>输入 <code>native-ui</code></li>
<li>enter</li>
<li>选择第一个install</li>
</ol>
<p>GIF教程： <img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1fnqvdqfc63g30bn06oqve.gif" alt=""> 设置： 设置里面找到 theme ，然后选择 native，再然后点击右侧设置图标，取消勾选 User Interface</p>
<h1 id="plugin"><a href="#plugin" class="headerlink" title="plugin"></a>plugin</h1><p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1fnqvdu0u11j319i1d6k01.jpg" alt=""></p>
<h2 id="atom-beautify"><a href="#atom-beautify" class="headerlink" title="atom-beautify"></a>atom-beautify</h2><p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1fnqxhwjn5pg30bn06t7wk.gif" alt=""></p>
<p>用来对代码进行格式化，让杂乱无章的代码瞬间变得有条理，我自己平常用的最多的就是格式化 JSON 字符串了，注意在格式化的时候必须先选定语言。快捷键可以通过 <code>cmd+shift+p</code> 搜索得到。</p>
<h2 id="browser-plus"><a href="#browser-plus" class="headerlink" title="browser-plus"></a>browser-plus</h2><p><del>不太推荐这个插件了，有点丑。</del></p>
<p>最近发现了一个比较好看的<code>atom-html-preview</code>，非常奶思。效果如下：<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcly1fraqdswhhwg30gn09ix6t.gif" alt=""></p>
<p>可以在 atom 里面预览HTML，快捷键可以通过 <code>cmd+shift+p</code> 搜索得到。</p>
<h2 id="file-icons"><a href="#file-icons" class="headerlink" title="file-icons"></a>file-icons</h2><p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1fnqxmi7iftj30dk0pk0up.jpg" alt=""></p>
<p>这个比较好，让atom更加美丽，会在文件前面给一个小图标，针对不同格式的文件有着很好的支持，atom 的文件图标再也不会那么单调了。</p>
<h2 id="markdown-preview-enhanced"><a href="#markdown-preview-enhanced" class="headerlink" title="markdown-preview-enhanced"></a>markdown-preview-enhanced</h2><p>不推荐这个插件了，过于臃肿。</p>
<p><del>这个号称最全markdown插件，可算是集成了所有用得到的和用不到的功能，强大的有些许过分，有时候会造成写markdown的时候卡顿，由于暂时没有找到能够同步滑动的插件，就暂时使用了这个。更多设置可以参考作者的中文文档：<a href="https://shd101wyy.github.io/markdown-preview-enhanced/#/zh-cn/">https://shd101wyy.github.io/markdown-preview-enhanced/#/zh-cn/</a></del></p>
<p>好消息好消息：<a href="https://atom.io/packages/markdown-preview-plus">Markdown Preview Plus (MPP)</a> 也支持同步滑动了。<br>效果挺好的，简洁好看，推荐使用！</p>
<h2 id="markdown-table-editor"><a href="#markdown-table-editor" class="headerlink" title="markdown-table-editor"></a>markdown-table-editor</h2><p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1fnqxrf6hbsg30bn06tqv8.gif" alt=""></p>
<p>markdown中编辑表格的神器，只需要打一个 <code>|</code> 然后通过 table 和回车键就可以完成表格的编辑，比起自己写效率高到不知道哪里去了。</p>
<h2 id="pdf-view"><a href="#pdf-view" class="headerlink" title="pdf-view"></a>pdf-view</h2><p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1fnqxs6qyevj31kw0z0459.jpg" alt=""></p>
<p>没啥好说的，让你可以在Atom中直接查看PDF</p>
<h2 id="platformio-ide-terminal"><a href="#platformio-ide-terminal" class="headerlink" title="platformio-ide-terminal"></a>platformio-ide-terminal</h2><p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1fnqxsrxwwtj31kw0z0160.jpg" alt=""></p>
<p>可以在atom中直接使用终端，是 <code>terminal-plus</code> 的替代品。有些许卡顿，大体可以接受。可以自定义shell。如果配合 iTerm2+Oh my zsh 使用出现乱码，注意在插件设置中设置你在 iTerm 中使用的字体，我的是<code>Meslo LG M DZ for Powerline</code></p>
<h2 id="sync-settings"><a href="#sync-settings" class="headerlink" title="sync-settings"></a>sync-settings</h2><p>这个插件可以将你的atom配置和插件同步到指定的Gist，换了电脑也不需要一个一个的自己重新安装了，它会自动帮你装好，恢复你原来的工作环境。配置和使用教程查看官方说明。</p>
]]></content>
      <categories>
        <category>工具</category>
      </categories>
      <tags>
        <tag>tools</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 中的偏向锁、轻量级锁和重量级锁</title>
    <url>/lock-of-synchronized.html</url>
    <content><![CDATA[<p>今天我们来聊聊 Synchronized 里面的各种锁：偏向锁、轻量级锁、重量级锁，以及三个锁之间是如何进行锁膨胀的。</p>
<span id="more"></span>
<p><img src="../media/GodsGarden_ZH-CN3317703606_1920x1080.jpg" alt="GodsGarden_ZH-CN3317703606_1920x1080"></p>
<p>众所周知，线程阻塞带来的上下文切换的代价是很大的，Java 为了尽量减少上下文的切换从而引入了更多的锁机制。在了解各种锁机制之前，先要学习一些前置知识。对于各种锁的获取和释放、以及锁升级的流程，在文末总结处有一张图，如果赶时间，直接看图吧。</p>
<h2 id="Mark-Word"><a href="#Mark-Word" class="headerlink" title="Mark Word"></a>Mark Word</h2><p><img src="../media/image-20190719003918974.png" alt="image-20190719003918974"></p>
<p>Java 对象头里面有一部分叫做 Mark Word，在32位虚拟机下面占有 32bit，在 64 位虚拟机下面占用 64 bit，本文以 32 位虚拟机为例子。</p>
<h2 id="锁介绍"><a href="#锁介绍" class="headerlink" title="锁介绍"></a>锁介绍</h2><p><strong>偏向锁：</strong>一个线程反复的去获取/释放一个锁，如果这个锁是轻量级锁或者重量级锁，不断的加解锁显然是没有必要的，造成了资源的浪费。于是引入了偏向锁，偏向锁在获取资源的时候会在资源对象上记录该对象是偏向该线程的，偏向锁并不会主动释放，这样每次偏向锁进入的时候都会判断改资源是否是偏向自己的，如果是偏向自己的则不需要进行额外的操作，直接可以进入同步操作。</p>
<p><strong>轻量级锁：</strong>轻量级锁是由偏向所升级来的，偏向锁运行在一个线程进入同步块的情况下，当第二个线程加入锁争用的时候，偏向锁就会升级为轻量级锁； 在轻量级锁中如果发生多线程竞争，未持有锁的线程会自旋等待。</p>
<p><strong>重量级锁：</strong>由操作系统的 mutex 来实现，多线程竞争下，未持有锁的线程将被阻塞。</p>
<h2 id="锁的流转"><a href="#锁的流转" class="headerlink" title="锁的流转"></a>锁的流转</h2><p><img src="../media/lock-of-jvm.jpeg" alt="锁转移"></p>
<h3 id="对象分配"><a href="#对象分配" class="headerlink" title="对象分配"></a>对象分配</h3><p>首先确定该类的偏向锁是否可用（决定了在新建对象实例的时候倒数第三 bit 是 0 还是 1）；如果不可用，直接使用轻量锁。如果偏向锁可用，新建实例对象 obj，此时 obj 进入到未锁定、未偏向、可偏向的状态。</p>
<h3 id="偏向锁-初始锁定"><a href="#偏向锁-初始锁定" class="headerlink" title="偏向锁 初始锁定"></a>偏向锁 初始锁定</h3><p>此时线程 A 想要获取对象 obj 的偏向锁，由于此时 obj 没有偏向任何线程（有可能是刚刚新建，也有可能是由锁定状态重偏向之后导致的未锁定状态），所以利用 CAS 操作将线程 ID 写入到 Mark Word 里面，此时 线程 A 获取了 obj 的偏向锁。obj 处于已偏向、锁定状态。</p>
<h3 id="偏向锁-锁定-解锁"><a href="#偏向锁-锁定-解锁" class="headerlink" title="偏向锁 锁定/解锁"></a>偏向锁 锁定/解锁</h3><p>一旦 obj 第一次偏向了线程 A，A 就可以在没有竞争的情况下，也就是锁不升级的情况下，以极小的代价反复获取 obj 对象的锁。</p>
<h3 id="偏向锁-重偏向（rebias）"><a href="#偏向锁-重偏向（rebias）" class="headerlink" title="偏向锁 重偏向（rebias）"></a>偏向锁 重偏向（rebias）</h3><p>如果 obj 先被线程 A 锁定，然后释放。然后线程 B 过来是否能重新获取偏向锁呐？在一定条件下，经过了重偏向可以重新获得偏向锁。</p>
<p>通过上面的 mark word 我们可以看出，在偏向锁的时候有一个字段是 epoch，同时在 obj 的类 O 信息里面也有一个 epoch，每次系统到达安全点会对类的 epoch 加 1，变成 epoch_new，然后扫描所有的类 O 的实例，判断该偏向锁是否还被持有，如果被持有则将 epoch_new 复制给对象头的 epoch 字段。</p>
<p>每次去获取偏向锁的时候回去判断对象实例的 epoch 和 类的 epoch 是否相等，如果不等代表对象是未锁定、可偏向、未偏向状态，可以偏向新的线程。</p>
<h3 id="撤销偏向（revoke）"><a href="#撤销偏向（revoke）" class="headerlink" title="撤销偏向（revoke）"></a>撤销偏向（revoke）</h3><blockquote>
<p> Once biased, that thread can subsequently lock and unlock the object without resorting to expensive atomic instructions. Obviously, an object can be biased toward at most one thread at any given time. (We refer to that thread as the bias holding thread). If another thread tries to acquire a biased object, however, we need to <em>revoke</em> the bias from the original thread. </p>
</blockquote>
<p>线程 B 来竞争，此时偏向锁会膨胀位轻量级锁。当 B 线程想利用 CAS 获取偏向锁失败， A 线程被暂停，然后检查 A 线程；</p>
<ol>
<li><p>A 线程已经退出了同步代码块，或者是已经不在存活了，如果是上面两种情况之一的，将 obj 的 Mark Word 后 3bit 改为 001（无锁状态），然后再去唤醒 A 线程。</p>
</li>
<li><p>A 线程还在同步代码块中，此时将 A 线程的偏向锁升级为轻量级锁。首先会 copy 一份 obj 对象的 mark word 内容，放到线程 A 当前栈帧的一个叫做 Lock Record 空间的 displace mark word 的位置，</p>
<p><img src="../media/20170420102716139.jpeg" alt="20170420102716139"></p>
<p>然后通过 CAS 将 obj 中 mark word 的内容替换为指向栈帧中 mark word 的位置，最后将 Lock Record 里面的 owner 指向 obj 对象。并且对象Mark Word的锁标志位设置为“00”，到此完成了锁的升级。</p>
<p><img src="../media/20170420102754608.jpeg" alt="20170420102754608"></p>
</li>
</ol>
<h3 id="Bulk-Revoke"><a href="#Bulk-Revoke" class="headerlink" title="Bulk Revoke"></a>Bulk Revoke</h3><p>Java 会在类信息里面维护一个计数器记录，记录该类发生偏向锁 revoke 的次数，一旦达到某个阈值，则认为这个类不适合偏向锁，将会禁用这个类的偏向锁功能。这个类新建的对象实例的最后 3bit 将是 001（无锁）。</p>
<blockquote>
<p>Objects that are explicitly designed to be shared between multiple threads, such as producer/consumer queues, are not suitable for biased locking. Therefore, biased locking is disabled for a class if revocations for its instances happened frequently in the past. This is called <em>bulk revocation</em>. If the locking code is invoked on an instance of a class for which biased locking was disabled, it performs the standard thin locking. Newly allocated instances of the class are marked as non-biasable.</p>
</blockquote>
<h3 id="轻量级锁定"><a href="#轻量级锁定" class="headerlink" title="轻量级锁定"></a>轻量级锁定</h3><p>刚刚在上文中关于撤销偏向（revoke）中已经描述了锁定的流程。在此再总结一遍</p>
<ol>
<li>复制 mark word 到 displace mark word</li>
<li>CAS 替换 mark word 中的信息为指针</li>
<li>修改 锁 标志为 00</li>
<li>将栈帧中的 owner 指向锁定的对象</li>
</ol>
<h3 id="轻量级锁-递归锁定"><a href="#轻量级锁-递归锁定" class="headerlink" title="轻量级锁 递归锁定"></a>轻量级锁 递归锁定</h3><p><img src="../media/image-20190719112951132.png" alt="image-20190719112951132"></p>
<p>线程 A 在去获取轻量级锁的时候，会首先使用 CAS 操作，如果操作失败那么会在此时判断是不是该线程已经持有过该对象的锁了，通过判断对象的 mark word 是不是指向当前线程的栈帧，如果是则会在最新的栈帧处新建一个 displaced mark word 为 null 的 lock record。关于为什么要这么做，其实就是为了记录一下锁的重入，发生重入了需要记录，本来是记录到 mark word 里面最方便，可能是 mark word 没有足够的空间。如果是在 lock record里面记录的话，需要遍历 lock record 才可以获取这个数量。所以最终Hotspot选择每次获得锁都添加一个<code>Lock Record</code>来表示锁的重入。</p>
<p>类似的递归的解锁只需要将栈帧中的 lock records 删除即可。</p>
<h3 id="轻量级锁-膨胀"><a href="#轻量级锁-膨胀" class="headerlink" title="轻量级锁 膨胀"></a>轻量级锁 膨胀</h3><p>线程 A 持有对象 obj 的轻量级锁，线程 B 过来 CAS 失败，开始自旋等待，自旋到达一定次数之后如果还没有获取该轻量级锁，则会将该锁膨胀为重量级锁。会将 mark word 的的锁标志为改为10，同时经指针指向互斥量，然后线程 B 挂起。</p>
<blockquote>
<p>JDK1.6中 -XX:+UseSpinning开启； -XX:PreBlockSpin=10 为自旋次数。<br>JDK1.7后，去掉 -XX:PreBlockSpin 参数，由jvm控制。</p>
</blockquote>
<h3 id="轻量级锁-解锁"><a href="#轻量级锁-解锁" class="headerlink" title="轻量级锁 解锁"></a>轻量级锁 解锁</h3><p>轻量级锁解锁也是理由 CAS 将 mark word 里面的指针替换为无锁的 mark word 信息。需要判断 mark word 里面是指向该线程的 lock record</p>
<ol>
<li>如果不是，说明已经锁膨胀了，CAS 失败，此时需要唤醒在等待重量级锁的线程</li>
<li>如果是，说明锁没有膨胀，直接 CAS 操作将 mark word 改为 001（无锁）状态，为下一个线程获取轻量级锁做好准备。</li>
</ol>
<h3 id="重量级锁-解锁"><a href="#重量级锁-解锁" class="headerlink" title="重量级锁 解锁"></a>重量级锁 解锁</h3><p>释放 mutex，唤醒在等待的线程</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>新标签页打开图片可看大图</p>
<p><img src="../media/v2-9db4211af1be81785f6cc51a58ae6054_r.jpg" alt="v2-9db4211af1be81785f6cc51a58ae6054_r"></p>
<h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="https://blog.csdn.net/zqz_zqz/article/details/70233767">java 中的锁 — 偏向锁、轻量级锁、自旋锁、重量级锁 - zqz_zqz的博客 - CSDN博客</a></p>
<p><a href="https://zhuanlan.zhihu.com/p/34606653">Java 8 并发篇 - 冷静分析 Synchronized（下） - 知乎</a></p>
<p><a href="https://blogs.oracle.com/dave/biased-locking-in-hotspot">Biased Locking in HotSpot | Oracle David Dice’s Blog</a>  //rebias</p>
<p><a href="http://www.ishenping.com/ArtInfo/266444.html#Bulk_Rebias_192">Java中的偏向锁，轻量级锁， 重量级锁解析 - 神评网</a> //rebias</p>
<p><a href="https://www.zhihu.com/question/56582060"> BiasedLocking模式下markOop中位域epoch的根本作用是什么？ - 知乎</a> //rebias</p>
<p><a href="https://juejin.im/post/5bfffafc6fb9a049d37ed198">死磕Synchronized底层实现—轻量级锁 - 掘金</a></p>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>JVM</tag>
        <tag>锁</tag>
        <tag>偏向锁</tag>
      </tags>
  </entry>
  <entry>
    <title>Java压缩流GZIPStream导致的内存泄露</title>
    <url>/things-about-java-gzipstream.html</url>
    <content><![CDATA[<p>我们来聊聊<code>GZIPOutputStream</code> 和 <code>GZIPInputStream</code>, 如果不关闭流会引起的内存泄露问题，以及<code>GZIPStream</code>申请和释放堆外内存的流程， Let’s do it!<br><span id="more"></span></p>
<h3 id="引子"><a href="#引子" class="headerlink" title="引子"></a>引子</h3><p>在我的工程里面又一个工具类 <code>ZipHelper</code> 用来压缩和解压 <code>String</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.ByteArrayInputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.ByteArrayOutputStream;</span><br><span class="line"><span class="keyword">import</span> java.util.zip.GZIPInputStream;</span><br><span class="line"><span class="keyword">import</span> java.util.zip.GZIPOutputStream;</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* 用来压缩和解压字符串</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ZipHelper</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 压缩</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">compress</span><span class="params">(String str)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (str == <span class="keyword">null</span> || str.length() == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> str;</span><br><span class="line">        &#125;</span><br><span class="line">        ByteArrayOutputStream out = <span class="keyword">new</span> ByteArrayOutputStream();</span><br><span class="line">        GZIPOutputStream gzip = <span class="keyword">new</span> GZIPOutputStream(out);</span><br><span class="line">        gzip.write(str.getBytes());</span><br><span class="line">        gzip.close();</span><br><span class="line">        <span class="keyword">return</span> out.toString(<span class="string">&quot;ISO-8859-1&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 解压缩</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">uncompress</span><span class="params">(String str)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (str == <span class="keyword">null</span> || str.length() == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> str;</span><br><span class="line">        &#125;</span><br><span class="line">        ByteArrayOutputStream out = <span class="keyword">new</span> ByteArrayOutputStream();</span><br><span class="line">        ByteArrayInputStream in = <span class="keyword">new</span> ByteArrayInputStream(str.getBytes(<span class="string">&quot;ISO-8859-1&quot;</span>));</span><br><span class="line">        GZIPInputStream gunzip = <span class="keyword">new</span> GZIPInputStream(in);</span><br><span class="line">        <span class="keyword">byte</span>[] buffer = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1024</span>];</span><br><span class="line">        <span class="keyword">int</span> n;</span><br><span class="line">        <span class="keyword">while</span> ((n = gunzip.read(buffer)) &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">            out.write(buffer, <span class="number">0</span>, n);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> out.toString();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>最近服务出现了占用swap空间的问题，初步定位为内存泄漏，最后通过分析定位到是 Native 方法<code>Java_java_util_zip_Inflater_init</code>一直在申请内存（关于分析方法可以查阅这篇博客<a href="https://jacobchang.cn/action-in-memory-leak-analysis.html">内存泄露分析实战</a>）但是没有释放，很有可能就是流没有关闭造成的，而这部分代码最大的问题就是没有在<code>finally</code>里面去关闭流，于是乎我打算改造这部分代码，利用 try-with-resource 语法糖，然后代码就被修改成了这样：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.ByteArrayInputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.ByteArrayOutputStream;</span><br><span class="line"><span class="keyword">import</span> java.util.zip.GZIPInputStream;</span><br><span class="line"><span class="keyword">import</span> java.util.zip.GZIPOutputStream;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Created by jacob.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * 用来压缩和解压字符串</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ZipHelper</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 压缩字符串</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> str 待压缩的字符串</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 压缩后的字符串</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@throws</span> Exception 压缩过程中的异常</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">compress</span><span class="params">(String str)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (str == <span class="keyword">null</span> || str.length() == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> str;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// ByteArrayOutputStream 和 ByteArrayInputStream 是一个虚拟的流,</span></span><br><span class="line">        <span class="comment">// JDk源码中关闭方法是空的, 所以无需关闭, 为了代码整洁，还是放到了try-with-resource里面</span></span><br><span class="line">        <span class="keyword">try</span> (ByteArrayOutputStream out = <span class="keyword">new</span> ByteArrayOutputStream();</span><br><span class="line">             GZIPOutputStream gzip = <span class="keyword">new</span> GZIPOutputStream(out)) &#123;</span><br><span class="line">            gzip.write(str.getBytes());</span><br><span class="line"><span class="comment">//            gzip.finish();</span></span><br><span class="line">            <span class="keyword">return</span> out.toString(<span class="string">&quot;ISO-8859-1&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 解压字符串</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> str 待解压的字符串</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 解压后的字符串</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@throws</span> Exception 解压过程中的异常</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">uncompress</span><span class="params">(String str)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (str == <span class="keyword">null</span> || str.length() == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> str;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">try</span> (ByteArrayOutputStream out = <span class="keyword">new</span> ByteArrayOutputStream();</span><br><span class="line">             ByteArrayInputStream in = <span class="keyword">new</span> ByteArrayInputStream(str.getBytes(<span class="string">&quot;ISO-8859-1&quot;</span>));</span><br><span class="line">             GZIPInputStream gunzip = <span class="keyword">new</span> GZIPInputStream(in)) &#123;</span><br><span class="line">            <span class="keyword">byte</span>[] buffer = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1024</span>];</span><br><span class="line">            <span class="keyword">int</span> n;</span><br><span class="line">            <span class="keyword">while</span> ((n = gunzip.read(buffer)) &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">                out.write(buffer, <span class="number">0</span>, n);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> out.toString();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>是不是顺眼多了呐，可是这样的代码可以压缩的，在解压的时候会报错。一开始我以为是解压的代码出现了问题，最后才发现是因为压缩的时候没有成功压缩，导致解压的时候无法解压。报以下错误</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Exception in thread &quot;main&quot; java.io.EOFException: Unexpected end of ZLIB input stream</span><br><span class="line">	at java.util.zip.InflaterInputStream.fill(InflaterInputStream.java:240)</span><br><span class="line">	at java.util.zip.InflaterInputStream.read(InflaterInputStream.java:158)</span><br><span class="line">	at java.util.zip.GZIPInputStream.read(GZIPInputStream.java:117)</span><br><span class="line">	at java.io.FilterInputStream.read(FilterInputStream.java:107)</span><br><span class="line">	at coderbean.ZipHelper.uncompress(ZipHelper.java:52)</span><br><span class="line">	at coderbean.Main.main(Main.java:12)</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>好好的代码怎么会突然压缩失败，后来发现的问题是在<code>GZIPOutputStream</code>中，在<code>close()</code>方法中会主动调用<code>finish()</code>方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* Writes remaining compressed data to the output stream and closes the</span></span><br><span class="line"><span class="comment">* underlying stream.</span></span><br><span class="line"><span class="comment">* <span class="doctag">@exception</span> IOException if an I/O error has occurred</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">close</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">   <span class="keyword">if</span> (!closed) &#123;</span><br><span class="line">       finish();</span><br><span class="line">       <span class="keyword">if</span> (usesDefaultDeflater)</span><br><span class="line">           def.end();</span><br><span class="line">       out.close();</span><br><span class="line">       closed = <span class="keyword">true</span>;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在下面的方法中才会将压缩后的数据输出到输入流，由于原来的代码会调用 <code>close()</code>方法，从而间接调用了 <code>finish()</code> 方法。那我我们的<code>try-with-resource</code>到底出了什么问题，其实问题就在于执行<code>close()</code>的时间。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Finishes writing compressed data to the output stream without closing</span></span><br><span class="line"><span class="comment"> * the underlying stream. Use this method when applying multiple filters</span></span><br><span class="line"><span class="comment"> * in succession to the same output stream.</span></span><br><span class="line"><span class="comment"> * 在该方法中才会将压缩后的数据输出到输入流，由于原来的代码会调用 close()方法，从而</span></span><br><span class="line"><span class="comment"> * 间接调用了 finish() 方法。</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@exception</span> IOException if an I/O error has occurred</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">finish</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!def.finished()) &#123;</span><br><span class="line">        def.finish();</span><br><span class="line">        <span class="keyword">while</span> (!def.finished()) &#123;</span><br><span class="line">            <span class="keyword">int</span> len = def.deflate(buf, <span class="number">0</span>, buf.length);</span><br><span class="line">            <span class="keyword">if</span> (def.finished() &amp;&amp; len &lt;= buf.length - TRAILER_SIZE) &#123;</span><br><span class="line">                <span class="comment">// last deflater buffer. Fit trailer at the end</span></span><br><span class="line">                writeTrailer(buf, len);</span><br><span class="line">                len = len + TRAILER_SIZE;</span><br><span class="line">                out.write(buf, <span class="number">0</span>, len);</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (len &gt; <span class="number">0</span>)</span><br><span class="line">                out.write(buf, <span class="number">0</span>, len);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// if we can&#x27;t fit the trailer at the end of the last</span></span><br><span class="line">        <span class="comment">// deflater buffer, we write it separately</span></span><br><span class="line">        <span class="keyword">byte</span>[] trailer = <span class="keyword">new</span> <span class="keyword">byte</span>[TRAILER_SIZE];</span><br><span class="line">        writeTrailer(trailer, <span class="number">0</span>);</span><br><span class="line">        out.write(trailer);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="try-with-resource-执行时机和条件"><a href="#try-with-resource-执行时机和条件" class="headerlink" title="try-with-resource 执行时机和条件"></a><code>try-with-resource</code> 执行时机和条件</h3><p><code>try-with-resource</code> 是在 JDK7 中新增加的语法糖（其实就是抄的C#），用来自动执行流的关闭操作，只要该类实现了<code>AutoCloseable</code>的<code>close()</code>方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">package</span> java.lang;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">AutoCloseable</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@throws</span> Exception if this resource cannot be closed</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">close</span><span class="params">()</span> <span class="keyword">throws</span> Exception</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>实现了这个接口之后，我们可以将会在<code>try</code>代码块执行结束之后自动关闭流</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span>(<span class="comment">/* 在此处初始化资源 */</span>)&#123;</span><br><span class="line">  <span class="comment">// do something</span></span><br><span class="line">&#125; <span class="comment">//在代码块执行结束前最后一步关闭流</span></span><br></pre></td></tr></table></figure>
<p>由于在<code>GZIPOutputStream</code>执行了<code>finish()</code>方法或者<code>close()</code>方法之后才会真正的将压缩后的数据写入流，在上文我改造的代码中并没有首先执行<code>finish()</code>方法，而是直接在<code>try</code>代码块执行完之后关闭了流 <code>GZIPOutputStream</code>, 由于<code>close()</code>方法执行在<code>out.toString(&quot;ISO-8859-1&quot;)</code>之后，因此压缩并没有真正的被执行，然而对于<code>ZipHelper.compress()</code>方法并没有感知，而是返回了没有压缩成功的字符串，从而造成在解压的时候报错。</p>
<h3 id="为什么会引起的堆外内存泄漏"><a href="#为什么会引起的堆外内存泄漏" class="headerlink" title="为什么会引起的堆外内存泄漏"></a>为什么会引起的堆外内存泄漏</h3><p>通过最开始的代码我们可以看出，在没有发生异常的情况下，<code>compress()</code>方法是可以正常的关闭流的，所以内存泄露的根源应该是在<code>uncompress()</code>方法，通过跟踪<code>GZIPInputStream</code>的构造函数和<code>close()</code>应该很快就能找到答案。</p>
<p>下面是申请堆外内存和释放堆外内存的过程调用图，可以对比代码参考<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcly1flnrogyecdj316u0mudiq.jpg" alt="堆外内存调用释放流程图"></p>
<p>由于篇幅的原因就不将JDK源码注释一同贴上来了，感兴趣的同学可以按图索骥，找到对应的注释。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//java.util.zip.GZIPInputStream.java</span></span><br><span class="line"><span class="keyword">public</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GZIPInputStream</span> <span class="keyword">extends</span> <span class="title">InflaterInputStream</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">GZIPInputStream</span><span class="params">(InputStream in)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>(in, <span class="number">512</span>); <span class="comment">//调用下面的构造函数</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">GZIPInputStream</span><span class="params">(InputStream in, <span class="keyword">int</span> size)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(in, <span class="keyword">new</span> Inflater(<span class="keyword">true</span>), size); <span class="comment">//新建 Inflater 对象</span></span><br><span class="line">        usesDefaultInflater = <span class="keyword">true</span>;</span><br><span class="line">        readHeader(in);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">close</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (!closed) &#123;</span><br><span class="line">            <span class="keyword">super</span>.close(); <span class="comment">//这里的父类是java.util.zip.InflaterInputStream</span></span><br><span class="line">            eos = <span class="keyword">true</span>;</span><br><span class="line">            closed = <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//java.util.zip.Inflater.java</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Inflater</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Inflater</span><span class="params">(<span class="keyword">boolean</span> nowrap)</span> </span>&#123;</span><br><span class="line">        zsRef = <span class="keyword">new</span> ZStreamRef(init(nowrap));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Closes the decompressor and discards any unprocessed input.</span></span><br><span class="line"><span class="comment">     * This method should be called when the decompressor is no longer</span></span><br><span class="line"><span class="comment">     * being used, but will also be called automatically by the finalize()</span></span><br><span class="line"><span class="comment">     * method. Once this method is called, the behavior of the Inflater</span></span><br><span class="line"><span class="comment">     * object is undefined.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">end</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">synchronized</span> (zsRef) &#123;</span><br><span class="line">            <span class="keyword">long</span> addr = zsRef.address();</span><br><span class="line">            zsRef.clear();</span><br><span class="line">            <span class="keyword">if</span> (addr != <span class="number">0</span>) &#123;</span><br><span class="line">                end(addr);</span><br><span class="line">                buf = <span class="keyword">null</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 此处调用了 Native 方法</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">native</span> <span class="keyword">static</span> <span class="keyword">long</span> <span class="title">init</span><span class="params">(<span class="keyword">boolean</span> nowrap)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">native</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">end</span><span class="params">(<span class="keyword">long</span> addr)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//java.util.zip.InflaterInputStream.java</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">InflaterInputStream</span> <span class="keyword">extends</span> <span class="title">FilterInputStream</span> </span>&#123;</span><br><span class="line">  <span class="comment">/**</span></span><br><span class="line"><span class="comment">   * Closes this input stream and releases any system resources associated</span></span><br><span class="line"><span class="comment">   * with the stream.</span></span><br><span class="line"><span class="comment">   * <span class="doctag">@exception</span> IOException if an I/O error has occurred</span></span><br><span class="line"><span class="comment">   */</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">close</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">      <span class="keyword">if</span> (!closed) &#123;</span><br><span class="line">          <span class="keyword">if</span> (usesDefaultInflater)</span><br><span class="line">              inf.end();</span><br><span class="line">          in.close();</span><br><span class="line">          closed = <span class="keyword">true</span>;</span><br><span class="line">      &#125;</span><br><span class="line">  &#125;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>openJDK 中 JVM 关于这个本地方法的实现</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">JNIEXPORT jlong JNICALL</span><br><span class="line"><span class="built_in">Java_java_util_zip_Inflater_init</span>(JNIEnv *env, jclass cls, jboolean nowrap)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//此处使用 calloc 申请了堆外内存</span></span><br><span class="line">    z_stream *strm = <span class="built_in">calloc</span>(<span class="number">1</span>, <span class="built_in"><span class="keyword">sizeof</span></span>(z_stream));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (strm == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="built_in">JNU_ThrowOutOfMemoryError</span>(env, <span class="number">0</span>);</span><br><span class="line">        <span class="keyword">return</span> jlong_zero;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">char</span> *msg;</span><br><span class="line">        <span class="keyword">int</span> ret = <span class="built_in">inflateInit2</span>(strm, nowrap ? -MAX_WBITS : MAX_WBITS);</span><br><span class="line">        <span class="built_in"><span class="keyword">switch</span></span> (ret) &#123;</span><br><span class="line">          <span class="keyword">case</span> Z_OK:</span><br><span class="line">            <span class="keyword">return</span> <span class="built_in">ptr_to_jlong</span>(strm);</span><br><span class="line">          <span class="keyword">case</span> Z_MEM_ERROR:</span><br><span class="line">            <span class="built_in">free</span>(strm);</span><br><span class="line">            <span class="built_in">JNU_ThrowOutOfMemoryError</span>(env, <span class="number">0</span>);</span><br><span class="line">            <span class="keyword">return</span> jlong_zero;</span><br><span class="line">          <span class="keyword">default</span>:</span><br><span class="line">            msg = ((strm-&gt;msg != <span class="literal">NULL</span>) ? strm-&gt;msg :</span><br><span class="line">                   (ret == Z_VERSION_ERROR) ?</span><br><span class="line">                   <span class="string">&quot;zlib returned Z_VERSION_ERROR: &quot;</span></span><br><span class="line">                   <span class="string">&quot;compile time and runtime zlib implementations differ&quot;</span> :</span><br><span class="line">                   (ret == Z_STREAM_ERROR) ?</span><br><span class="line">                   <span class="string">&quot;inflateInit2 returned Z_STREAM_ERROR&quot;</span> :</span><br><span class="line">                   <span class="string">&quot;unknown error initializing zlib library&quot;</span>);</span><br><span class="line">            <span class="built_in">free</span>(strm);</span><br><span class="line">            <span class="built_in">JNU_ThrowInternalError</span>(env, msg);</span><br><span class="line">            <span class="keyword">return</span> jlong_zero;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">JNIEXPORT <span class="keyword">void</span> JNICALL</span><br><span class="line"><span class="built_in">Java_java_util_zip_Inflater_end</span>(JNIEnv *env, jclass cls, jlong addr)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">inflateEnd</span>(<span class="built_in">jlong_to_ptr</span>(addr)) == Z_STREAM_ERROR) &#123;</span><br><span class="line">        <span class="built_in">JNU_ThrowInternalError</span>(env, <span class="number">0</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">free</span>(<span class="built_in">jlong_to_ptr</span>(addr)); <span class="comment">//此处释放堆外内存</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h3 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h3><ul>
<li><a href="http://blog.csdn.net/Revivedsun/article/details/52073554">java.io.EOFException: Unexpected end of ZLIB input stream异常处理</a></li>
</ul>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>tech</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring 中 Transactional 注解原理</title>
    <url>/transactional-of-spring.html</url>
    <content><![CDATA[<p>利用 Spring 框架可以很容易的使用注解的方式来使用事务，为我们的开发带来了巨大的便利，这种便利的实现是通过 Spring 本身的一系列机制来实现的，主要包含动态代理和 Spring Bean 的加载过程。本文将深入源码，揭开层层面纱…</p>
<span id="more"></span>
<h2 id="Spring-Transactional-的使用"><a href="#Spring-Transactional-的使用" class="headerlink" title="Spring @Transactional 的使用"></a>Spring @Transactional 的使用</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Transactional(rollbackFor = Exception.class, propagation = Propagation.REQUIRED)</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">methodInTransaction</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 对数据库的一些操作</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们只需要在对应的方法上使用 <code>@Transactional</code> 注解即可让这个方法在事务中执行。这里需要注意的是，如果是在一个类中的两个方法，事务是不会生效的。举例：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.springframework.transaction.annotation.Propagation;</span><br><span class="line"><span class="keyword">import</span> org.springframework.transaction.annotation.Transactional;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Created by zhangbo on 2019-02-28.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TransactionTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">methodNotIntransaction</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.methodInTransaction(); <span class="comment">// 此时被调用的方法 methodInTransaction 的事务并不会生效</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Transactional(rollbackFor = Exception.class, propagation = Propagation.REQUIRED)</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">methodInTransaction</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 对数据库的一些操作</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>为何不会生效？是因为这样的调用不会经过 Spring 的代理，无法通过 Spring 的 advisor 来拦截数据库操作请求。</p>
<h2 id="Spring-BeanPostProcessor"><a href="#Spring-BeanPostProcessor" class="headerlink" title="Spring BeanPostProcessor"></a>Spring BeanPostProcessor</h2><p>首先我们来了解一下 Spring Bean 的生命周期</p>
<blockquote>
<ol>
<li>调用InstantiationAwareBeanPostProcessor的postProcessBeforeInstantiation(Class&lt;?&gt; beanClass, String beanName)</li>
<li>bean实例化</li>
<li>调用InstantiationAwareBeanPostProcessor的postProcessAfterInstantiation(Object bean, String beanName)</li>
<li>调用InstantiationAwareBeanPostProcessor的postProcessPropertyValues(PropertyValues pvs, PropertyDescriptor[] pds, Object bean, String beanName)</li>
<li>bean注入properties</li>
<li>分别调用BeanNameAware,BeanClassLoaderAware,BeanFactoryAware中的方法</li>
<li>调用BeanPostProcessor的postProcessBeforeInitialization(Object bean, String beanName)</li>
<li>调用InitializingBean的afterPropertiesSet方法</li>
<li>调用自定义初始化方法</li>
<li>调用BeanPostProcessor的postProcessAfterInitialization(Object bean, String beanName)</li>
<li>调用DisposableBean的destroy()方法</li>
<li>调用自定义销毁方法</li>
</ol>
<p>作者：土豆肉丝盖浇饭</p>
<p>链接：<a href="https://www.jianshu.com/p/6d5c58168493">https://www.jianshu.com/p/6d5c58168493</a></p>
<p>来源：简书</p>
<p>简书著作权归作者所有，任何形式的转载都请联系作者获得授权并注明出处。</p>
</blockquote>
<p>通过上面的流程我们可以看到，BeanPostProcessor 中的两个方法 <code>postProcessBeforeInitialization</code>和<code>postProcessAfterInitialization</code> 分别在 bean 调用 init 方法前后调用。其中对于对象的代理就是在 <code>postProcessAfterInitialization</code> 方法中完成的，用代理的 bean 来替换原来的 bean</p>
<p>默认情况下，<code>BeanPostProcessor</code> 的职能是通过默认实现类 <code>DefaultAdvisorAutoProxyCreator</code>实现的，类 <code>DefaultAdvisorAutoProxyCreator</code>继承自<code>AbstractAdvisorAutoProxyCreator</code>该类的继承关系如下图</p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcgy1g0m8bd5ifmj31zi0m276o.jpg" alt="AbstractAutoProxyCreator继承关系"></p>
<h2 id="DefaultAdvisorAutoProxyCreator-如何代理被-Transactional注解的方法所属类"><a href="#DefaultAdvisorAutoProxyCreator-如何代理被-Transactional注解的方法所属类" class="headerlink" title="DefaultAdvisorAutoProxyCreator 如何代理被@Transactional注解的方法所属类"></a><code>DefaultAdvisorAutoProxyCreator</code> 如何代理被<code>@Transactional</code>注解的方法所属类</h2><p>来看看 <code>AbstractAutoProxyCreator</code>中发生了什么</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* Create a proxy with the configured interceptors if the bean is</span></span><br><span class="line"><span class="comment">* identified as one to proxy by the subclass.</span></span><br><span class="line"><span class="comment">* <span class="doctag">@see</span> #getAdvicesAndAdvisorsForBean</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="comment">// 类 AbstractAutoProxyCreator</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Object <span class="title">postProcessAfterInitialization</span><span class="params">(<span class="meta">@Nullable</span> Object bean, String beanName)</span> <span class="keyword">throws</span> BeansException </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (bean != <span class="keyword">null</span>) &#123;</span><br><span class="line">        Object cacheKey = getCacheKey(bean.getClass(), beanName);</span><br><span class="line">        <span class="keyword">if</span> (!<span class="keyword">this</span>.earlyProxyReferences.contains(cacheKey)) &#123;</span><br><span class="line">            <span class="keyword">return</span> wrapIfNecessary(bean, beanName, cacheKey); <span class="comment">// 主要逻辑在这里</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> bean;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>跟进 <code>wrapIfNecessary</code> 方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* Wrap the given bean if necessary, i.e. if it is eligible for being proxied.</span></span><br><span class="line"><span class="comment">* <span class="doctag">@param</span> bean the raw bean instance</span></span><br><span class="line"><span class="comment">* <span class="doctag">@param</span> beanName the name of the bean</span></span><br><span class="line"><span class="comment">* <span class="doctag">@param</span> cacheKey the cache key for metadata access</span></span><br><span class="line"><span class="comment">* <span class="doctag">@return</span> a proxy wrapping the bean, or the raw bean instance as-is</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="comment">// 类 AbstractAutoProxyCreator</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> Object <span class="title">wrapIfNecessary</span><span class="params">(Object bean, String beanName, Object cacheKey)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (StringUtils.hasLength(beanName) &amp;&amp; <span class="keyword">this</span>.targetSourcedBeans.contains(beanName)) &#123;</span><br><span class="line">        <span class="keyword">return</span> bean;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (Boolean.FALSE.equals(<span class="keyword">this</span>.advisedBeans.get(cacheKey))) &#123;</span><br><span class="line">        <span class="keyword">return</span> bean;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (isInfrastructureClass(bean.getClass()) || shouldSkip(bean.getClass(), beanName)) &#123;</span><br><span class="line">        <span class="keyword">this</span>.advisedBeans.put(cacheKey, Boolean.FALSE);</span><br><span class="line">        <span class="keyword">return</span> bean;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Create proxy if we have advice.</span></span><br><span class="line">    <span class="comment">// 查找 advice 从而创建代理类</span></span><br><span class="line">    Object[] specificInterceptors = getAdvicesAndAdvisorsForBean(bean.getClass(), beanName, <span class="keyword">null</span>);</span><br><span class="line">    <span class="keyword">if</span> (specificInterceptors != DO_NOT_PROXY) &#123;</span><br><span class="line">        <span class="keyword">this</span>.advisedBeans.put(cacheKey, Boolean.TRUE);</span><br><span class="line">        Object proxy = createProxy(</span><br><span class="line">            bean.getClass(), beanName, specificInterceptors, <span class="keyword">new</span> SingletonTargetSource(bean));</span><br><span class="line">        <span class="keyword">this</span>.proxyTypes.put(cacheKey, proxy.getClass());</span><br><span class="line">        <span class="keyword">return</span> proxy;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">this</span>.advisedBeans.put(cacheKey, Boolean.FALSE);</span><br><span class="line">    <span class="keyword">return</span> bean;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// AbstractAdvisorAutoProxyCreator 类</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="meta">@Nullable</span></span><br><span class="line"><span class="keyword">protected</span> Object[] getAdvicesAndAdvisorsForBean(</span><br><span class="line">    Class&lt;?&gt; beanClass, String beanName, <span class="meta">@Nullable</span> TargetSource targetSource) &#123;</span><br><span class="line"></span><br><span class="line">    List&lt;Advisor&gt; advisors = findEligibleAdvisors(beanClass, beanName);</span><br><span class="line">    <span class="keyword">if</span> (advisors.isEmpty()) &#123;</span><br><span class="line">        <span class="keyword">return</span> DO_NOT_PROXY;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> advisors.toArray();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* Find all eligible Advisors for auto-proxying this class.</span></span><br><span class="line"><span class="comment">* <span class="doctag">@param</span> beanClass the clazz to find advisors for</span></span><br><span class="line"><span class="comment">* <span class="doctag">@param</span> beanName the name of the currently proxied bean</span></span><br><span class="line"><span class="comment">* <span class="doctag">@return</span> the empty List, not &#123;<span class="doctag">@code</span> null&#125;,</span></span><br><span class="line"><span class="comment">* if there are no pointcuts or interceptors</span></span><br><span class="line"><span class="comment">* <span class="doctag">@see</span> #findCandidateAdvisors</span></span><br><span class="line"><span class="comment">* <span class="doctag">@see</span> #sortAdvisors</span></span><br><span class="line"><span class="comment">* <span class="doctag">@see</span> #extendAdvisors</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// AbstractAdvisorAutoProxyCreator 类</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> List&lt;Advisor&gt; <span class="title">findEligibleAdvisors</span><span class="params">(Class&lt;?&gt; beanClass, String beanName)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 获取 候选 advisor </span></span><br><span class="line">    <span class="comment">// BeanFactoryUtils.beanNamesForTypeIncludingAncestors(this.beanFactory, Advisor.class, true, false);</span></span><br><span class="line">    List&lt;Advisor&gt; candidateAdvisors = findCandidateAdvisors();</span><br><span class="line">    <span class="comment">// 筛选 可用的 advisor</span></span><br><span class="line">    List&lt;Advisor&gt; eligibleAdvisors = findAdvisorsThatCanApply(candidateAdvisors, beanClass, beanName);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 此处会获取名称为org.springframework.transaction.interceptor.TransactionInterceptor#0的拦截器</span></span><br><span class="line">    extendAdvisors(eligibleAdvisors);</span><br><span class="line">    <span class="keyword">if</span> (!eligibleAdvisors.isEmpty()) &#123;</span><br><span class="line">        eligibleAdvisors = sortAdvisors(eligibleAdvisors);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> eligibleAdvisors;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们来看看生成的动态类是什么样子的？</p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcgy1g0mbz0xk0rj31yg0mytgq.jpg" alt="Jietu20190228-174753@2x"></p>
<p>其中有一个 advisor 为</p>
<p>adviceBeanName：org.springframework.transaction.interceptor.TransactionInterceptor#0</p>
<h2 id="TransactionInterceptor-类"><a href="#TransactionInterceptor-类" class="headerlink" title="TransactionInterceptor 类"></a>TransactionInterceptor 类</h2><p>我们此时来看看 <code>org.springframework.transaction.interceptor.TransactionInterceptor</code> 里究竟是如何执行 SQL 语句的。我们需要关注的方法为 <code>invoke</code> 方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Object <span class="title">invoke</span><span class="params">(<span class="keyword">final</span> MethodInvocation invocation)</span> <span class="keyword">throws</span> Throwable </span>&#123;</span><br><span class="line">    <span class="comment">// Work out the target class: may be &#123;@code null&#125;.</span></span><br><span class="line">    <span class="comment">// The TransactionAttributeSource should be passed the target class</span></span><br><span class="line">    <span class="comment">// as well as the method, which may be from an interface.</span></span><br><span class="line">    Class&lt;?&gt; targetClass = (invocation.getThis() != <span class="keyword">null</span> ? AopUtils.getTargetClass(invocation.getThis()) : <span class="keyword">null</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Adapt to TransactionAspectSupport&#x27;s invokeWithinTransaction...</span></span><br><span class="line">    <span class="keyword">return</span> invokeWithinTransaction(invocation.getMethod(), targetClass, <span class="keyword">new</span> InvocationCallback() &#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> Object <span class="title">proceedWithInvocation</span><span class="params">()</span> <span class="keyword">throws</span> Throwable </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> invocation.proceed();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>查看 <code>invokeWithinTransaction</code> 方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// TransactionAspectSupport.java</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> Object <span class="title">invokeWithinTransaction</span><span class="params">(Method method, Class&lt;?&gt; targetClass, <span class="keyword">final</span> InvocationCallback invocation)</span></span></span><br><span class="line"><span class="function">		<span class="keyword">throws</span> Throwable </span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// If the transaction attribute is null, the method is non-transactional.</span></span><br><span class="line">	<span class="keyword">final</span> TransactionAttribute txAttr = getTransactionAttributeSource().getTransactionAttribute(method, targetClass);</span><br><span class="line">	<span class="keyword">final</span> PlatformTransactionManager tm = determineTransactionManager(txAttr);</span><br><span class="line">	<span class="keyword">final</span> String joinpointIdentification = methodIdentification(method, targetClass);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// txAttr（事务属性）为空或者tm（事务管理器）为空时，是声明式事务</span></span><br><span class="line">	<span class="keyword">if</span> (txAttr == <span class="keyword">null</span> || !(tm <span class="keyword">instanceof</span> CallbackPreferringPlatformTransactionManager)) &#123;</span><br><span class="line">		<span class="comment">// Standard transaction demarcation with getTransaction and commit/rollback calls.</span></span><br><span class="line">        <span class="comment">// 获取该方法上事务的信息</span></span><br><span class="line">		TransactionInfo txInfo = createTransactionIfNecessary(tm, txAttr, joinpointIdentification);</span><br><span class="line">		Object retVal = <span class="keyword">null</span>;</span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">			<span class="comment">// This is an around advice: Invoke the next interceptor in the chain.</span></span><br><span class="line">			<span class="comment">// This will normally result in a target object being invoked.</span></span><br><span class="line">			retVal = invocation.proceedWithInvocation();</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">			<span class="comment">// target invocation exception</span></span><br><span class="line">            <span class="comment">// 事务回滚</span></span><br><span class="line">			completeTransactionAfterThrowing(txInfo, ex);</span><br><span class="line">			<span class="keyword">throw</span> ex;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">finally</span> &#123;</span><br><span class="line">			cleanupTransactionInfo(txInfo);</span><br><span class="line">		&#125;</span><br><span class="line">		commitTransactionAfterReturning(txInfo);</span><br><span class="line">		<span class="keyword">return</span> retVal;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">else</span> &#123;</span><br><span class="line">		<span class="comment">// It&#x27;s a CallbackPreferringPlatformTransactionManager: pass a TransactionCallback in.</span></span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">			Object result = ((CallbackPreferringPlatformTransactionManager) tm).execute(txAttr,</span><br><span class="line">					<span class="keyword">new</span> TransactionCallback&lt;Object&gt;() &#123;</span><br><span class="line">						<span class="meta">@Override</span></span><br><span class="line">						<span class="function"><span class="keyword">public</span> Object <span class="title">doInTransaction</span><span class="params">(TransactionStatus status)</span> </span>&#123;</span><br><span class="line">							TransactionInfo txInfo = prepareTransactionInfo(tm, txAttr, joinpointIdentification, status);</span><br><span class="line">							<span class="keyword">try</span> &#123;</span><br><span class="line">								<span class="keyword">return</span> invocation.proceedWithInvocation();</span><br><span class="line">							&#125;</span><br><span class="line">							<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">								<span class="keyword">if</span> (txAttr.rollbackOn(ex)) &#123;</span><br><span class="line">									<span class="comment">// A RuntimeException: will lead to a rollback.</span></span><br><span class="line">									<span class="keyword">if</span> (ex <span class="keyword">instanceof</span> RuntimeException) &#123;</span><br><span class="line">										<span class="keyword">throw</span> (RuntimeException) ex;</span><br><span class="line">									&#125;</span><br><span class="line">									<span class="keyword">else</span> &#123;</span><br><span class="line">										<span class="keyword">throw</span> <span class="keyword">new</span> ThrowableHolderException(ex);</span><br><span class="line">									&#125;</span><br><span class="line">								&#125;</span><br><span class="line">								<span class="keyword">else</span> &#123;</span><br><span class="line">									<span class="comment">// A normal return value: will lead to a commit.</span></span><br><span class="line">									<span class="keyword">return</span> <span class="keyword">new</span> ThrowableHolder(ex);</span><br><span class="line">								&#125;</span><br><span class="line">							&#125;</span><br><span class="line">							<span class="keyword">finally</span> &#123;</span><br><span class="line">								cleanupTransactionInfo(txInfo);</span><br><span class="line">							&#125;</span><br><span class="line">						&#125;</span><br><span class="line">					&#125;);</span><br><span class="line"></span><br><span class="line">			<span class="comment">// Check result: It might indicate a Throwable to rethrow.</span></span><br><span class="line">			<span class="keyword">if</span> (result <span class="keyword">instanceof</span> ThrowableHolder) &#123;</span><br><span class="line">				<span class="keyword">throw</span> ((ThrowableHolder) result).getThrowable();</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				<span class="keyword">return</span> result;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (ThrowableHolderException ex) &#123;</span><br><span class="line">			<span class="keyword">throw</span> ex.getCause();</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>至此，我们基本了解了 Spring 声明式事务的工作流程</p>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>tech</tag>
      </tags>
  </entry>
  <entry>
    <title>Java类加载器的准备和初始化</title>
    <url>/preparation-and-init-of-classloader.html</url>
    <content><![CDATA[<p>好久没有更新博客了，昨天在浏览微信公众号的时候看到了一道面试题，给出一个包含 main 方法的类，一些输出语句，让读者判断这些语句的输出顺序，其实也就是考察读者的对于 Java 类的初始化的顺序，看到这道题目的时候，我也就按照自己的理解得出了一个顺序，可是和答案却大相径庭，原因就在于对于 Java 类加载器在加载类时候的两个阶段：<strong>准备 </strong>和 <strong>初始化</strong> 没有足够的理解。今天我们在本文中就来聊聊类加载的这两个阶段</p>
<span id="more"></span>
<h2 id="那道题"><a href="#那道题" class="headerlink" title="那道题"></a>那道题</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">StaticTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        staticFunction();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> StaticTest st = <span class="keyword">new</span> StaticTest();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;1&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;2&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    StaticTest() &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;3&quot;</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;a=&quot;</span> + a + <span class="string">&quot;,b=&quot;</span> + b);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">staticFunction</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;4&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">110</span>;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">int</span> b = <span class="number">112</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>如果你能很简单的得到这段代码的输出结果，也就无需继续看下去了，如果结果和你得出的结果有差异，那不妨看看类加载器是如何来对这些变量做处理的。</p>
<p>答案如下</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">2</span><br><span class="line">3</span><br><span class="line">a&#x3D;110,b&#x3D;0</span><br><span class="line">1</span><br><span class="line">4</span><br></pre></td></tr></table></figure>
<p>看那道题目之前我们先看些简单的吧！</p>
<h2 id="类加载"><a href="#类加载" class="headerlink" title="类加载"></a>类加载</h2><p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcgy1g1fanowdz6j311q0fqwqu.jpg" alt="类加载过程"></p>
<p>上图是类加载的过程，涉及到类变量和类实例变量的初始化和赋值都在<strong>准备</strong>和<strong>初始化</strong>这两个阶段，因此我们只讨论这两部分。</p>
<h3 id="准备"><a href="#准备" class="headerlink" title="准备"></a>准备</h3><p>此时是为类变量（静态变量或者静态代码块）分配内存并进行初始化的阶段。这些变量都是分配在<strong>方法区</strong>的，而不是在堆中。并且这里的初始化指的是数据类型的<strong>零值</strong>，对于基本数据类型就是0，对于引用类型则是 <code>null</code>。这里我们举两个例子</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span> value = <span class="number">123</span>;</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> Person person = <span class="keyword">new</span> Person(<span class="string">&quot;Jacob&quot;</span>);</span><br></pre></td></tr></table></figure>
<p>在准备阶段，<code>value</code> 会被初始化为0，<code>person</code> 会被初始化为 <code>null</code>，将 value 设置为 123 ，是在类加载的初始化阶段中，在<code>&lt;clinit&gt;()</code> 方法中。但是有一种情况除外，如果一个字段是常量，这个字段就会在准备阶段赋值，如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> x = <span class="number">999</span>; <span class="comment">// x会在准备阶段被赋值为999</span></span><br></pre></td></tr></table></figure>
<h3 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h3><p>在准备阶段被初始化为零值的那些变量会在初始化阶段赋值为在代码中定义的值。初始化阶段其实就是执行 <code>&lt;clinit&gt;()</code> 方法。 <code>&lt;clinit&gt;()</code> 方法是做的就是类变量的赋值动作和静态语句块。<strong>并且变量的赋值顺序就是在代码源文件中的出现顺序</strong>，静态语句块只能访问到定义在它之前的变量，定义在它之后的变量可以赋值，但是不能访问。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span> value = <span class="number">123</span>;</span><br><span class="line"><span class="keyword">static</span> &#123; <span class="comment">// 静态语句块</span></span><br><span class="line">  System.out.println(<span class="string">&quot;This is a static code block&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">  <span class="keyword">static</span> &#123;</span><br><span class="line">    i = <span class="number">0</span>;  <span class="comment">// 赋值可以通过</span></span><br><span class="line">    Ssytem.out.print(i); <span class="comment">// 引用无法通过编译（提示“非法向前引用”）</span></span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">static</span> <span class="keyword">int</span> i = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="顺便说说实例的初始化顺序"><a href="#顺便说说实例的初始化顺序" class="headerlink" title="顺便说说实例的初始化顺序"></a>顺便说说实例的初始化顺序</h2><p>实例的初始化函数是 <code>&lt;init&gt;()</code>，它的执行顺序是：</p>
<blockquote>
<ol>
<li>父类变量初始化 和  父类语句块  （顺序是源码顺序）</li>
<li>父类构造函数 </li>
<li>子类变量初始化 和 子类语句块 （顺序是源码顺序）</li>
<li>子类构造函数</li>
</ol>
</blockquote>
<p>对应的类初始化函数 <code>&lt;client&gt;()</code> 的执行顺序是：</p>
<blockquote>
<ol>
<li>父类静态变量初始化 和 静态语句块 （顺序是源码顺序）</li>
<li>子类静态变量初始化 和 子类静态语句块（顺序是源码顺序）</li>
</ol>
</blockquote>
<p>综合上面两个函数可以得到一个包含所有步骤的顺序</p>
<blockquote>
<ol>
<li>父类静态变量初始化 和 静态语句块 （顺序是源码顺序）</li>
<li>子类静态变量初始化 和 子类静态语句块（顺序是源码顺序）</li>
<li>父类变量初始化 和  父类语句块  （顺序是源码顺序）</li>
<li>父类构造函数 </li>
<li>子类变量初始化 和 子类语句块 （顺序是源码顺序）</li>
<li>子类构造函数</li>
</ol>
</blockquote>
<p>注意这里说的是开始顺序，并不是结束顺序，实例的初始化可以在类的初始化之前完成，也就是说，<code>&lt;clinit&gt;()</code> 可能发生在 <code>&lt;init&gt;()</code> 之后，文章开头的例子就是这样。</p>
<h2 id="题目解析"><a href="#题目解析" class="headerlink" title="题目解析"></a>题目解析</h2><p>在本文的例子中，当准备阶段完成后，类变量会被赋值为以下值：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">StaticTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        staticFunction();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> StaticTest st = <span class="keyword">new</span> StaticTest(); <span class="comment">// 此时 st 为 null</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;1&quot;</span>); <span class="comment">// 还未执行，这个会在&lt;clinit&gt;()执行, 也就是类加载的初始化阶段</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;2&quot;</span>);<span class="comment">// 还未执行，这个会在&lt;init&gt;()执行</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    StaticTest() &#123; <span class="comment">// 还未执行，这个会在&lt;init&gt;()执行</span></span><br><span class="line">        System.out.println(<span class="string">&quot;3&quot;</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;a=&quot;</span> + a + <span class="string">&quot;,b=&quot;</span> + b);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">staticFunction</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;4&quot;</span>); <span class="comment">// 还未执行，这个会在类加载完成后被调用的时候执行</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">110</span>; <span class="comment">// 还未执行，这个会在&lt;init&gt;()执行</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">int</span> b = <span class="number">112</span>; <span class="comment">// 此时 b 为 0</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>完成准备阶段后，来到了初始化阶段 <code>&lt;clinit&gt;()</code>的前半段，会在 <code>&lt;clinit&gt;()</code>结束前，执行 <code>&lt;init&gt;()</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">StaticTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        staticFunction();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> StaticTest st = <span class="keyword">new</span> StaticTest(); <span class="comment">// 此时 需要为st赋值，因此会进入 StaticTest 的 &lt;init&gt;() 函数</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;1&quot;</span>); <span class="comment">// 还未执行，需要先执行StaticTest 的 &lt;init&gt;() 函数，由于上一条语句需要实例化 StaticTest</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;2&quot;</span>);<span class="comment">// 执行作为第1行输出，因为已经进入了 &lt;init&gt;() 函数 ---------</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    StaticTest() &#123; <span class="comment">// 执行，晚于实例代码块和实例变量</span></span><br><span class="line">        System.out.println(<span class="string">&quot;3&quot;</span>); <span class="comment">//第2行输出 ---------</span></span><br><span class="line">        <span class="comment">// 此时由于构造函数晚于实例代码块和实例变量的赋值，</span></span><br><span class="line">        <span class="comment">// 因此 a=110, 由于&lt;clinit&gt;()没有完成，所以 b=0</span></span><br><span class="line">        System.out.println(<span class="string">&quot;a=&quot;</span> + a + <span class="string">&quot;,b=&quot;</span> + b); <span class="comment">//第3行输出---------</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">staticFunction</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;4&quot;</span>); <span class="comment">// 还未执行，这个会在类加载完成后被调用的时候执行</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">110</span>; <span class="comment">// 执行，被赋值为110</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">int</span> b = <span class="number">112</span>; <span class="comment">// 此时 b 为 0</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>然后是  <code>&lt;clinit&gt;()</code> 的后半段</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">StaticTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        staticFunction();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> StaticTest st = <span class="keyword">new</span> StaticTest(); <span class="comment">// 赋值完成</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;1&quot;</span>); <span class="comment">// 作为第4行输出---------</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;2&quot;</span>);<span class="comment">// 已经执行完毕</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    StaticTest() &#123; <span class="comment">// 已经执行完毕</span></span><br><span class="line">        System.out.println(<span class="string">&quot;3&quot;</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;a=&quot;</span> + a + <span class="string">&quot;,b=&quot;</span> + b); </span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">staticFunction</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;4&quot;</span>); <span class="comment">// 作为第5行输出---------</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">110</span>; <span class="comment">// 已经在&lt;init&gt;()执行完毕</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">int</span> b = <span class="number">112</span>; <span class="comment">// 执行赋值操作， b 为 112</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>至此，对于变量的初始化应该就在比较明确了。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>重点还是记住下面的顺序，同时对 类加载过程有一定的了解。</p>
<blockquote>
<ol>
<li>父类静态变量初始化 和 静态语句块 （顺序是源码顺序）</li>
<li>子类静态变量初始化 和 子类静态语句块（顺序是源码顺序）</li>
<li>父类变量初始化 和  父类语句块  （顺序是源码顺序）</li>
<li>父类构造函数 </li>
<li>子类变量初始化 和 子类语句块 （顺序是源码顺序）</li>
<li>子类构造函数</li>
</ol>
</blockquote>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>JVM</tag>
        <tag>tech</tag>
      </tags>
  </entry>
  <entry>
    <title>三门问题</title>
    <url>/Monty-Hall-problem.html</url>
    <content><![CDATA[<p>前几天有一个关注很久的公众号推送了一篇讲 “三门问题” 的文章。这不是我第一看到这个问题的分析，我一直觉得这个是个伪命题，后面两个门打开后面有大奖的概率都应该是<code>1/2</code>才对呀。直到我去看了一些分析，写了这篇文章。</p>
<span id="more"></span>
<p><img src="../media/HawksbillCrag_ZH-CN4429681235_1920x1080.jpg" alt="HawksbillCrag_ZH-CN4429681235_1920x1080"></p>
<h3 id="什么是三门问题？"><a href="#什么是三门问题？" class="headerlink" title="什么是三门问题？"></a>什么是三门问题？</h3><blockquote>
<p><strong>蒙提霍尔问题</strong>，亦称为<strong>蒙特霍问题</strong>或<strong>三门问题</strong>（英文：Monty Hall problem），是一个源自<a href="https://zh.wikipedia.org/wiki/%E5%8D%9A%E5%BC%88%E8%AB%96">博弈论</a>的<a href="https://zh.wikipedia.org/wiki/%E6%95%B8%E5%AD%B8%E9%81%8A%E6%88%B2">数学游戏</a>问题，大致出自<a href="https://zh.wikipedia.org/wiki/%E7%BE%8E%E5%9C%8B">美国</a>的<a href="https://zh.wikipedia.org/wiki/%E9%9B%BB%E8%A6%96">电视</a><a href="https://zh.wikipedia.org/wiki/%E9%81%8A%E6%88%B2%E7%AF%80%E7%9B%AE">游戏节目</a><em>Let’s Make a Deal</em>。问题的名字来自该节目的主持人<a href="https://zh.wikipedia.org/wiki/%E8%92%99%E8%92%82%C2%B7%E9%9C%8D%E5%B0%94">蒙蒂·霍尔</a>。</p>
<p>这个游戏的玩法是：参赛者会看见三扇关闭了的门，其中一扇的后面有一辆<a href="https://zh.wikipedia.org/wiki/%E6%B1%BD%E8%BB%8A">汽车</a>或者是奖品，选中后面有车的那扇门就可以赢得该汽车或奖品，而另外两扇门后面则各藏有一只<a href="https://zh.wikipedia.org/wiki/%E5%B1%B1%E7%BE%8A">山羊</a>。当参赛者选定了一扇门，但未去开启它的时候，知道门后情形的节目主持人会开启剩下两扇门的其中一扇，露出其中一只山羊。主持人其后会问参赛者要不要换另一扇仍然关上的门。问题是：换另一扇门会否增加参赛者赢得汽车的几率？如果严格按照上述的条件的话，答案是<strong>会</strong>。换门的话，赢得汽车的几率是2/3。</p>
<p>这条问题亦被叫做<strong>蒙提霍尔悖论</strong>：虽然该问题的答案在逻辑上并不自相矛盾，但十分违反直觉。这问题曾引起一阵热烈的讨论。</p>
</blockquote>
<h3 id="理论证明"><a href="#理论证明" class="headerlink" title="理论证明"></a>理论证明</h3><p>先说一下错误的思维方式：当主持人打开一扇后面有羊的门之后，问题就变成了有两扇门，一扇门里有汽车，一扇门里有羊，选择任何一个门获的汽车的概率必然是相同的，也就是<code>1/2</code>。</p>
<p>上面这种方式的问题就是，打开一扇门后，并不等价于在两扇门里做选择；而是你是否需要转换。（人的直觉往往是不可信的）</p>
<p>当年的<a href="https://zh.wikipedia.org/wiki/%E7%91%AA%E9%BA%97%E8%93%AE%C2%B7%E6%B2%83%E6%96%AF%C2%B7%E8%8E%8E%E5%87%A1%E7%89%B9">玛丽莲·沃斯·莎凡特</a>用整整4个专栏，数百个新闻故事及在小学生课堂模拟的测验来说服她的读者她是正确的。</p>
<p>我们只有最最简单的列出所有可能的情况就可以证明这个问题。</p>
<p><img src="../media/image-20190621171632386.png" alt="image-20190621171632386"></p>
<p>上图可以看到，转换后获胜的概率是<code>2/3</code>。</p>
<p>但是有质疑是为什么第一种情况下不可以分为 A 羊和 B 羊，而是算作一种情况呐。我认为这是由于打开门后出现 A 羊还是 B 羊，并不会影响我们计算的概率，影响概率的是<strong>主持人选羊</strong>这件事。所以 A 羊和 B 羊并不能算作两种情况。</p>
<h3 id="代码佐证"><a href="#代码佐证" class="headerlink" title="代码佐证"></a>代码佐证</h3><p>概率问题通过增大样本容量来获取接近概率的值。所以不如写一段代码来看一下结果</p>
<p>🐑🐑 区分 A 羊和 B 羊</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">boolean</span> CAR = <span class="keyword">true</span>; <span class="comment">// 是车</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">boolean</span> GOAT_A = <span class="keyword">false</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">boolean</span> GOAT_B = <span class="keyword">false</span>;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> ExecutionException, InterruptedException </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> changeSuccess = <span class="number">0</span>; <span class="comment">// 换选换奖次数</span></span><br><span class="line">        <span class="keyword">int</span> totalCount = <span class="number">100_0000</span>; <span class="comment">// 总次数</span></span><br><span class="line"></span><br><span class="line">        List&lt;List&lt;Boolean&gt;&gt; lists = Arrays.asList( <span class="comment">// 所有可能的初始状态</span></span><br><span class="line">                Arrays.asList(CAR, GOAT_A, GOAT_B),</span><br><span class="line">                Arrays.asList(CAR, GOAT_B, GOAT_A),</span><br><span class="line">                Arrays.asList(GOAT_A, CAR, GOAT_B),</span><br><span class="line">                Arrays.asList(GOAT_A, GOAT_B, CAR),</span><br><span class="line">                Arrays.asList(GOAT_B, GOAT_A, CAR),</span><br><span class="line">                Arrays.asList(GOAT_B, CAR, GOAT_A));</span><br><span class="line">        ExecutorService executorService = Executors.newCachedThreadPool();</span><br><span class="line"></span><br><span class="line">        List&lt;Future&lt;Boolean&gt;&gt; resultList = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; totalCount; i++) &#123;</span><br><span class="line">            Future&lt;Boolean&gt; resultFuture = executorService.submit(() -&gt; &#123;</span><br><span class="line">                <span class="keyword">int</span> randomIndex = ((<span class="keyword">int</span>) (Math.random() * <span class="number">100</span>)) % lists.size();</span><br><span class="line">                List&lt;Boolean&gt; doors = lists.get(randomIndex); <span class="comment">// 随机选择一种初始情况</span></span><br><span class="line">                <span class="keyword">int</span> firstChoose = ((<span class="keyword">int</span>) (Math.random() * <span class="number">100</span>)) % <span class="number">3</span>; <span class="comment">// 随机选择一个门</span></span><br><span class="line">                <span class="keyword">int</span> openDoorIndex = <span class="number">0</span>;</span><br><span class="line">                <span class="keyword">do</span> &#123;</span><br><span class="line">                    openDoorIndex = ((<span class="keyword">int</span>) (Math.random() * <span class="number">100</span>)) % <span class="number">3</span>;</span><br><span class="line">                &#125; <span class="keyword">while</span> (openDoorIndex == firstChoose || doors.get(openDoorIndex)); <span class="comment">// 随机打开一扇羊的门</span></span><br><span class="line">                <span class="keyword">int</span> secondChoose = <span class="number">3</span> - firstChoose - openDoorIndex;</span><br><span class="line">                <span class="keyword">return</span> doors.get(secondChoose); <span class="comment">// 返回换选是否获奖</span></span><br><span class="line">            &#125;);</span><br><span class="line">            resultList.add(resultFuture);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (Future&lt;Boolean&gt; booleanFuture : resultList) &#123;</span><br><span class="line">            Boolean aBoolean = booleanFuture.get();</span><br><span class="line">            <span class="keyword">if</span> (aBoolean) &#123;</span><br><span class="line">                changeSuccess++;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;换选获胜次数：&quot;</span> + changeSuccess);</span><br><span class="line">        System.out.println(<span class="string">&quot;总次数：&quot;</span> + totalCount);</span><br><span class="line">        System.out.println(<span class="string">&quot;换选获胜几率：&quot;</span> + (changeSuccess * <span class="number">1.0</span> / totalCount));</span><br><span class="line"></span><br><span class="line">        executorService.shutdown();</span><br><span class="line">        <span class="keyword">while</span> (executorService.isTerminated()) &#123;</span><br><span class="line">            executorService.awaitTermination(<span class="number">100</span>, TimeUnit.SECONDS);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">换选获胜次数：666072</span><br><span class="line">总次数：1000000</span><br><span class="line">换选获胜几率：0.666072</span><br></pre></td></tr></table></figure>
<p>🐑 不区分两只羊</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main2</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">boolean</span> CAR = <span class="keyword">true</span>; <span class="comment">// 是车</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">boolean</span> GOAT = <span class="keyword">false</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> ExecutionException, InterruptedException </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> changeSuccess = <span class="number">0</span>; <span class="comment">// 换选换奖次数</span></span><br><span class="line">        <span class="keyword">int</span> totalCount = <span class="number">100_0000</span>; <span class="comment">// 总次数</span></span><br><span class="line"></span><br><span class="line">        List&lt;List&lt;Boolean&gt;&gt; lists = Arrays.asList( <span class="comment">// 所有可能的初始状态</span></span><br><span class="line">                Arrays.asList(CAR, GOAT, GOAT),</span><br><span class="line">                Arrays.asList(GOAT, CAR, GOAT),</span><br><span class="line">                Arrays.asList(GOAT, GOAT, CAR));</span><br><span class="line">        ExecutorService executorService = Executors.newCachedThreadPool();</span><br><span class="line"></span><br><span class="line">        List&lt;Future&lt;Boolean&gt;&gt; resultList = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; totalCount; i++) &#123;</span><br><span class="line">            Future&lt;Boolean&gt; resultFuture = executorService.submit(() -&gt; &#123;</span><br><span class="line">                <span class="keyword">int</span> randomIndex = ((<span class="keyword">int</span>) (Math.random() * <span class="number">100</span>)) % lists.size();</span><br><span class="line">                List&lt;Boolean&gt; doors = lists.get(randomIndex); <span class="comment">// 随机选择一种初始情况</span></span><br><span class="line">                <span class="keyword">int</span> firstChoose = ((<span class="keyword">int</span>) (Math.random() * <span class="number">100</span>)) % <span class="number">3</span>; <span class="comment">// 随机选择一个门</span></span><br><span class="line">                <span class="keyword">int</span> openDoorIndex = <span class="number">0</span>;</span><br><span class="line">                <span class="keyword">do</span> &#123;</span><br><span class="line">                    openDoorIndex = ((<span class="keyword">int</span>) (Math.random() * <span class="number">100</span>)) % <span class="number">3</span>;</span><br><span class="line">                &#125; <span class="keyword">while</span> (openDoorIndex == firstChoose || doors.get(openDoorIndex)); <span class="comment">// 随机打开一扇羊的门</span></span><br><span class="line">                <span class="keyword">int</span> secondChoose = <span class="number">3</span> - firstChoose - openDoorIndex;</span><br><span class="line">                <span class="keyword">return</span> doors.get(secondChoose); <span class="comment">// 返回换选是否获奖</span></span><br><span class="line">            &#125;);</span><br><span class="line">            resultList.add(resultFuture);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (Future&lt;Boolean&gt; booleanFuture : resultList) &#123;</span><br><span class="line">            Boolean aBoolean = booleanFuture.get();</span><br><span class="line">            <span class="keyword">if</span> (aBoolean) &#123;</span><br><span class="line">                changeSuccess++;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;换选获胜次数：&quot;</span> + changeSuccess);</span><br><span class="line">        System.out.println(<span class="string">&quot;总次数：&quot;</span> + totalCount);</span><br><span class="line">        System.out.println(<span class="string">&quot;换选获胜几率：&quot;</span> + (changeSuccess * <span class="number">1.0</span> / totalCount));</span><br><span class="line"></span><br><span class="line">        executorService.shutdown();</span><br><span class="line">        <span class="keyword">while</span> (executorService.isTerminated()) &#123;</span><br><span class="line">            executorService.awaitTermination(<span class="number">100</span>, TimeUnit.SECONDS);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">换选获胜次数：665600</span><br><span class="line">总次数：1000000</span><br><span class="line">换选获胜几率：0.6656</span><br></pre></td></tr></table></figure>
<h3 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h3><p><a href="https://zh.wikipedia.org/wiki/%E8%92%99%E6%8F%90%E9%9C%8D%E7%88%BE%E5%95%8F%E9%A1%8C">蒙提霍尔问题 - 维基百科，自由的百科全书</a></p>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>算法</tag>
        <tag>概率论</tag>
      </tags>
  </entry>
  <entry>
    <title>分布式一致性协议学习笔记：2PC 和 3PC</title>
    <url>/2PC-and-3PC.html</url>
    <content><![CDATA[<p>了解分布式一致性的一些概念和理论，介绍简单常用的分布式一致性协议。主要介绍二阶段提交和三阶段提交的基本流程，并相互比较。</p>
<span id="more"></span>
<p><img src="../media/Transfagarasan_ZH-CN5760731327_1920x1080.jpg" alt="Transfagarasan_ZH-CN5760731327_1920x1080"></p>
<h2 id="分布式的特点"><a href="#分布式的特点" class="headerlink" title="分布式的特点"></a>分布式的特点</h2><ol>
<li>分布性：系统中的机器随意分布。</li>
<li>对等性：组成分布式系统的所有节点都是对等的。</li>
<li>并发性：需要协调并发操作一些共享资源。</li>
<li>缺乏全局时钟：在分布式系统中，很难定义两件事情谁先谁后。</li>
<li>故障总是会发生：任何在设计阶段考虑到的异常情况，一定会在系统实际运行中发生。</li>
</ol>
<h2 id="分布式系统常见问题"><a href="#分布式系统常见问题" class="headerlink" title="分布式系统常见问题"></a>分布式系统常见问题</h2><ol>
<li>通讯异常：单机内存访问往往延时在纳秒数量级（通常是 10ns 左右）；网络则在 0.1～1ms，消息丢失和消息延迟变得非常普遍</li>
<li>网络分区：俗称”脑裂”，部分节点和整个分布式系统失去联系，自己单独组成了一个小集群。</li>
<li>三态：成功、失败和超时。无法预测超时的请求是否到达了接收方，还是在接收方返回的时候丢失了。</li>
</ol>
<h2 id="CAP-和-BASE-理论"><a href="#CAP-和-BASE-理论" class="headerlink" title="CAP 和 BASE 理论"></a>CAP 和 BASE 理论</h2><p>通常可以把一系列的分布式的操作序列称之为子事务。分布式事务也可以被定义为一种嵌套型事物。分布式系统不可能像单机系统那样可以严格满足 ACID 特性，所以要有不同的取舍，因此有了 CAP 和 BASE 理论。</p>
<h3 id="CAP"><a href="#CAP" class="headerlink" title="CAP"></a>CAP</h3><p>一个分布式系统不可能同时满足<strong>一致性（C：Consistency）</strong>、<strong>可用性（A：Availability）</strong>和<strong>分区容错性（P：Partition tolerance）</strong>这三个基本需求。</p>
<p><strong>一致性：</strong> 在分布式环境中，一致性是指数据在多个副本之间是否能够保持一致的特性。数据在发生变化的时候，需要保证发生变化之后，所有的副本都能保持一致。</p>
<p><strong>可用性：</strong>系统提供的服务必须一直处于可运送的状态，对于每一个请求总是能够在有限的时间内返回结果。</p>
<p><strong>分区容错性：</strong> 分布式系统在遇到任何网络分区故障的时候，仍然需要保证对外提供满足一致性和可用性的服务。</p>
<p><img src="https://www.wangbase.com/blogimg/asset/201807/bg2018071607.jpg" alt="CAP 定理示意图"></p>
<p>对于 CAP 的取舍：放弃 P 的通用做法是将所有的数据都放到一个节点，这样可以避免网络分区带来的问题，但是值得注意的是，<strong>放弃了 P 也就等于放弃了系统的扩展性</strong>；放弃 A ：当系统发生故障的时候，允许系统短时间内不可用，需要等待系统恢复后方可继续提供服务； 放弃 C：<strong>放弃 C 并不是说放弃最终一致性</strong>，而是说放弃强一致性，保证系统在某段时间之后能够达到最终一致性。</p>
<p>对于一个分布式系统而言，不同节点之间必然存在网络通讯，因此网络分区问题是不可避免的。同时分布式系统的扩展特性也是无法舍弃的。<strong>因此系统的设计和架构一般都考虑如何权衡 A 和 C。</strong></p>
<h3 id="BASE"><a href="#BASE" class="headerlink" title="BASE"></a>BASE</h3><p>BASE 是 Basically Available（基本可用）、Soft status（软状态）和 Eventually consistent（最终一致性）三个短语的缩写。</p>
<p><strong>基本可用：</strong>在系统出现故障的时候，允许损失部分可用性。比如响应时间上的损失和功能上的损失。</p>
<p><strong>软状态：</strong> 也称为弱状态，表示允许系统中的数据存在中间状态，即允许不同的数据副本之间数据同步存在延迟。</p>
<p><strong>最终一致性：</strong> 经过一段时间的同步之后，系统最终能够达到一个数据一致的状态。</p>
<blockquote>
<p>最终一致性的一些变种：</p>
<ol>
<li>因果一致性：进程 A 更新数据后通知了进程 B，进程 B 必须对修改后的数据可见。</li>
<li>读己之所写：进程 A 对数据做的更改，自己总是能够获得最新的值。</li>
<li>会话一致性：在单个会话中，进程 A 总是能够看到最新的值。</li>
<li>单调读一致性：在系统中读出数据项 Y 的值 y 后，后面的读取中不允许返回比 y 更旧的值。</li>
<li>单调写一致性：系统需要保证来自同一个进程的写操作被顺序的执行。</li>
</ol>
</blockquote>
<h2 id="2PC-与-3PC"><a href="#2PC-与-3PC" class="headerlink" title="2PC 与 3PC"></a>2PC 与 3PC</h2><p>2PC 和 3PC 就是用来保证 CAP 中的 C 或者 BASE 里面的 E 的协议。</p>
<p>当一个事务需要跨越多个分布式节点的时候，需要引入一个成为<strong>“协调者”（Coordinator）</strong>的组件来统一调度所有分布式节点的执行逻辑，这些被调度的分布式节点则被称为<strong>“参与者”（Participant）</strong>。协调者负责调度参与者的行为，并最终决定这些参与者是否真的需要把事务进行真正的提交。由此，衍生出二阶段提交和三阶段提交两种协议。</p>
<h3 id="2PC"><a href="#2PC" class="headerlink" title="2PC"></a>2PC</h3><p>两阶段提交主要包含<strong>投票（Propose）</strong>和<strong>执行（Commit）</strong>两个阶段。</p>
<p>在 Propose 阶段，协调者向所有参与者（voter）发送事务内容，询问是否可以执行事务提交操作，并等待参与者的响应。各个参与者为该事务预留资源，保证在下一个阶段能够提交事务，如果资源可以获取反馈给协调者 agree 响应，反之返回 disagree 响应。</p>
<p><img src="../media/v2-e2f7149a81d9ad3aa46589e25503d688_r.jpg" alt="事务提交流程"></p>
<p><strong>事务提交：</strong>如果在 Propose 阶段，所有参与者都返回的是 agree 信号，那么协调者会发送提交事务的请求；所有参与者收到事务提交请求后会正式执行事务提交操作并释放事务执行期间占用的资源，执行完成后将事务执行结果反馈给协调者；协调者收到所有参与者的反馈信息后，事务执行完毕。</p>
<p><img src="../media/v2-d40abfa365ed84e84e264ba13900f64b_hd.jpg" alt="事务中断流程"></p>
<p><strong>事务中断：</strong> 如果在 Propose 阶段有参与者返回 disagree 或者有的节点超时未返回，没有投票达成一致，那么协调者会向所有参与者发送回滚请求；参与者收到回滚请求之后会释放掉在 Propose 阶段占用的资源，然后反馈信号给协调者；协调者收到反馈消息之后，完成事务终端。</p>
<hr>
<p>二阶段提交协议的优点是：原理简单，实现方便。</p>
<p>二阶段提交协议的缺点是：</p>
<ol>
<li>同步阻塞：2PC 协议的各个阶段都是同步阻塞的，任何节点故障都有会导致事务提交失败。</li>
<li>单点问题：一旦协调者出现问题，将无法提交事务。如果协调者在收到参与者的 Propose 确认之后，发送 Commit 信号之前发生故障，那么所有参与者占用的资源将无法得到释放。</li>
<li>数据不一致：如果在 Commit 阶段部分参与者发生了故障，导致事务无法提交，但是正常的参与者提交了事务。导致数据不一致。</li>
</ol>
<blockquote>
<p>假设coordinator和voter3都在Commit这个阶段crash了, 而voter1和voter2没有收到commit消息. 这时候voter1和voter2就陷入了一个困境. 因为他们并不能判断现在是两个场景中的哪一种: </p>
<ol>
<li>上轮全票通过然后 voter3 第一个收到了 commit 的消息并在 commit 操作之后 crash 了。</li>
<li>上轮 voter3 反对所以干脆没有通过。</li>
</ol>
<p><img src="../media/v2-a9e4ef8b9082ffdf76bc426e61ba3ed2_hd.jpg" alt="coordinator和voter3 crash, voter{1,2}无法判断当前状态而卡死"></p>
</blockquote>
<hr>
<h3 id="3PC"><a href="#3PC" class="headerlink" title="3PC"></a>3PC</h3><p>三阶段提交是在二阶段提交的基础上进行的改进，将二阶段提交的 Propose Phase 一分为二，形成了 CanCommit、PreCommit 和 doCommit 三个阶段。</p>
<p><img src="../media/v2-28c17c86e689007015a4853f0d0c4a89_hd-3021775.jpg" alt="3PC, coordinator提议通过, voter{1,2,3}达成新的共识"></p>
<p><strong>CanCommit：</strong>协调者向所有参与者询问是否可以执行事务提交操作；各参与者想协调者反馈是否可以提交事务。</p>
<p><strong>PreCommit：</strong> </p>
<ul>
<li>如果所有参与者反馈的都是 agree， 那么就会执行事务预提交。协调者发送 PreCommit 请求，参与者收到请求之后进行资源的分配工作，最后将资源分配结果反馈这协调者。等待后面的  commit 命令或者 abort 命令。</li>
<li>如果任意一个参与者返回了 disagree，或者在协调者等待超时之后。协调者将会向所有参与者节点发送 abort 请求。</li>
</ul>
<p><strong>doCommit：</strong> </p>
<ul>
<li>如果上面 PreCommit 全部成功，即协调者收到了全部参与者的成功反馈，那么协调者将会发送 doCommit 请求；参与者接收到 doCommit 会正式提交事务然后释放事务执行期间占用的资源，反馈事务提交结果给协调者；协调者收到参与者反馈的消息后，完成事务。</li>
<li>如果上面的 PreCommit 有部分失败反馈，或者是等待超时就会中断事务。协调者会向所有参与者节点发送 abort 请求；参与者收到 abort 请求后执行回滚操作，并反馈给协调者；协调者收到反馈消息后，完成事务的中断。</li>
</ul>
<p>需要注意的是，在 doCommit 阶段，可能出现下面的两种故障：</p>
<ul>
<li>协调者出现问题</li>
<li>协调者和参与者之间网络故障</li>
</ul>
<p>无论出现上面那种情况，都会造成从黁税额无法及时收到来自协调者的 doCommit 请求或者是 abort 请求，针对这种情况，参与者会在等待超时之后，继续事务的提交操作。</p>
<hr>
<p>三阶段提交协议的优点是：相比于 2PC 能够在单点故障后继续达成一致。</p>
<p>三阶段提交协议的缺点是：引入了新的问题，如果某个参与者与协调者出现在不同的网络分区，那么该参与者会提交事务，有可能出现数据不一致的情况。</p>
<hr>
<h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="https://zhuanlan.zhihu.com/p/35298019">漫话分布式系统共识协议: 2PC/3PC篇 - 知乎</a></p>
<p><a href="https://www.amazon.cn/dp/B00RECRKPK">《从Paxos到Zookeeper:分布式一致性原理与实践》 倪超【摘要 书评 试读】图书</a></p>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>分布式</tag>
        <tag>一致性协议</tag>
        <tag>CAP</tag>
        <tag>BASE</tag>
      </tags>
  </entry>
  <entry>
    <title>利用 JavaSE 创建简单 HTTP Server</title>
    <url>/how-to-use-java-se-create-simple-httpserver.html</url>
    <content><![CDATA[<p>在工作中经常会遇到简单实用 HTTP server 的场景，可是又不需要 Spring MVC 或者 web容器那样重的东西。这种情况下 JavaSE 就可以满足我们的要求了。<br><span id="more"></span></p>
<h1 id="上代码"><a href="#上代码" class="headerlink" title="上代码"></a>上代码</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> web;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.sun.net.httpserver.Headers;</span><br><span class="line"><span class="keyword">import</span> com.sun.net.httpserver.HttpExchange;</span><br><span class="line"><span class="keyword">import</span> com.sun.net.httpserver.HttpHandler;</span><br><span class="line"><span class="keyword">import</span> com.sun.net.httpserver.HttpServer;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Service;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> javax.annotation.PostConstruct;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> java.io.OutputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.OutputStreamWriter;</span><br><span class="line"><span class="keyword">import</span> java.net.HttpURLConnection;</span><br><span class="line"><span class="keyword">import</span> java.net.InetSocketAddress;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Created by zhangbo54 on 2019-01-24.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SimpleServer</span> </span>&#123;</span><br><span class="line">    <span class="meta">@PostConstruct</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">initHttpServer</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        HttpServer server = HttpServer.create(<span class="keyword">new</span> InetSocketAddress(<span class="number">8418</span>), <span class="number">0</span>);</span><br><span class="line">        server.createContext(<span class="string">&quot;/test&quot;</span>, <span class="keyword">new</span> MyHandler());</span><br><span class="line">        server.setExecutor(<span class="keyword">null</span>); <span class="comment">// creates a default executor</span></span><br><span class="line">        server.start();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">MyHandler</span> <span class="keyword">implements</span> <span class="title">HttpHandler</span> </span>&#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">handle</span><span class="params">(HttpExchange httpExchange)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;receive&quot;</span>);</span><br><span class="line">            String requestMethod = httpExchange.getRequestMethod();</span><br><span class="line">            <span class="keyword">if</span> (requestMethod.equalsIgnoreCase(<span class="string">&quot;GET&quot;</span>)) &#123;<span class="comment">//客户端的请求是get方法</span></span><br><span class="line">                <span class="comment">//设置服务端响应的编码格式，否则在客户端收到的可能是乱码</span></span><br><span class="line">                Headers responseHeaders = httpExchange.getResponseHeaders();</span><br><span class="line">                responseHeaders.set(<span class="string">&quot;Content-Type&quot;</span>, <span class="string">&quot;text/html;charset=utf-8&quot;</span>);</span><br><span class="line"></span><br><span class="line">                <span class="comment">//在这里通过httpExchange获取客户端发送过来的消息</span></span><br><span class="line">                <span class="comment">//URI url = httpExchange.getRequestURI();</span></span><br><span class="line">                <span class="comment">//InputStream requestBody = httpExchange.getRequestBody();</span></span><br><span class="line"></span><br><span class="line">                <span class="comment">// 这里获取get请求参数</span></span><br><span class="line">                <span class="comment">// will return string in format similar to this: &quot;field1=value1&amp;field2=value2&amp;field3=value3...&quot;</span></span><br><span class="line">                String queryParams = httpExchange.getRequestURI().getQuery();</span><br><span class="line">                System.out.println(queryParams);</span><br><span class="line"></span><br><span class="line">                String response = <span class="string">&quot;this is server&quot;</span>;</span><br><span class="line"></span><br><span class="line">                httpExchange.sendResponseHeaders(HttpURLConnection.HTTP_OK, response.getBytes(<span class="string">&quot;UTF-8&quot;</span>).length);</span><br><span class="line"></span><br><span class="line">                <span class="keyword">try</span> (OutputStream responseBody = httpExchange.getResponseBody();</span><br><span class="line">                         OutputStreamWriter writer = <span class="keyword">new</span> OutputStreamWriter(responseBody, <span class="string">&quot;UTF-8&quot;</span>)) &#123;</span><br><span class="line">                        writer.write(response);</span><br><span class="line">                    &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><ol>
<li><a href="https://blog.csdn.net/qq_29125669/article/details/77873923">CSDN</a></li>
<li><a href="https://stackoverflow.com/questions/11640025/how-to-obtain-the-query-string-in-a-get-with-java-httpserver-httpexchange">stackoverflow</a></li>
</ol>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>tech</tag>
      </tags>
  </entry>
  <entry>
    <title>到底什么是 Git 三方对比</title>
    <url>/about-git-dff.html</url>
    <content><![CDATA[<p>问题： 为什么我在 gitlab 上提交的MR，代码diff不是两个分支的区别？</p>
<p>带有场景的举例子：</p>
<p>小A 在linke上建立的新的迭代，同时生成了代码分支 <code>dev</code>，然后 A、B两个同学都在这个迭代进行开发。小 A 基于 <code>dev</code> 分支创建了 <code>dev_A</code> 分支，小B基于dev分支创建了<code>dev_B</code> 分支。后来小A将他的变更合并到了 dev 分支，小B在开发过程中由于和master分支有冲突，所以他在本地解决冲突合并了一下 master 分支，然后又合并了一下 dev 分支，然后推送到 antcode 进行 MR，发现MR中代入了 master 的代码，和小A已经提交的代码？why？<br><span id="more"></span></p>
<p><img src="../media/抓狂.png" alt="抓狂"></p>
<h1 id="AntCode-是如何做代码diff的"><a href="#AntCode-是如何做代码diff的" class="headerlink" title="AntCode 是如何做代码diff的"></a>AntCode 是如何做代码diff的</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git diff [&lt;options&gt;] &lt;commit&gt;...&lt;commit&gt; [--] [&lt;path&gt;...]</span><br><span class="line">	   This form is to view the changes on the branch containing and up to the second &lt;commit&gt;, starting at a common</span><br><span class="line">	   ancestor of both &lt;commit&gt;. <span class="string">&quot;git diff A...B&quot;</span> is equivalent to <span class="string">&quot;git diff <span class="subst">$(git merge-base A B)</span> B&quot;</span>. You can omit any</span><br><span class="line">	   one of &lt;commit&gt;, <span class="built_in">which</span> has the same effect as using HEAD instead.</span><br></pre></td></tr></table></figure>
<h1 id="上面的命令究竟是如何对比的？"><a href="#上面的命令究竟是如何对比的？" class="headerlink" title="上面的命令究竟是如何对比的？"></a>上面的命令究竟是如何对比的？</h1><p>既然 <code>&quot;git diff A...B&quot; is equivalent to &quot;git diff $(git merge-base A B) B&quot;</code>，那我们先来看看 <code>git merge-base A B</code>，看看这个是个啥？</p>
<p>简单来说就是 找到 A 和 B 的两个的最近的共同祖先，然后在和 B 做比较。</p>
<p>这个时候我们来看下上面那个场景的拓扑图</p>
<p><img src="../media/git 三方合并.png" alt="git 三方合并"></p>
<p>这个是 小 B 提交了合并到 <code>dev</code> 的 MR，他看到的对比应该是</p>
<p><code>dev</code> 和 <code>dev_B</code> 的共同祖先是 <code>X</code> 节点，所以展示的diff的区别就是 <code>X</code> 和 <code>devB</code> 最新commit 的区别，所以区别上会带入其他迭代的代码和<code>dev_A</code>的代码。</p>
<h1 id="如何避免这类问题"><a href="#如何避免这类问题" class="headerlink" title="如何避免这类问题"></a>如何避免这类问题</h1><ol>
<li>每次提交代码都是基于新的分支，这个新分支的建立是基于最新的 dev 分支创建的</li>
<li>不要主动去合并master然后推送了，等到预发的时候再合并master。</li>
</ol>
]]></content>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>如何构建一个正确的单例</title>
    <url>/create-right-singleton.html</url>
    <content><![CDATA[<p>单例模式是设计模式里面最常见的，也是在面试过程中面试官最容易考到的，通过单例模式还可以引申到其他的一些并发问题。今天我们来聊聊如何正确的构建一个单例。</p>
<span id="more"></span>
<p><img src="../media/dragonboat_ZH-CN0697680986_1920x1080.jpg" alt="dragonboat_ZH-CN0697680986_1920x1080"></p>
<p>首先我们来写一个最简单的单例，该单例只能使用在单线程的情况下。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UnsafeLazyInitialization</span> </span>&#123;</span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> Resource resource;</span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Resource <span class="title">getInstance</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (resource == <span class="keyword">null</span>) &#123;</span><br><span class="line">      resource = <span class="keyword">new</span> Resource(); <span class="comment">// 多线程情况下是不安全的发布</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> resource;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们来分析一下这个单例如果放到多线程模式下会有什么问题，如果线程 A 和 B 同时到达 <code>if (resource == null)</code> 的时候（或者是由于可见性的原因，线程 B 没有看到线程 A 已经对引用赋值了），此时判断逻辑都为 <code>true</code>  于是就会创建 <code>Resource</code> 对象，两个线程创建了两个对象（如果有多个线程，可能会创建更多的对象），不符合单例的要求。相对这个问题而言，这里还隐藏了更危险的问题，由于指令的重排序的原因，可能导致一些线程能够获取这个单例，但是单例对象还在初始化，内部处于一个不确定的状态。</p>
<h4 id="饿汉单例（静态类模式）"><a href="#饿汉单例（静态类模式）" class="headerlink" title="饿汉单例（静态类模式）"></a>饿汉单例（静态类模式）</h4><p>下面我们改进一下这个单例，让其变为一个线程安全的单例。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">EagerInitialization</span> </span>&#123; <span class="comment">// 饿汉模式</span></span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> Resource resource = <span class="keyword">new</span> Resource(); <span class="comment">// 通过类加载过程，提前加载单例，避免线程竞争</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Resource <span class="title">getResource</span><span class="params">()</span> </span>&#123; </span><br><span class="line">    <span class="keyword">return</span> resource;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面的代码如何做到的线程安全，是由于利用了静态初始化器。</p>
<blockquote>
<p>静态初始化器是由 JVM 再类的初始化阶段执行，即在类被加载后并且被线程使用之前。由于 JVM 将在初始化期间获得一个锁，并且每个线程都至少获取一次这个锁以确保这个累已经加载，因此在静态初始化期间，内存写入操作将自动对所有线程可见。</p>
</blockquote>
<h4 id="懒汉单例-（静态类模式）"><a href="#懒汉单例-（静态类模式）" class="headerlink" title="懒汉单例 （静态类模式）"></a>懒汉单例 （静态类模式）</h4><p>上面的模式展示了如何使用 Java 本身类加载的机制来保证单例的，使用同样的方式也可以构建一种懒汉模式的单例。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ResourceFactory</span> </span>&#123;</span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">ResourceHolder</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> Resource resource = <span class="keyword">new</span> Resource();</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Resource <span class="title">getResource</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> ResourceHolder.resource;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="双重检测锁-Double-Checked-Locking"><a href="#双重检测锁-Double-Checked-Locking" class="headerlink" title="双重检测锁 Double Checked Locking"></a>双重检测锁 Double Checked Locking</h4><p>在江湖上还传说着一种既高效又线程安全的单例写法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 不要这么做</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DoubleCheckedLocking</span> </span>&#123;</span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> Resource resource;</span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="title">getInstance</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (resource == <span class="keyword">null</span>) &#123;</span><br><span class="line">      <span class="keyword">synchronized</span>(DoubleCheckedLocking.class) &#123;</span><br><span class="line">        <span class="keyword">if</span> (resource == <span class="keyword">null</span>) &#123;</span><br><span class="line">          resource = <span class="keyword">new</span> Resource();</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> resource;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面的 DCL 真正的问题在于在没有同步的情况下读取一个共享对象。可见性问题会造成有些线程可能看到失效的值，这个值可能是一个 <code>null</code> ，更加糟糕的情况下，这个值可能是一个不确定的值，因为该对象还没有初始化完成就发布了。</p>
<p>上面的例子可以改写成以下代码，通过使用 <code>volatile</code> 来保证共享对象的可见性</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DoubleCheckedLocking</span> </span>&#123;</span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">volatile</span> Resource resource;</span><br><span class="line">  .....</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>然而，DCL 的这种使用方法已经被广泛地废弃了——促使该模式出现的驱动力（无竞争同步的执行速度很慢，以及 JVM 启动很慢）已经不复存在了，因此它不是一种高效的优化措施。延迟初始化能带来同样的优势，并且更容易理解。</p>
</blockquote>
<h4 id="枚举类模式实现枚举"><a href="#枚举类模式实现枚举" class="headerlink" title="枚举类模式实现枚举"></a>枚举类模式实现枚举</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MySingleton</span> </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">doSomething</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">enum</span> <span class="title">Singleton</span> <span class="keyword">implements</span> <span class="title">MySingleton</span> </span>&#123;</span><br><span class="line">  INSTANCE &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doSomething</span><span class="params">()</span> </span>&#123;</span><br><span class="line">      System.out.println(<span class="string">&quot;complete singleton&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;;</span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> MySingleton <span class="title">getInstance</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> Singleton.INSTANCE;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>effective java 推荐使用枚举来做单例</p>
<blockquote>
<p>使用枚举实现单例的方法虽然还没有广泛采用，但是单元素的枚举类型已经成为实现Singleton的最佳方法。</p>
</blockquote>
<p>使用枚举来做单例的优势是：写法简单，单例线程安全由 JMM 提供、反序列化得到的对象也能保证是同一个。</p>
<p>单例线程安全：如果反编译单例的代码之后，会发现是一些 static 的属性和代码块。</p>
<p>反序列化的保障：</p>
<blockquote>
<p>Java的序列化机制针对枚举类型是特殊处理的。简单来讲，在序列化枚举类型时，只会存储枚举类的引用和枚举常量的名称。随后的反序列化的过程中，这些信息被用来在运行时环境中查找存在的枚举类型对象。</p>
</blockquote>
<h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>推荐使用枚举去实现单例模式！</p>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>tech</tag>
      </tags>
  </entry>
  <entry>
    <title>幂等设计</title>
    <url>/idempotence-design.html</url>
    <content><![CDATA[<p>我们实际系统中有很多操作，是不管做多少次，都应该产生一样的效果或返回一样的结果。 例如</p>
<ol>
<li>前端重复提交选中的数据，应该后台只产生对应这个数据的一个反应结果；</li>
<li>我们发起一笔付款请求，应该只扣用户账户一次钱，当遇到网络重发或系统bug重发，也应该只扣一次钱；</li>
<li>发送消息，也应该只发一次</li>
<li>创建业务订单，一次业务请求只能创建一个，创建多个就会出大问题等等很多重要的情况都需要幂等的特性来支持。 <span id="more"></span>
</li>
</ol>
<h1 id="幂等性概念"><a href="#幂等性概念" class="headerlink" title="幂等性概念"></a>幂等性概念</h1><p>幂等（idempotent、idempotence）是一个数学与计算机学概念，常见于抽象代数中。 在编程中一个幂等操作的特点是其任意多次执行所产生的影响均与一次执行的影响相同。幂等函数，或幂等方法，是指可以使用相同参数重复执行，并能获得相同结果的函数。这些函数不会影响系统状态，也不用担心重复执行会对系统造成改变。例如，“getUsername()和setTrue()”函数就是一个幂等函数. 更复杂的操作幂等保证是利用唯一交易号(流水号)实现。幂等就是一个操作，不论执行多少次，产生的效果和返回的结果都是一样的。</p>
<h1 id="幂等方案设计"><a href="#幂等方案设计" class="headerlink" title="幂等方案设计"></a>幂等方案设计</h1><p>有些操作是天然幂等的，譬如查询操作、删除操作。其他一些插入和更新操作有时候需要引入其他的机制来保证幂等，比如乐观锁、悲观锁等。这里介绍两种常用的来保证幂等的方案：利用数据库唯一索引和通过token机制来实现幂等。</p>
<h2 id="利用数据库来实现幂等，"><a href="#利用数据库来实现幂等，" class="headerlink" title="利用数据库来实现幂等，"></a>利用数据库来实现幂等，</h2><p>利用数据库的一些特性来实现幂等设计，可以将一些复杂的加锁解锁操作转移到数据库来处理。这样可以大大减少编码的成本。<br>这里用来创建唯一索引的数据必须保证是唯一的，不可出现业务上允许的重复。<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNbRwgy1fyawz1gt0nj30e40ckwev.jpg" alt=""></p>
<h2 id="token机制方式重复提交"><a href="#token机制方式重复提交" class="headerlink" title="token机制方式重复提交"></a>token机制方式重复提交</h2><p>token机制防止页面重复提交。由于网络或者重复点击的问题，可能会有重复的请求发送到服务器。利用token方式可以防止重复提交。</p>
<ol>
<li>数据提交之前要想服务器申请token， token放到redis或者tair</li>
<li>提交后后台检验token，同时更新token状态</li>
<li>进行逻辑处理</li>
</ol>
<p>这种方式适合web平台的管理页面，在进入页面后便拿到token，点击提交之后携带改token来请求逻辑处理，如果该token不存在，拒绝处理，如果已经处理完毕，返回处理结果。</p>
<h1 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h1><ol>
<li><a href="https://blog.csdn.net/u011635492/article/details/81058153">高并发下接口幂等性解决方案</a></li>
<li><a href="http://blog.51cto.com/8132260/2134050">再谈幂等机制-java杂记-51CTO博客</a></li>
</ol>
]]></content>
      <categories>
        <category>设计</category>
      </categories>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>愿你做你自己</title>
    <url>/be-yourself.html</url>
    <content><![CDATA[<p>希望你永远都会是你自己，少年！</p>
]]></content>
      <categories>
        <category>随笔</category>
      </categories>
      <tags>
        <tag>think</tag>
      </tags>
  </entry>
  <entry>
    <title>理解VisualVM中的保留大小(Retained Size)</title>
    <url>/meaning-of-retained-size.html</url>
    <content><![CDATA[<p>是否在各种内存分析工具中看到过保留大小的概念呐，保留大小到底代表着什么？<br>保留大小代表着实例本事和引用的对象的总大小吗？</p>
<span id="more"></span>
<h2 id="Shallow-Size-and-Retained-Size"><a href="#Shallow-Size-and-Retained-Size" class="headerlink" title="Shallow Size and Retained Size"></a>Shallow Size and Retained Size</h2><p>Shallow Size and Retained Size 的含义都是指的实例对象，不是类本身。</p>
<p>下面将用 sampleClass 表示类Sample的一个实例（instance）</p>
<h3 id="Shallow-Size-含义"><a href="#Shallow-Size-含义" class="headerlink" title="Shallow Size 含义"></a>Shallow Size 含义</h3><p>Shallow Size 就是对象本身所占用的大小，不包括其引用的对象。</p>
<p>举个例子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SampleClass</span> </span>&#123;</span><br><span class="line">    <span class="comment">// age 属于 Shallow Size</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age = <span class="number">1</span>;</span><br><span class="line">    <span class="comment">// String 类型引用本身 name 属于 Shallow Size，</span></span><br><span class="line">    <span class="comment">// 被引用对象 &quot;SOME_STRING&quot; 不属于 Sample 的实例Shallow Size</span></span><br><span class="line">    <span class="keyword">private</span> String name = SOME_STRING;</span><br><span class="line">    <span class="comment">// 数组所有内容都属于 Shallow Size</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span>[] ids = &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>&#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="Retained-Size-含义"><a href="#Retained-Size-含义" class="headerlink" title="Retained Size 含义"></a>Retained Size 含义</h3><h4 id="针对可达对象"><a href="#针对可达对象" class="headerlink" title="针对可达对象"></a>针对可达对象</h4><p>可达对象就是说，从 GC roots 开始搜索，能够到达的对象，也就是说下一次GC不会被清理的对象，采用下面的计算方法来得到 Retained Size 对象</p>
<p>Retained Size的含义相对于Shallow Size 不太好理解。依然是上面的例子，在不同的情况下，sampleClass 的Retained Size 并不相同。下面引用StackOverFlow上的一个<a href="https://stackoverflow.com/questions/23941688/yourkit-the-retained-size-of-an-object-doesnt-equal-the-retained-size-of-all">回答</a>来解释这个问题。</p>
<blockquote>
<p><strong>Retained size</strong> of an object is its shallow size plus the shallow sizes of the objects that are accessible, directly or indirectly, only from this object. In other words, the retained size represents the amount of memory that will be freed by the garbage collector when this object is collected.</p>
</blockquote>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/0069RVTdgy1fu2cp92ho4j30p00fd0v3.jpg" alt="图一"></p>
<p>上图应该比较容易理解，看起来每个对象的Retained Size 都符合公式（ Retained Size = Shallow Size + 直接子对象的 Retained Size），并没有什么问题，然后我们看另外一种引用关系中，Obj1 的Retained Size 就会发生变化。</p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/0069RVTdgy1fu2cpvt71gj30p00jc76z.jpg" alt="图二"></p>
<p>第二种图中，你会发现Obj2的Retained Size 不再符合我们刚刚总结出来的公式，这是因为Obj2的直接子对象Obj5还被Obj6所引用，造成的结果就是，如果Obj2被回收，Obj5并不会被回收，所以 Obj2 的Retained Size就不应该包括 Obj5 的Retained Size. 虽然 Obj2 的 Retained Size 发生了变化，但是 Obj1 的 Retained Size 并没有发生变化。</p>
<h4 id="针对不可达对象"><a href="#针对不可达对象" class="headerlink" title="针对不可达对象"></a>针对不可达对象</h4><p>指的是下一次 GC 会被清理的对象，也就是说没有其他对象引用自己的对象。</p>
<p>如果你尝试使用 VisualVM 分析dump文件的时候，你会发现有些对象的 Retained Size 居然是0，这个0就推翻了上面那种分析方法的的结论，觉得无论什么样的对象肯定是会有 Shallow Size 的，不可能为0。</p>
<p>通过分析最后可以这样理解：针对不可达对象，也就是可以完全被清除的对象，Retained Size 都是0</p>
<h2 id="附录"><a href="#附录" class="headerlink" title="附录"></a>附录</h2><h3 id="理解误区"><a href="#理解误区" class="headerlink" title="理解误区"></a>理解误区</h3><p>好多人是简单的将保留大小描述为，<code>该对象本身和其直接引用或者间接引用的对象的大小的总和</code>。这种理解是片面的，就像上面例子中的 Obj2 一样，如果按照这种理解就会得到错误的结论。</p>
<h3 id="如何在-VisualVM-中计算保留大小（Retained-Size）"><a href="#如何在-VisualVM-中计算保留大小（Retained-Size）" class="headerlink" title="如何在 VisualVM 中计算保留大小（Retained Size）"></a>如何在 VisualVM 中计算保留大小（Retained Size）</h3><p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/0069RVTdgy1fu2d8odqlmj31kw0a1ac9.jpg" alt=""></p>
<h3 id="一些-Retained-Size-为-0-的例子"><a href="#一些-Retained-Size-为-0-的例子" class="headerlink" title="一些 Retained Size 为 0 的例子"></a>一些 Retained Size 为 0 的例子</h3><p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/0069RVTdgy1fu2d9wrsm0j30ny074dgj.jpg" alt=""></p>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>JVM</tag>
        <tag>tech</tag>
      </tags>
  </entry>
  <entry>
    <title>线上死锁分析解决纪实</title>
    <url>/record-of-deadlock-analytics.html</url>
    <content><![CDATA[<p>服务发生死锁，死锁检测时间较长，31s后死锁检测出来事务才得以回滚，期间不断有相同请求进来，造成死锁越来越复杂，并且服务端线程池中的所有线程都在等待锁，最后造成服务端线程池无空闲线程，拒绝服务。</p>
<p><strong>注意：单条 SQL 也是一个事务，也会和其他事务发生死锁。</strong></p>
<span id="more"></span>
<h1 id="原因分析"><a href="#原因分析" class="headerlink" title="原因分析"></a>原因分析</h1><p>首先奉上 InnoDB 日志 和 DDL</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> `campaignmockqueue` (</span><br><span class="line">  `id` <span class="type">int</span>(<span class="number">10</span>) unsigned <span class="keyword">NOT</span> <span class="keyword">NULL</span> AUTO_INCREMENT,</span><br><span class="line">  `campaignid` <span class="type">int</span>(<span class="number">10</span>) unsigned <span class="keyword">NOT</span> <span class="keyword">NULL</span>,</span><br><span class="line">  `addtime` <span class="type">int</span>(<span class="number">10</span>) unsigned <span class="keyword">NOT</span> <span class="keyword">NULL</span>,</span><br><span class="line">  <span class="keyword">PRIMARY</span> KEY (`id`),</span><br><span class="line">  KEY `idx_campid` (`campaignid`),</span><br><span class="line">  KEY `addtime` (`addtime`)</span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB AUTO_INCREMENT<span class="operator">=</span><span class="number">1065620134</span> <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8</span><br></pre></td></tr></table></figure>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">------------------------</span></span><br><span class="line">LATEST DETECTED DEADLOCK</span><br><span class="line"><span class="comment">------------------------</span></span><br><span class="line"><span class="number">2019</span><span class="number">-05</span><span class="number">-16</span> <span class="number">15</span>:<span class="number">38</span>:<span class="number">17</span> <span class="number">0x7f121830f700</span></span><br><span class="line"><span class="operator">*</span><span class="operator">*</span><span class="operator">*</span> (<span class="number">1</span>) TRANSACTION:</span><br><span class="line">TRANSACTION <span class="number">161027737922</span>, ACTIVE <span class="number">0</span> sec fetching <span class="keyword">rows</span></span><br><span class="line">mysql tables <span class="keyword">in</span> use <span class="number">1</span>, locked <span class="number">1</span></span><br><span class="line">LOCK WAIT <span class="number">88</span> lock struct(s), heap size <span class="number">24784</span>, <span class="number">1412</span> <span class="type">row</span> lock(s), undo log entries <span class="number">2</span></span><br><span class="line">MySQL thread id <span class="number">42610991</span>, OS thread handle <span class="number">139701841643264</span>, query id <span class="number">40446554160</span> <span class="number">10.7</span><span class="number">.23</span><span class="number">.224</span> promotio_e0a1 updating</span><br><span class="line"><span class="comment">/*id:6d7cc9a7*/</span><span class="keyword">DELETE</span> <span class="keyword">FROM</span> `campaignmockqueue` <span class="keyword">WHERE</span> `campaignid`<span class="operator">=</span><span class="number">52327710</span> <span class="keyword">and</span> `addtime` <span class="operator">&lt;=</span> <span class="number">1557992297</span></span><br><span class="line"><span class="operator">*</span><span class="operator">*</span><span class="operator">*</span> (<span class="number">1</span>) WAITING <span class="keyword">FOR</span> THIS LOCK <span class="keyword">TO</span> BE GRANTED:</span><br><span class="line">RECORD LOCKS space id <span class="number">1975</span> page <span class="keyword">no</span> <span class="number">5280</span> n bits <span class="number">1272</span> index idx_campid <span class="keyword">of</span> <span class="keyword">table</span> `promotion`.`campaignmockqueue` trx id <span class="number">161027737922</span> lock_mode X waiting</span><br><span class="line"><span class="operator">*</span><span class="operator">*</span><span class="operator">*</span> (<span class="number">2</span>) TRANSACTION:</span><br><span class="line">TRANSACTION <span class="number">161027729748</span>, ACTIVE <span class="number">31</span> sec inserting</span><br><span class="line">mysql tables <span class="keyword">in</span> use <span class="number">1</span>, locked <span class="number">1</span></span><br><span class="line"><span class="number">7</span> lock struct(s), heap size <span class="number">1136</span>, <span class="number">7</span> <span class="type">row</span> lock(s), undo log entries <span class="number">14</span></span><br><span class="line">MySQL thread id <span class="number">42610752</span>, OS thread handle <span class="number">139715692001024</span>, query id <span class="number">40446554260</span> <span class="number">10.43</span><span class="number">.174</span><span class="number">.209</span> promotio_e0a1 update</span><br><span class="line"><span class="comment">/*id:dfdc4b66*/</span><span class="keyword">INSERT</span> <span class="keyword">INTO</span> `campaignmockqueue`(</span><br><span class="line">           `campaignid`, `addtime`</span><br><span class="line">        )</span><br><span class="line">        <span class="keyword">VALUES</span></span><br><span class="line">          </span><br><span class="line">            (<span class="number">52327709</span>, <span class="number">1557992297</span>)</span><br><span class="line">         , </span><br><span class="line">            (<span class="number">52327709</span>, <span class="number">1559383140</span>)</span><br><span class="line">         , </span><br><span class="line">            (<span class="number">52327709</span>, <span class="number">1557992296</span>)</span><br><span class="line"><span class="operator">*</span><span class="operator">*</span><span class="operator">*</span> (<span class="number">2</span>) HOLDS THE LOCK(S):</span><br><span class="line">RECORD LOCKS space id <span class="number">1975</span> page <span class="keyword">no</span> <span class="number">5280</span> n bits <span class="number">1272</span> index idx_campid <span class="keyword">of</span> <span class="keyword">table</span> `promotion`.`campaignmockqueue` trx id <span class="number">161027729748</span> lock_mode X locks rec but <span class="keyword">not</span> gap</span><br><span class="line"><span class="operator">*</span><span class="operator">*</span><span class="operator">*</span> (<span class="number">2</span>) WAITING <span class="keyword">FOR</span> THIS LOCK <span class="keyword">TO</span> BE GRANTED:</span><br><span class="line">RECORD LOCKS space id <span class="number">1975</span> page <span class="keyword">no</span> <span class="number">5271</span> n bits <span class="number">1272</span> index idx_campid <span class="keyword">of</span> <span class="keyword">table</span> `promotion`.`campaignmockqueue` trx id <span class="number">161027729748</span> lock_mode X locks gap before rec <span class="keyword">insert</span> intention waiting</span><br><span class="line"><span class="operator">*</span><span class="operator">*</span><span class="operator">*</span> WE ROLL BACK TRANSACTION (<span class="number">2</span>)</span><br></pre></td></tr></table></figure>
<p>如果看不懂日志，可以参考 <a href="https://blog.csdn.net/sun_ashe/article/details/88381576">这个文章</a></p>
<p>我们先来看看 <strong>事务1</strong> 和<strong>事务2</strong> 分别持有什么锁，又在等待什么锁。由于日志是在事务 1 的角度来打印的，所以我们只能看到事务 2 持有 <code>lock_mode X locks rec but not gap</code> 锁，在等待 <code>lock_mode X locks gap before rec insert intention</code> 锁。</p>
<blockquote>
<p><code>lock_mode X locks rec but not gap</code> 就是写记录锁，只锁了单条记录。</p>
<p> <code>lock_mode X locks gap before rec insert intention</code>  就是一个插入意向锁，目标是在对应的间隙上（不包括记录本身）加锁。</p>
</blockquote>
<p>通过事务 2 的锁信息我们可以推测出事务 1 的锁持有信息，因此就有了下面的图。</p>
<p>事务 1 当前拥有的应该是间隙 A 和 记录X 组成的 nexy-key 锁 ，现在正在等待的是间隙 b 的锁。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> `campaignmockqueue` <span class="keyword">WHERE</span> `campaignid`<span class="operator">=</span><span class="number">52327710</span> <span class="keyword">and</span> `addtime` <span class="operator">&lt;=</span> <span class="number">1557992297</span></span><br></pre></td></tr></table></figure>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/20190517142816.png" alt=""></p>
<p>事务 2 当前拥有的应该是 记录 Y 的 X锁， 间隙 D 的插入意向锁， 间隙 C 的插入意向锁 ，现在正在等待的是间隙 e 的 间隙插入意向锁（也有可能是 记录 Y 的 record lock）。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> `campaignmockqueue`(`campaignid`, `addtime` )<span class="keyword">VALUES</span> (<span class="number">52327709</span>, <span class="number">1557992297</span>) , (<span class="number">52327709</span>, <span class="number">1559383140</span>), (<span class="number">52327709</span>, <span class="number">1557992296</span>)</span><br></pre></td></tr></table></figure>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/20190516203451.png" alt=""></p>
<p> 由于 next-key 锁 和 插入意向锁互斥，所以事务 1 在等待事务 2 释放 C, Y, D；事务 2 在等待事务 1 释放 A。 这样看来正好符合 InnoDB 中的 log。</p>
<h1 id="模拟流程"><a href="#模拟流程" class="headerlink" title="模拟流程"></a>模拟流程</h1><p>个人认为 由于 next-key 锁和 Gap 锁不是一种锁，因此必然存在时间差，这种时间差在并发量很大的情况下才会凸显出来。</p>
<div class="table-container">
<table>
<thead>
<tr>
<th style="text-align:left">事务1</th>
<th>事务2</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"></td>
<td>INSERT INTO <code>campaignmockqueue</code> 记录 Y</td>
</tr>
<tr>
<td style="text-align:left">DELETE FROM <code>campaignmockqueue</code> 获取了 A 的间隙锁还没获取 next-key  锁</td>
<td></td>
</tr>
<tr>
<td style="text-align:left"></td>
<td>INSERT INTO <code>campaignmockqueue</code>(<code>campaignid</code>, <code>addtime</code> )VALUES (52327709, 1557992297) , (52327709, 1559383140), (52327709, 1557992296) （阻塞）</td>
</tr>
<tr>
<td style="text-align:left">DELETE FROM <code>campaignmockqueue</code> 获取 next-key  锁 （阻塞）</td>
</tr>
</tbody>
</table>
</div>
<h1 id="验证猜想"><a href="#验证猜想" class="headerlink" title="验证猜想"></a>验证猜想</h1><p>上面的情况仅仅是推测，如果我们能拿到 INSERT 语句角度的死锁日志就好了。搜寻了一下 InnoDB 日志，得到了下面的日志，通过交叉分析，可以验证了我们的猜想。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">------------------------</span><br><span class="line">LATEST DETECTED DEADLOCK</span><br><span class="line">------------------------</span><br><span class="line">2019-05-16 03:02:36 0x7f11eebd8700</span><br><span class="line">*** (1) TRANSACTION:</span><br><span class="line">TRANSACTION 161022641521, ACTIVE 0 sec inserting</span><br><span class="line">mysql tables in use 1, locked 1</span><br><span class="line">LOCK WAIT 5 lock struct(s), heap size 1136, 3 row lock(s), undo log entries 2</span><br><span class="line">MySQL thread id 42518313, OS thread handle 139715027216128, query id 40270385508 10.7.224.35 promotio_e0a1 update</span><br><span class="line">&#x2F;*id:dfdc4b66*&#x2F;INSERT INTO &#96;campaignmockqueue&#96;(</span><br><span class="line">&#96;campaignid&#96;, &#96;addtime&#96;</span><br><span class="line">)</span><br><span class="line">VALUES</span><br><span class="line"></span><br><span class="line">(52247612, 1557946956)</span><br><span class="line">, </span><br><span class="line">(52247612, 1557936000)</span><br><span class="line">, </span><br><span class="line">(52247612, 1558022399)</span><br><span class="line">*** (1) WAITING FOR THIS LOCK TO BE GRANTED:</span><br><span class="line">RECORD LOCKS space id 1975 page no 4904 n bits 1264 index addtime of table &#96;promotion&#96;.&#96;campaignmockqueue&#96; trx id 161022641521 lock_mode X locks gap before rec insert intention waiting</span><br><span class="line">*** (2) TRANSACTION:</span><br><span class="line">TRANSACTION 161022641523, ACTIVE 0 sec fetching rows</span><br><span class="line">mysql tables in use 1, locked 1</span><br><span class="line">4 lock struct(s), heap size 1136, 3 row lock(s), undo log entries 1</span><br><span class="line">MySQL thread id 42543917, OS thread handle 139714996569856, query id 40270385509 10.7.23.224 promotio_e0a1 updating</span><br><span class="line">&#x2F;*id:6d7cc9a7*&#x2F;DELETE FROM &#96;campaignmockqueue&#96; WHERE &#96;campaignid&#96;&#x3D;52247612 and &#96;addtime&#96; &lt;&#x3D; 1557946956</span><br><span class="line">*** (2) HOLDS THE LOCK(S):</span><br><span class="line">RECORD LOCKS space id 1975 page no 4904 n bits 1264 index addtime of table &#96;promotion&#96;.&#96;campaignmockqueue&#96; trx id 161022641523 lock_mode X</span><br><span class="line">*** (2) WAITING FOR THIS LOCK TO BE GRANTED:</span><br><span class="line">RECORD LOCKS space id 1975 page no 4904 n bits 1264 index addtime of table &#96;promotion&#96;.&#96;campaignmockqueue&#96; trx id 161022641523 lock_mode X waiting</span><br><span class="line">*** WE ROLL BACK TRANSACTION (2)</span><br></pre></td></tr></table></figure>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>DELETE FROM <code>campaignmockqueue</code> 不使用 <code>idx_campid</code> 这个索引加锁，而使用唯一主键来做操作，和 insert 操作使用不同的索引，来避免这个问题。</p>
<h1 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h1><ol>
<li><a href="http://hedengcheng.com/?p=771#_Toc374698311">何登成的技术博客 » MySQL 加锁处理分析</a></li>
<li><a href="https://my.oschina.net/hebaodan/blog/1835966">mysql并发insert死锁问题——gap、插入意向锁冲突 - hebaodan的博客 - OSCHINA</a></li>
<li><a href="https://jacobchang.cn/lock-of-mysql.html#%E8%AF%BB%E5%86%99%E6%84%8F%E5%90%91%E9%94%81">MySQL 中的锁 [ 黄小豆的博客 ]</a></li>
<li><a href="https://www.aneasystone.com/archives/2018/06/insert-locks-via-mysql-source-code.html">读 MySQL 源码再看 INSERT 加锁流程 - aneasystone’s blog</a></li>
<li><a href="https://blog.csdn.net/sun_ashe/article/details/88381576">Innodb死锁日志分段解读-如何阅读死锁日志 - 奎因&amp;华洛 - CSDN博客</a></li>
<li><a href="http://www.fordba.com/lock-analyse-of-delete.html">MySQL DELETE 删除语句加锁分析 | | For DBA</a></li>
</ol>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>tech</tag>
        <tag>MySQL</tag>
      </tags>
  </entry>
  <entry>
    <title>如何解决动态规划问题</title>
    <url>/solve-dp-problems.html</url>
    <content><![CDATA[<p>曾经的我以为动态规划很神秘，很难理解。后来随着刷的动态规划相关的题越来越多，对于动态规划也就驾轻就熟了。我一开始来认识动态规划是通过概念来理解的，这对于我来说总是显得晦涩。我不是一个善于死记理论的人，反而是通过多刷题，回头再去看动态规划的使用情况则是有一种恍然大悟感。这样的获得想必也不会轻易忘记。刷题并不能让人变得聪明，但确实能够锻炼一个人的思维。</p>
<span id="more"></span>
<p><img src="../media/fe310b0e3c026f78.jpg" alt="img"></p>
<h2 id="递归-数组-动态规划"><a href="#递归-数组-动态规划" class="headerlink" title="递归 + 数组 = 动态规划"></a>递归 + 数组 = 动态规划</h2><blockquote>
<p>先看一眼概念也未必是坏事：<a href="https://zh.wikipedia.org/wiki/%E5%8A%A8%E6%80%81%E8%A7%84%E5%88%92">动态规划 - 维基百科，自由的百科全书</a></p>
</blockquote>
<p>动态规划的所有题目，在不考虑性能的情况下，都可以使用简单的递归来解决。</p>
<h3 id="题目"><a href="#题目" class="headerlink" title="题目"></a>题目</h3><p>下面来看一道 LeetCode 经典动态规划题目：<a href="https://leetcode.com/problems/climbing-stairs/">(70) Climbing Stairs - LeetCode</a></p>
<p>You are climbing a stair case. It takes <em>n</em> steps to reach to the top.</p>
<p>Each time you can either climb 1 or 2 steps. In how many distinct ways can you climb to the top?</p>
<p><strong>Note:</strong> Given <em>n</em> will be a positive integer.</p>
<p><strong>Example 1:</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Input: 2</span><br><span class="line">Output: 2</span><br><span class="line">Explanation: There are two ways to climb to the top.</span><br><span class="line">1. 1 step + 1 step</span><br><span class="line">2. 2 steps</span><br></pre></td></tr></table></figure>
<p><strong>Example 2:</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Input: 3</span><br><span class="line">Output: 3</span><br><span class="line">Explanation: There are three ways to climb to the top.</span><br><span class="line">1. 1 step + 1 step + 1 step</span><br><span class="line">2. 1 step + 2 steps</span><br><span class="line">3. 2 steps + 1 step</span><br></pre></td></tr></table></figure>
<h3 id="递归"><a href="#递归" class="headerlink" title="递归"></a>递归</h3><p>公式如下（这个也是做动态规划必须要列出的公式）</p>
<script type="math/tex; mode=display">
f(n)=\begin{cases}
n,\quad n\leq 2 \\\\
f(n-1)+f(n-2),\quad x>2
\end{cases}</script><p>我们来试试用简单的递归来解决这个问题：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Created by jacob on 2019-08-20.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">climbStairs</span><span class="params">(<span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (n &lt;= <span class="number">2</span>) &#123; <span class="comment">// 递归终结点</span></span><br><span class="line">            <span class="keyword">return</span> n;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> climbStairs(n - <span class="number">1</span>) + climbStairs(n - <span class="number">2</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果我们这时候计算的是 <code>climbStairs(5)</code>，通过下图可以看到我们一共计算 <code>f(3)</code> 2次、 <code>f(2)</code> 3次、 <code>f(1)</code> 2次。这是在台阶数 n 很小的情况下，如果 n 很大的话，重复计算的模块会变的更多。</p>
<p><img src="../media/DP.png" alt="DP"></p>
<h3 id="数组记录中间结果"><a href="#数组记录中间结果" class="headerlink" title="数组记录中间结果"></a>数组记录中间结果</h3><p>这个时候如果我们能够通过一个数组来记录中间结果就好了，当上图中的每一个节点如果已经计算过，就不再重复计算了。</p>
<p><img src="../media/DP2.png" alt="DP2"></p>
<p>代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">climbStairs</span><span class="params">(<span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] dp = <span class="keyword">new</span> <span class="keyword">int</span>[n + <span class="number">1</span>];</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.climbStairs(dp, n);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">int</span> <span class="title">climbStairs</span><span class="params">(<span class="keyword">int</span>[] dp, <span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (n &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (dp[n] &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> dp[n];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span> num = climbStairs(dp, n - <span class="number">1</span>) + climbStairs(dp, n - <span class="number">2</span>);</span><br><span class="line">        dp[n] = num;</span><br><span class="line">        <span class="keyword">return</span> num;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="去掉递归"><a href="#去掉递归" class="headerlink" title="去掉递归"></a>去掉递归</h3><p>递归会新建很多局部变量，建立很多栈帧，带来很多不必要的消耗，如果能够去除递归效率会有进一步的提升。</p>
<script type="math/tex; mode=display">
f(n)=\begin{cases}
n,\quad n\leq 2 \\\\
f(n-1)+f(n-2),\quad x>2
\end{cases}</script><p>上面我们都是从图中的根结点一步一步向下计算，是一个深度优先搜索的过程，如果我们从根节点向上算就能去掉递归了。</p>
<p><img src="../media/DP-3.png" alt="DP-3"></p>
<p>代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Created by jacob on 2019-08-20.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Solution2</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">climbStairs</span><span class="params">(<span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] dp = <span class="keyword">new</span> <span class="keyword">int</span>[n + <span class="number">1</span>];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (i &lt;= <span class="number">2</span>) &#123;</span><br><span class="line">                dp[i] = i;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                dp[i] = dp[i - <span class="number">1</span>] + dp[i - <span class="number">2</span>];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> dp[n];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="动态规划题目"><a href="#动态规划题目" class="headerlink" title="动态规划题目"></a>动态规划题目</h2><p>多刷点题，就啥都明白了。</p>
<ul>
<li><a href="https://leetcode.com/problems/fibonacci-number/">(746) Fibonacci Number - LeetCode</a></li>
<li><a href="https://leetcode.com/problems/n-th-tribonacci-number/">(1137) N-th Tribonacci Number - LeetCode</a></li>
<li><a href="https://blog.csdn.net/hrn1216/article/details/51534607">动态规划 最长公共子序列 过程图解 - hrn1216的博客 - CSDN博客</a></li>
</ul>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title>聊聊 ConcurrentModificationException</title>
    <url>/ConcurrentModificationException.html</url>
    <content><![CDATA[<p>今天有朋友突然在群里抛出一句，”java中使用foreach遍历时，为啥不让删除元素呢？设计ConcurrentModificationException的意义是什么目的呢？如果单线程操作，还需要吗？” 。今天我们就来聊一聊这件事。</p>
<span id="more"></span>
<p>如果在使用 <code>Iterator</code> 遍历一个元素的时候，如果同时使用 <code>List.remove()</code> 方法去移除元素，会报出 <code>ConcurrentModificationException</code> 异常。如何避免发生这种异常，以及如何为什么会抛出这个异常。</p>
<p>先上代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    List&lt;Integer&gt; list = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">        list.add(i);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (Integer integer : list) &#123;</span><br><span class="line">        <span class="keyword">if</span> (integer == <span class="number">7</span>) &#123;</span><br><span class="line">            list.remove(integer);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>异常日志：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Exception in thread &quot;main&quot; java.util.ConcurrentModificationException</span><br><span class="line">	at java.util.ArrayList$Itr.checkForComodification(ArrayList.java:909)</span><br><span class="line">	at java.util.ArrayList$Itr.next(ArrayList.java:859)</span><br><span class="line">	at collections.Main2.main(Main2.java:16)</span><br></pre></td></tr></table></figure>
<h3 id="看看源码"><a href="#看看源码" class="headerlink" title="看看源码"></a>看看源码</h3><p>我们看上面的代码貌似没有使用 <code>Iterator</code> 呀，其实上面的 <code>foreach</code> 循环就是其实就是使用了这个对象，甚至如果你使用这样的语句 <code>System.out.print(list)</code> 也会使用的<code>list</code>对象的迭代器。我们把上面的代码改写一下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    List&lt;Integer&gt; list = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">        list.add(i);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Iterator&lt;Integer&gt; iterator = list.iterator();</span><br><span class="line">    <span class="keyword">while</span> (iterator.hasNext()) &#123;</span><br><span class="line">        Integer next = iterator.next(); <span class="comment">// 此处会抛出异常</span></span><br><span class="line">        <span class="keyword">if</span> (next == <span class="number">7</span>) &#123;</span><br><span class="line">            list.remove(next);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>iterator.next()</code> 究竟在哪里抛出了异常，我们来一探究竟。首先 <code>iterator</code> 是 <code>ArrayList.Itr</code> 类型，next 方法源码如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> E <span class="title">next</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    checkForComodification(); <span class="comment">// 此处抛出异常</span></span><br><span class="line">    <span class="keyword">int</span> i = cursor;</span><br><span class="line">    <span class="keyword">if</span> (i &gt;= size)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> NoSuchElementException();</span><br><span class="line">    Object[] elementData = ArrayList.<span class="keyword">this</span>.elementData;</span><br><span class="line">    <span class="keyword">if</span> (i &gt;= elementData.length)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> ConcurrentModificationException();</span><br><span class="line">    cursor = i + <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">return</span> (E) elementData[lastRet = i];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title">checkForComodification</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (modCount != expectedModCount)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> ConcurrentModificationException(); <span class="comment">// 此处抛出异常</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>到这里我们明白，抛出异常的原因是因为 <code>modCount != expectedModCount</code>，但是这两个字段是什么含义呐，为什么会不相等？</p>
<p><code>modCount</code> 是<code>AbstractList</code>的一个字段，用来表示这个容器实例被修改的次数，如果容器中的元素有增加、移除、替换等操作的都会修改这个值。<code>expectedModCount</code> 是 <code>ArrayList.Itr</code> 类的一个字段。在创建迭代器的时候，会将<code>modCount</code>  赋值给<code>expectedModCount</code> </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="class"><span class="keyword">class</span> <span class="title">Itr</span> <span class="keyword">implements</span> <span class="title">Iterator</span>&lt;<span class="title">E</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> cursor;       <span class="comment">// index of next element to return</span></span><br><span class="line">    <span class="keyword">int</span> lastRet = -<span class="number">1</span>; <span class="comment">// index of last element returned; -1 if no such</span></span><br><span class="line">    <span class="keyword">int</span> expectedModCount = modCount;  <span class="comment">// 赋值</span></span><br><span class="line"></span><br><span class="line">    Itr() &#123;&#125;</span><br></pre></td></tr></table></figure>
<p>然后我们来结合我们的代码来分析，在循环中我们移除了元素值为 7 的元素 <code>list.remove(next);</code> 在该方法内部调用了 <code>fastRemove</code> 方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Private remove method that skips bounds checking and does not</span></span><br><span class="line"><span class="comment"> * return the value removed.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">fastRemove</span><span class="params">(<span class="keyword">int</span> index)</span> </span>&#123;</span><br><span class="line">    modCount++; <span class="comment">// 修改了 modCount 的值</span></span><br><span class="line">    <span class="keyword">int</span> numMoved = size - index - <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">if</span> (numMoved &gt; <span class="number">0</span>)</span><br><span class="line">        System.arraycopy(elementData, index+<span class="number">1</span>, elementData, index,</span><br><span class="line">                         numMoved);</span><br><span class="line">    elementData[--size] = <span class="keyword">null</span>; <span class="comment">// clear to let GC do its work</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>等到到达下一次 <code>Integer next = iterator.next();</code> 的时候就触发了 <code>ConcurrentModificationException</code>异常。</p>
<p>以上，对于异常的原因分析就结束了。</p>
<h3 id="如何避免这个异常"><a href="#如何避免这个异常" class="headerlink" title="如何避免这个异常"></a>如何避免这个异常</h3><p>改写上面的代码，利用 <code>iterator</code> 来移除元素即可。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    List&lt;Integer&gt; list = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">        list.add(i);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Iterator&lt;Integer&gt; iterator = list.iterator();</span><br><span class="line">    <span class="keyword">while</span> (iterator.hasNext()) &#123;</span><br><span class="line">        Integer next = iterator.next();</span><br><span class="line">        <span class="keyword">if</span> (next == <span class="number">7</span>) &#123;</span><br><span class="line">            iterator.remove(); <span class="comment">//  改写移除元素的方法</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们来看看 <code>iterator.remove();</code>发生了什么</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">remove</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (lastRet &lt; <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException();</span><br><span class="line">    checkForComodification();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        ArrayList.<span class="keyword">this</span>.remove(lastRet); <span class="comment">// 移除了元素</span></span><br><span class="line">        cursor = lastRet; <span class="comment">// 把当前游标回拨</span></span><br><span class="line">        lastRet = -<span class="number">1</span>; <span class="comment">// 把上一个返回的元素的游标重置</span></span><br><span class="line">        expectedModCount = modCount; <span class="comment">// 重新吧 modCount 的值赋给 expectedModCount</span></span><br><span class="line">    &#125; <span class="keyword">catch</span> (IndexOutOfBoundsException ex) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> ConcurrentModificationException();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="为什么"><a href="#为什么" class="headerlink" title="为什么"></a>为什么</h3><h4 id="单线程的影响"><a href="#单线程的影响" class="headerlink" title="单线程的影响"></a>单线程的影响</h4><p>看到这里你可能会问，如此大费周章的去删除一个元素究竟是为了什么？</p>
<p>通过两种移除元素方法的对比可以发现，使用<code>iterator.remove();</code>移除元素，仅仅支持移除当前迭代的元素，并且在不进入下一次迭代前<code>iterator.remove();</code>只能调用一次。而通过 <code>list.remove</code> 的方式可以移除任意的元素。通过使用迭代器移除可以获得一个容器确定的视图。下面我们假装<code>list.remove</code>不会抛出异常，来举个例子</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        List&lt;Integer&gt; list = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">            list.add(i);</span><br><span class="line">        &#125;</span><br><span class="line">        Iterator&lt;Integer&gt; iterator = list.iterator();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">7</span>; i++) &#123;</span><br><span class="line">            iterator.next();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (Integer integer : list) &#123;</span><br><span class="line">            <span class="keyword">if</span> (integer == <span class="number">1</span>) &#123;</span><br><span class="line">                list.remove(integer);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        System.out.println(iterator.next()); <span class="comment">// expected 7, but found 8</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<p>我们本来是想拿到一个完整的迭代，但是却缺少了 7 这个元素。这里仅仅是做了打印处理，如果是要利用迭代器计算这个容器所有元素的和，那么这个和必然是不符合预期的。有人会说我自己知道移除了 1 这个元素，并且所以我预期的和就是没有 7 的和。如果真的是这样的话，这样的代码维护起来将是一个噩梦，你要注意在哪里移除了某个元素，以及是否会对后面的逻辑产生影响。当代码逻辑进一步复杂的时候，这样的做法会让代码表现更加的不可预期。</p>
<h4 id="多线程的影响"><a href="#多线程的影响" class="headerlink" title="多线程的影响"></a>多线程的影响</h4><p>如果是在多线程中使用 <code>ArrayList</code>，仅针对本文中涉及的元素来看，<code>modCount</code>的可见性会有问题，每个线程看到的<code>modCount</code>的大小可能是不一样的，同时<code>modCount++</code>等改变值得操作也会没有同步性措施而变得失去原子性。</p>
<p><code>Vector</code>作为同步版本的 <code>List</code>的做法是在有关 <code>modCount</code> 操作的地方使用 <code>synchronized</code>来保证可见性和同步。</p>
<p>由于 <code>list.iterator();</code> 每次都会生成新的迭代器，所以 <code>cursor</code> 和 <code>lastRet</code>变量是线程封闭的，无需同步。</p>
]]></content>
      <tags>
        <tag>Java</tag>
        <tag>tech</tag>
      </tags>
  </entry>
  <entry>
    <title>[面经] 如何拿下阿里/蚂蚁 offer</title>
    <url>/how-get-antfin-offer.html</url>
    <content><![CDATA[<p>总结一下我的阿里面试，做一个记录吧。分享给需要的人。</p>
<span id="more"></span>
<p><img src="../media/AugustBears_ZH-CN8159736622_1920x1080.jpg" alt="AugustBears_ZH-CN8159736622_1920x1080"></p>
<h3 id="整体时间流程"><a href="#整体时间流程" class="headerlink" title="整体时间流程"></a>整体时间流程</h3><ul>
<li>8月12日 14:00 通过猎头投递简历 </li>
<li>8月13日 20:01、20:11 电话没有接到，是初面电话。 </li>
<li>8月14日 21:10  接到初面的电话面试，面试内容多为基础内容，还有做过的项目。明确告知我初面过了，会有后续的面试 </li>
<li>8月15日 20点左右，接到了 HR 的联系电话，约定二面现场面试时间 </li>
<li>8月19日 15:30 ，在钱江浙商创投大厦进行面试，面试官 P9，全程讲项目，画各种图，最后考了一点技术问题。 </li>
<li>8月22日 15:00 ，在 Z 空间三面，项目主管和 HR 我们三个人在一个小屋子里面试，主要是相互了解，不涉及技术。 </li>
<li>8月24日 19:00，口头 offer，沟通薪资和职级 </li>
<li>目前等待体检通知、进行背调</li>
</ul>
<h3 id="一面"><a href="#一面" class="headerlink" title="一面"></a>一面</h3><p>一面主要考察了基础知识，同时对于原来公司负责的服务也进行了考察。我统计了一篇基础知识的笔记，在我的公众号做分享。</p>
<h3 id="二面"><a href="#二面" class="headerlink" title="二面"></a>二面</h3><p>二面则是基本上都是项目的介绍，面试官让我讲我负责的项目。就这样一个一个的讲，讲得我口干舌燥。</p>
<p>这里我感觉要突出你项目的亮点，你用了什么方式，解决的什么问题，用到了什么技术。由于这次面试主要是项目所以，没有太多可以具体的可以分享。如果能在面试之前过一遍自己的项目，然后准备一些技术难点或是隐藏比较深的问题的解决思路。</p>
<p>还有就是需要有一定的设计能力，当你自己提出了一个问题之后会问你是如何解决的这个问题，然后如果是现在的你会如何解决，有没有更好的方案，目的就是找出你的极限。</p>
<h3 id="三面（包含HR面）"><a href="#三面（包含HR面）" class="headerlink" title="三面（包含HR面）"></a>三面（包含HR面）</h3><p>三面是主管和HR一起进行面试的，主管应该就是我以后的汇报主管。问的问题主要包含历史绩效、在原来公司的职级，为什么要换工作、希望在工作中负责什么部分、为什么没有考研、蚂蚁这边的加班是否能够接受、有么有遇到什么挫折。我看来主要是看我这个候选人的性格、为人是否符合公司的价值观吧。这个阶段的 HR 的问题基本上都是类似的，比起前面的技术面试更加有套路。</p>
<h3 id="完整版资料"><a href="#完整版资料" class="headerlink" title="完整版资料"></a>完整版资料</h3><p><img src="../media/image-20190824164526602.png" alt="image-20190824164526602"></p>
<p>完整版基础知识资料请关注我的个人公众号，回复 “<strong>蚂蚁面经</strong>“ 获取 HTML 版本。</p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/uPic/2019-08-24-扫码_搜索联合传播样式-标准色版.png" alt=""></p>
]]></content>
      <categories>
        <category>随笔</category>
      </categories>
      <tags>
        <tag>阿里</tag>
        <tag>蚂蚁金服</tag>
        <tag>面经</tag>
      </tags>
  </entry>
  <entry>
    <title>JVM-探究（二）：JVM实验和 GC 日志解读</title>
    <url>/experiment-on-jvm.html</url>
    <content><![CDATA[<p>本文用于记录在学习 JVM 过程中的实验，在完成《深入理解Java虚拟机》第二版第三章中的实验的基础上，同时保证在<code>JDK1.8</code>上实验的可重复性，并且针对不同的垃圾收集器做实验扩充。在对不能在<code>JDK8</code>上重现的实验代码做了修正，在实验结果中分析了JVM的部分特性, 介绍了GC日志的解读等。<br><span id="more"></span></p>
<h2 id="GC-日志含义"><a href="#GC-日志含义" class="headerlink" title="GC 日志含义"></a>GC 日志含义</h2><p>GC 日志理解起来十分简单，因为日志本来就是要给开发人员看的，所以设计的很直观。</p>
<p>举个例子，我们来分别说明各个部分所代表的含义：</p>
<p><code>[GC (Allocation Failure) [ParNew: 7989K-&gt;424K(9216K), 0.0037834 secs] 7989K-&gt;6568K(19456K), 0.0038147 secs] [Times: user=0.02 sys=0.00, real=0.00 secs]</code></p>
<p>将上面 GC 日志抽象为各个部分，然后我们再分别说明各个部分的含义</p>
<p><code>[a(b)[c:d-&gt;e(f), g secs] h-&gt;i(j), k secs] [Times: user:l sys=m, real=n secs]</code></p>
<p>a: GC 或者是 Full GC， 这里引用一句话</p>
<blockquote>
<p>“GC日志开头的“[GC”和“[Full GC”说明了这次垃圾收集的停顿类型，而不是用来区分新生代GC还是老年代GC的。如果有“Full”，说明这次GC是发生了Stop-The-World的”</p>
<p>摘录来自: 周志明. “深入理解Java虚拟机：JVM高级特性与最佳实践（第2版）”。 iBooks.</p>
</blockquote>
<p>b: 用来说明发生这次 GC 的原因，我们例子中发生这次GC的原因是内存分配失败</p>
<p>c: 表示发生GC的区域，这里表示是新生代发生了GC，上面那个例子是因为在新生代中内存不够给新对象分配了，然后触发了 GC  </p>
<p>d: GC 之前该区域已使用的容量</p>
<p>e: GC 之后该区域已使用的容量</p>
<p>f: 该内存区域的总容量</p>
<p>g: 表示该区域这次 GC 使用的时间</p>
<p>h: 表示 GC 前整个堆的已使用容量</p>
<p>i: 表示 GC 后整个堆的已使用容量</p>
<p>j: 表示 Java 堆的总容量</p>
<p>k: 表示 Java堆 这次 GC 使用的时间</p>
<p>l: 代表用户态消耗的 CPU 时间</p>
<p>m: 代表内核态消耗的 CPU 时间</p>
<p>n: 整个 GC 事件从开始到结束的墙钟时间（Wall Clock Time）</p>
<blockquote>
<p>“CPU时间与墙钟时间的区别是，墙钟时间包括各种非运算的等待耗时，例如等待磁盘I/O、等待线程阻塞，而CPU时间不包括这些耗时，但当系统有多CPU或者多核的话，多线程操作会叠加这些CPU时间，所以读者看到user或sys时间超过real时间是完全正常的。”</p>
<p>摘录来自: 周志明. “深入理解Java虚拟机：JVM高级特性与最佳实践（第2版）”。 iBooks.</p>
</blockquote>
<h2 id="名词解释"><a href="#名词解释" class="headerlink" title="名词解释"></a>名词解释</h2><blockquote>
<p>“<strong>新生代GC（Minor GC)</strong>：指发生在新生代的垃圾收集动作，因为Java对象大多都具备朝生夕灭的特性，所以Minor GC非常频繁，一般回收速度也比较快。</p>
<p><strong>老年代GC（Major GC/Full GC）</strong>：指发生在老年代的GC，出现了Major GC，经常会伴随至少一次的Minor GC（但非绝对的，在Parallel Scavenge收集器的收集策略里就有直接进行Major GC的策略选择过程）。Major GC的速度一般会比Minor GC慢10倍以上。”</p>
<p>摘录来自: 周志明. “深入理解Java虚拟机：JVM高级特性与最佳实践（第2版）”。 iBooks.</p>
</blockquote>
<h2 id="常用垃圾收集器参数"><a href="#常用垃圾收集器参数" class="headerlink" title="常用垃圾收集器参数"></a>常用垃圾收集器参数</h2><div class="table-container">
<table>
<thead>
<tr>
<th>参数</th>
<th style="text-align:left">描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>UseSerialGC</td>
<td style="text-align:left">虚拟机在运行在 Client 模式下的默认值，打开此开关后，使用 Serial+Serial Old 收集器组合进行内存回收</td>
</tr>
<tr>
<td>UseParNewGC</td>
<td style="text-align:left">使用 ParNew + Serial Old 收集器组合进行内存回收</td>
</tr>
<tr>
<td>UseConcMarkSweepGC</td>
<td style="text-align:left">使用 ParNew + CMS + Serial Old 的收集器组合进行内存回收，当 CMS 出现 Concurrent Mode Failure 失败后会使用 Serial Old 作为备用收集器</td>
</tr>
<tr>
<td>UseParallelOldGC</td>
<td style="text-align:left">使用 Parallel Scavenge + Parallel Old 的收集器组合</td>
</tr>
<tr>
<td>UseParallelGC</td>
<td style="text-align:left">使用 Parallel Scavenge + Serial Old （PS MarkSweep）的收集器组合</td>
</tr>
<tr>
<td>SurvivorRatio</td>
<td style="text-align:left">新生代中 Eden 和任何一个 Survivor 区域的容量比值，默认为 8</td>
</tr>
<tr>
<td>PretenureSizeThreshold</td>
<td style="text-align:left">直接晋升到老年代对象的大小，单位是Byte</td>
</tr>
<tr>
<td>MaxTenuringThreshold</td>
<td style="text-align:left">晋升到老年代的对象最大年龄</td>
</tr>
<tr>
<td>UseAdaptiveSizePolicy</td>
<td style="text-align:left">动态调整 Java 堆中各区域的大小以及进入老年代的年龄</td>
</tr>
<tr>
<td>HandlePromotionFailure</td>
<td style="text-align:left">是否允许分配担保失败</td>
</tr>
<tr>
<td>ParallelGCThreads</td>
<td style="text-align:left">设置并行 GC 时进行内存回收的线程数</td>
</tr>
<tr>
<td>GCTimeRatio</td>
<td style="text-align:left">GC 时间站占时间的比率，默认值为99，只在 Parallel Scavenge 收集器的时候生效</td>
</tr>
<tr>
<td>MaxGCPauseMillis</td>
<td style="text-align:left">设置 GC 最大的停顿时间，只在 Parallel Scavenge 收集器的时候生效</td>
</tr>
<tr>
<td>CMSInitiatingOccupancyFraction</td>
<td style="text-align:left">设置 CMS 收集器在老年代空间被使用多少后触发垃圾收集，默认是 68%，仅在 CMS 收集器上生效</td>
</tr>
<tr>
<td>CMSFullGCsBeforeCompaction</td>
<td style="text-align:left">设置 CMS 收集器在进行多少次垃圾回收之后启动一次内存碎片整理</td>
</tr>
</tbody>
</table>
</div>
<blockquote>
<p>注：上表摘录来自: 周志明. “深入理解Java虚拟机：JVM高级特性与最佳实践（第2版）”，稍有调整.</p>
</blockquote>
<h2 id="实验代码"><a href="#实验代码" class="headerlink" title="实验代码"></a>实验代码</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.coderbean.jvm;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Created by jacobchang on 2017/11/20.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestAllocation</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> _1MB = <span class="number">1024</span>*<span class="number">1024</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * VM参数：（参数序号对应实验序号）</span></span><br><span class="line"><span class="comment">     *  1. -verbose:gc -Xms20M -Xmx20M -Xmn10M -XX:+UseParNewGC -XX:+PrintGCDetails -XX:SurvivorRatio=8</span></span><br><span class="line"><span class="comment">     *  2. -verbose:gc -Xms20M -Xmx20M -Xmn10M -XX:+UseParallelGC -XX:+PrintGCDetails -XX:SurvivorRatio=8</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">testAllocation</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">byte</span>[] allocation1, allocation2, allocation3, allocation4;</span><br><span class="line">        allocation1 = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">2</span>*_1MB];</span><br><span class="line">        allocation2 = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">2</span>*_1MB];</span><br><span class="line">        allocation3 = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">2</span>*_1MB];</span><br><span class="line">        allocation4 = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">4</span>*_1MB]; <span class="comment">//出现一次 Minor GC</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * VM参数：（参数序号对应实验序号）</span></span><br><span class="line"><span class="comment">     *  3. -verbose:gc -Xms20M -Xmx20M -Xmn10M -XX:+UseParNewGC -XX:+PrintGCDetails -XX:PretenureSizeThreshold=3145728</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">testPreteureSizeThreshold</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">byte</span>[] allocation;</span><br><span class="line">        allocation = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">4</span> * _1MB];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * VM参数：（参数序号对应实验序号）</span></span><br><span class="line"><span class="comment">     *  4. -verbose:gc -Xms20M -Xmx20M -Xmn10M -XX:+UseSerialGC -XX:+PrintGCDetails -XX:MaxTenuringThreshold=1 -XX:+PrintTenuringDistribution</span></span><br><span class="line"><span class="comment">     *  5. -verbose:gc -Xms20M -Xmx20M -Xmn10M -XX:+UseSerialGC -XX:+PrintGCDetails -XX:MaxTenuringThreshold=4 -XX:+PrintTenuringDistribution</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@SuppressWarnings(&quot;unused&quot;)</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">testTenuringThredhold</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">byte</span>[] allocation1, allocation2, allocation3, allocation4;</span><br><span class="line">        allocation1 = <span class="keyword">new</span> <span class="keyword">byte</span>[_1MB / <span class="number">16</span>];</span><br><span class="line">        allocation3 = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">4</span> * _1MB];</span><br><span class="line">        allocation4 = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">4</span> * _1MB];</span><br><span class="line">        allocation4 = <span class="keyword">null</span>;</span><br><span class="line">        allocation4 = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">4</span> * _1MB];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * VM参数：（参数序号对应实验序号）</span></span><br><span class="line"><span class="comment">     *  6. -verbose:gc -Xms20M -Xmx20M -Xmn10M -XX:+UseSerialGC -XX:+PrintGCDetails -XX:MaxTenuringThreshold=4 -XX:+PrintTenuringDistribution</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     *  如果在Survivor空间中相同年龄所有对象大小的总和&gt;Survivor空间的一半（ -XX:TargetSurvivorRatio）时，年龄&gt;=该年龄的对象就可以直接进入年老代</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@SuppressWarnings(&quot;unused&quot;)</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">testTenuringThredhold2</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">byte</span>[] allocation1, allocation2, allocation3, allocation4;</span><br><span class="line">        allocation1 = <span class="keyword">new</span> <span class="keyword">byte</span>[_1MB / <span class="number">4</span>];</span><br><span class="line">        allocation3 = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">4</span> * _1MB];</span><br><span class="line">        allocation4 = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">4</span> * _1MB];</span><br><span class="line">        allocation4 = <span class="keyword">null</span>;</span><br><span class="line">        allocation4 = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">4</span> * _1MB];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h2 id="实验"><a href="#实验" class="headerlink" title="实验"></a>实验</h2><h3 id="实验一：使用ParNew-Serial-Old的组合进行内存回收"><a href="#实验一：使用ParNew-Serial-Old的组合进行内存回收" class="headerlink" title="实验一：使用ParNew+Serial Old的组合进行内存回收"></a>实验一：使用<code>ParNew</code>+<code>Serial Old</code>的组合进行内存回收</h3><p><strong>对象优先在 Eden 区分配</strong></p>
<h4 id="实验结果："><a href="#实验结果：" class="headerlink" title="实验结果："></a>实验结果：</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[GC (Allocation Failure) [ParNew: 7989K-&gt;424K(9216K), 0.0037834 secs] 7989K-&gt;6568K(19456K), 0.0038147 secs] [Times: user&#x3D;0.02 sys&#x3D;0.00, real&#x3D;0.00 secs]</span><br><span class="line">Heap</span><br><span class="line"> par new generation   total 9216K, used 4685K [0x00000007bec00000, 0x00000007bf600000, 0x00000007bf600000)</span><br><span class="line">  eden space 8192K,  52% used [0x00000007bec00000, 0x00000007bf0290f0, 0x00000007bf400000)</span><br><span class="line">  from space 1024K,  41% used [0x00000007bf500000, 0x00000007bf56a3a8, 0x00000007bf600000)</span><br><span class="line">  to   space 1024K,   0% used [0x00000007bf400000, 0x00000007bf400000, 0x00000007bf500000)</span><br><span class="line"> tenured generation   total 10240K, used 6144K [0x00000007bf600000, 0x00000007c0000000, 0x00000007c0000000)</span><br><span class="line">   the space 10240K,  60% used [0x00000007bf600000, 0x00000007bfc00030, 0x00000007bfc00200, 0x00000007c0000000)</span><br><span class="line"> Metaspace       used 3242K, capacity 4496K, committed 4864K, reserved 1056768K</span><br><span class="line">  class space    used 359K, capacity 388K, committed 512K, reserved 1048576K</span><br><span class="line"></span><br><span class="line">Process finished with exit code 0</span><br></pre></td></tr></table></figure>
<h4 id="实验分析："><a href="#实验分析：" class="headerlink" title="实验分析："></a>实验分析：</h4><p> 通过上面的GC日志我们可以看出一开始出现了 MinorGC, 引起GC的原因是 <code>内存分配失败</code>，因为分配allocation4的时候，Eden区已经没有足够的区域来分配了，所以发生了本次 MinorGC ，经过 MinorGC 之后新生代的已使用容量从7989K减少到了424K，然而整个堆的内存总量却几乎没有减少，原因就是，由于发现新生代没有可以回收的对象，所以不得不使用内存担保将allocation1～3 三个对象提前转移到老年代。此时再在 Eden 区域为 allocation4 分配 4MB 的空间，因此最后我们发现 Eden 区域占用了 4MB，老年代占用了 6MB</p>
<h3 id="实验二：使用Parallel-Scavenge-Serial-Old的组合进行内存回收"><a href="#实验二：使用Parallel-Scavenge-Serial-Old的组合进行内存回收" class="headerlink" title="实验二：使用Parallel Scavenge+Serial Old的组合进行内存回收"></a>实验二：使用<code>Parallel Scavenge</code>+<code>Serial Old</code>的组合进行内存回收</h3><p><strong>对象优先在 Eden 区分配</strong></p>
<h4 id="实验结果"><a href="#实验结果" class="headerlink" title="实验结果"></a>实验结果</h4> <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Heap</span><br><span class="line">PSYoungGen      total 9216K, used 8192K [0x00000007bf600000, 0x00000007c0000000, 0x00000007c0000000)</span><br><span class="line"> eden space 8192K, 100% used [0x00000007bf600000,0x00000007bfe00000,0x00000007bfe00000)</span><br><span class="line"> from space 1024K, 0% used [0x00000007bff00000,0x00000007bff00000,0x00000007c0000000)</span><br><span class="line"> to   space 1024K, 0% used [0x00000007bfe00000,0x00000007bfe00000,0x00000007bff00000)</span><br><span class="line">ParOldGen       total 10240K, used 4096K [0x00000007bec00000, 0x00000007bf600000, 0x00000007bf600000)</span><br><span class="line"> object space 10240K, 40% used [0x00000007bec00000,0x00000007bf000010,0x00000007bf600000)</span><br><span class="line">Metaspace       used 3311K, capacity 4496K, committed 4864K, reserved 1056768K</span><br><span class="line"> class space    used 369K, capacity 388K, committed 512K, reserved 1048576K</span><br></pre></td></tr></table></figure>
<h4 id="实验分析"><a href="#实验分析" class="headerlink" title="实验分析"></a>实验分析</h4><p>  第一眼看到日志惊奇的发现居然没有发生任何类型的 GC， 起初看到日志的时候我也很惊奇，于是乎请出了Java内存分析工具<code>VisualVM</code>, JDK中会自带<code>JVisualVM</code> 效果类似，可以通过命令<code>jvisualvm</code>启动，具体分析方法可以参考我的另外一篇博客<a href="http://jacobchang.cn">VisualVM使用指南</a>。</p>
<blockquote>
<p>注： 如果想要重现下面的分析结果，除了JVM参数要和我一直外，在程序执行过程中务必设置几个断点，好让VisualVM能够出现这种阶梯图像，不然由于执行太快，看起来就像是一次性分配了内存一样。</p>
</blockquote>
<p>  <img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1fltm4roaulj31am11g78a.jpg" alt="堆内存占用情况分析"></p>
<p>  通过上图中我标出的五条线，可以看到在1位置的时候第一次在Eden中分配了 2MB 内存，然后在2，3，4 时刻分别又再次分配了 2MB 内存，此时 Eden 区域已经占用了 8MB 了，在 5 时刻当要分配 4MB 内存的时候，Eden 区域已经不够用了，直接分配到了<code>老年代</code>。这是因为<code>Parallel Scavenge</code>收集器为了保证吞吐量而直接将Eden区中无法存放的大对象直接放到了老年代。</p>
<blockquote>
<p>注： 虚拟机参数 <code>-XX：PretenureSizeThreshold</code> 并不是实验二中的大对象分配到老年代的直接控制参数，该参数只针对 <code>Serial</code> 和 <code>ParNew</code> 两个新生代收集器有作用。 该参数的作用在下一节大对象直接进入老年代的实验中讲解。</p>
</blockquote>
<h4 id="使用其他收集器组合探究对象的分配情况"><a href="#使用其他收集器组合探究对象的分配情况" class="headerlink" title="使用其他收集器组合探究对象的分配情况"></a>使用其他收集器组合探究对象的分配情况</h4><p> 由于<del>篇幅有限</del>，其实是我想偷懒了（＾∇＾），各位可以参考上面的垃圾收集器参数章节，自己去验证。</p>
<h3 id="实验三：使用参数-XX-PretenureSizeThreshold来设置直接进入老年代的对象的大小"><a href="#实验三：使用参数-XX-PretenureSizeThreshold来设置直接进入老年代的对象的大小" class="headerlink" title="实验三：使用参数-XX:PretenureSizeThreshold来设置直接进入老年代的对象的大小"></a>实验三：使用参数<code>-XX:PretenureSizeThreshold</code>来设置直接进入老年代的对象的大小</h3><p><strong>大对象直接进入老年代</strong><br>虚拟机提供一个参数 <code>-XX:PretenureSizeThreshold</code> 用来设置直接在老年代分配的对象的大小，如果对象大于这个值就会直接在老年代分配。这样做的目的是避免在<code>Eden</code>区及两个<code>Survivor</code>区之间发生大量的内存复制。</p>
<blockquote>
<p>“比遇到一个大对象更加坏的消息就是遇到一群“朝生夕灭”的“短命大对象”，写程序的时候应当避免”</p>
<p>摘录来自: 周志明. “深入理解Java虚拟机：JVM高级特性与最佳实践（第2版）”。 iBooks.</p>
</blockquote>
<h4 id="实验结果：-1"><a href="#实验结果：-1" class="headerlink" title="实验结果："></a>实验结果：</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Heap</span><br><span class="line"> par new generation   total 9216K, used 2936K [0x00000007bec00000, 0x00000007bf600000, 0x00000007bf600000)</span><br><span class="line">  eden space 8192K,  35% used [0x00000007bec00000, 0x00000007beede130, 0x00000007bf400000)</span><br><span class="line">  from space 1024K,   0% used [0x00000007bf400000, 0x00000007bf400000, 0x00000007bf500000)</span><br><span class="line">  to   space 1024K,   0% used [0x00000007bf500000, 0x00000007bf500000, 0x00000007bf600000)</span><br><span class="line"> tenured generation   total 10240K, used 4096K [0x00000007bf600000, 0x00000007c0000000, 0x00000007c0000000)</span><br><span class="line">   the space 10240K,  40% used [0x00000007bf600000, 0x00000007bfa00010, 0x00000007bfa00200, 0x00000007c0000000)</span><br><span class="line"> Metaspace       used 3278K, capacity 4496K, committed 4864K, reserved 1056768K</span><br><span class="line">  class space    used 364K, capacity 388K, committed 512K, reserved 1048576K</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h4 id="实验分析-1"><a href="#实验分析-1" class="headerlink" title="实验分析"></a>实验分析</h4><p> 通过上面的堆的内存占用情况很容易看出我们分配的4MB大小的对象直接被放到了老年代</p>
<h3 id="实验四：-使用参数-XX-MaxTenuringThreshold-1来设置进入老年代的对象年龄"><a href="#实验四：-使用参数-XX-MaxTenuringThreshold-1来设置进入老年代的对象年龄" class="headerlink" title="实验四： 使用参数-XX:MaxTenuringThreshold=1来设置进入老年代的对象年龄"></a>实验四： 使用参数<code>-XX:MaxTenuringThreshold=1</code>来设置进入老年代的对象年龄</h3><p> <strong>长期存活的对象将进入老年代</strong></p>
<h4 id="实验结果：-2"><a href="#实验结果：-2" class="headerlink" title="实验结果："></a>实验结果：</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[GC (Allocation Failure) [DefNew</span><br><span class="line">Desired survivor size 524288 bytes, new threshold 1 (max 1)</span><br><span class="line">- age   1:     749112 bytes,     749112 total</span><br><span class="line">: 6868K-&gt;731K(9216K), 0.0039484 secs] 6868K-&gt;4827K(19456K), 0.0039697 secs] [Times: user&#x3D;0.00 sys&#x3D;0.00, real&#x3D;0.00 secs]</span><br><span class="line">[GC (Allocation Failure) [DefNew</span><br><span class="line">Desired survivor size 524288 bytes, new threshold 1 (max 1)</span><br><span class="line">- age   1:       1496 bytes,       1496 total</span><br><span class="line">: 4911K-&gt;1K(9216K), 0.0012023 secs] 9007K-&gt;4706K(19456K), 0.0012150 secs] [Times: user&#x3D;0.00 sys&#x3D;0.00, real&#x3D;0.00 secs]</span><br><span class="line">Heap</span><br><span class="line"> def new generation   total 9216K, used 4316K [0x00000007bec00000, 0x00000007bf600000, 0x00000007bf600000)</span><br><span class="line">  eden space 8192K,  52% used [0x00000007bec00000, 0x00000007bf036cc8, 0x00000007bf400000)</span><br><span class="line">  from space 1024K,   0% used [0x00000007bf400000, 0x00000007bf4005d8, 0x00000007bf500000)</span><br><span class="line">  to   space 1024K,   0% used [0x00000007bf500000, 0x00000007bf500000, 0x00000007bf600000)</span><br><span class="line"> tenured generation   total 10240K, used 4704K [0x00000007bf600000, 0x00000007c0000000, 0x00000007c0000000)</span><br><span class="line">   the space 10240K,  45% used [0x00000007bf600000, 0x00000007bfa98328, 0x00000007bfa98400, 0x00000007c0000000)</span><br><span class="line"> Metaspace       used 3288K, capacity 4496K, committed 4864K, reserved 1056768K</span><br><span class="line">  class space    used 364K, capacity 388K, committed 512K, reserved 1048576K</span><br></pre></td></tr></table></figure>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1flzag9e7m8g30go04v1l7.gif" alt="实验4"></p>
<p>  通过上面的堆内存分配和visualGC的分析图片可以看出，当执行到最后一句话的时候，也就是进行第二次GC的时候，原本在<code>S0区域</code>的一些对象被移动到了老年代，这是因为当发生<code>MinorGC</code>的时候发现该对象的年龄已经到达我们设置的<code>MaxTenuringThreshold</code>值。</p>
<h3 id="实验五：-使用参数-XX-MaxTenuringThreshold-4来设置进入老年代的对象年龄"><a href="#实验五：-使用参数-XX-MaxTenuringThreshold-4来设置进入老年代的对象年龄" class="headerlink" title="实验五： 使用参数-XX:MaxTenuringThreshold=4来设置进入老年代的对象年龄"></a>实验五： 使用参数<code>-XX:MaxTenuringThreshold=4</code>来设置进入老年代的对象年龄</h3><h4 id="实验结果-1"><a href="#实验结果-1" class="headerlink" title="实验结果"></a>实验结果</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[GC (Allocation Failure) [DefNew</span><br><span class="line">Desired survivor size 524288 bytes, new threshold 1 (max 4)</span><br><span class="line">- age   1:     749320 bytes,     749320 total</span><br><span class="line">: 6868K-&gt;731K(9216K), 0.0040400 secs] 6868K-&gt;4827K(19456K), 0.0040616 secs] [Times: user&#x3D;0.00 sys&#x3D;0.01, real&#x3D;0.01 secs]</span><br><span class="line">[GC (Allocation Failure) [DefNew</span><br><span class="line">Desired survivor size 524288 bytes, new threshold 4 (max 4)</span><br><span class="line">- age   1:        808 bytes,        808 total</span><br><span class="line">: 4911K-&gt;0K(9216K), 0.0012952 secs] 9007K-&gt;4706K(19456K), 0.0013076 secs] [Times: user&#x3D;0.00 sys&#x3D;0.00, real&#x3D;0.00 secs]</span><br><span class="line">Heap</span><br><span class="line"> def new generation   total 9216K, used 4315K [0x00000007bec00000, 0x00000007bf600000, 0x00000007bf600000)</span><br><span class="line">  eden space 8192K,  52% used [0x00000007bec00000, 0x00000007bf036ca8, 0x00000007bf400000)</span><br><span class="line">  from space 1024K,   0% used [0x00000007bf400000, 0x00000007bf400328, 0x00000007bf500000)</span><br><span class="line">  to   space 1024K,   0% used [0x00000007bf500000, 0x00000007bf500000, 0x00000007bf600000)</span><br><span class="line"> tenured generation   total 10240K, used 4706K [0x00000007bf600000, 0x00000007c0000000, 0x00000007c0000000)</span><br><span class="line">   the space 10240K,  45% used [0x00000007bf600000, 0x00000007bfa988a8, 0x00000007bfa98a00, 0x00000007c0000000)</span><br><span class="line"> Metaspace       used 3282K, capacity 4496K, committed 4864K, reserved 1056768K</span><br><span class="line">  class space    used 364K, capacity 388K, committed 512K, reserved 1048576K</span><br></pre></td></tr></table></figure>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1flzag1vzrjg30gn03yb2l.gif" alt="实验5"></p>
<h4 id="实验分析-2"><a href="#实验分析-2" class="headerlink" title="实验分析"></a>实验分析</h4><p>通过上面的日志和图片，可以看出与实验四不同的是，这里执行到最后的时候<code>S0区域</code>中的对象并没有进入到老年代而是进入了<code>S1区域</code>,因为我们把进入老年代的年龄设置为了4.</p>
<blockquote>
<p>注：如果没有按照我的代码写的话，你可能会发现即使把<code>MaxTenuringThreshold</code>设置为了大于1的值，但还是有可能会在一次 GC 之后就进入了老年代，这是因为进入我们设置的只是一个最大值，真正的值可能比这个值更小，是动态变化的，这种情况会在下面的实验中介绍。</p>
</blockquote>
<h3 id="实验六：TenuringThreshold-是动态变化的"><a href="#实验六：TenuringThreshold-是动态变化的" class="headerlink" title="实验六：TenuringThreshold 是动态变化的"></a>实验六：TenuringThreshold 是动态变化的</h3><h4 id="实验结果-2"><a href="#实验结果-2" class="headerlink" title="实验结果"></a>实验结果</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[GC (Allocation Failure) [DefNew</span><br><span class="line">Desired survivor size 524288 bytes, new threshold 1 (max 4)</span><br><span class="line">- age   1:     948816 bytes,     948816 total</span><br><span class="line">: 7123K-&gt;926K(9216K), 0.0065933 secs] 7123K-&gt;5022K(19456K), 0.0066172 secs] [Times: user&#x3D;0.00 sys&#x3D;0.01, real&#x3D;0.01 secs]</span><br><span class="line">[GC (Allocation Failure) [DefNew</span><br><span class="line">Desired survivor size 524288 bytes, new threshold 4 (max 4)</span><br><span class="line">- age   1:        464 bytes,        464 total</span><br><span class="line">: 5270K-&gt;0K(9216K), 0.0013556 secs] 9366K-&gt;4900K(19456K), 0.0013708 secs] [Times: user&#x3D;0.00 sys&#x3D;0.00, real&#x3D;0.00 secs]</span><br><span class="line">Heap</span><br><span class="line"> def new generation   total 9216K, used 4233K [0x00000007bec00000, 0x00000007bf600000, 0x00000007bf600000)</span><br><span class="line">  eden space 8192K,  51% used [0x00000007bec00000, 0x00000007bf022570, 0x00000007bf400000)</span><br><span class="line">  from space 1024K,   0% used [0x00000007bf400000, 0x00000007bf4001d0, 0x00000007bf500000)</span><br><span class="line">  to   space 1024K,   0% used [0x00000007bf500000, 0x00000007bf500000, 0x00000007bf600000)</span><br><span class="line"> tenured generation   total 10240K, used 4899K [0x00000007bf600000, 0x00000007c0000000, 0x00000007c0000000)</span><br><span class="line">   the space 10240K,  47% used [0x00000007bf600000, 0x00000007bfac8f38, 0x00000007bfac9000, 0x00000007c0000000)</span><br><span class="line"> Metaspace       used 3315K, capacity 4500K, committed 4864K, reserved 1056768K</span><br><span class="line">  class space    used 367K, capacity 388K, committed 512K, reserved 1048576K</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcly1fm0dsme76og30bn060hdz.gif" alt="实验结果"></p>
<h4 id="实验分析-3"><a href="#实验分析-3" class="headerlink" title="实验分析"></a>实验分析</h4><p> 看到上面的堆信息可以看到最后<code>from</code>和<code>to</code>两个<code>Survivor</code>区域占用情况都为0，原因就是在<code>allocation1</code>被放到<code>S1</code>区域之后，由于该对象和一些附加信息的和占用了<code>S1区域</code>一半以上的空间，因为此时<code>allocation1</code>对象的年龄为1，所以此时<code>TenuringThreshold</code>的值也就变成了1，在GC的时候进入了老年代。通过上图中VisualGC左下角的<code>TenuringThreshold</code>值一直在变化也能得到验证。</p>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>JVM</tag>
        <tag>tech</tag>
      </tags>
  </entry>
  <entry>
    <title>JVM 探究（三）：垃圾回收算法和垃圾回收器</title>
    <url>/gc-algorithms-and-garbage-collector.html</url>
    <content><![CDATA[<p>通过介绍当前的垃圾回收器和垃圾回收算法的对比和不同的优势，来帮助读者选择适合自己的垃圾回收器。主要涉及对象存活的判断、三种垃圾回收算法以及新生代和老年代的几种传统的收集器，最后会介绍一下比较火热的<code>Garbage First(G1)收集器</code>。<br><span id="more"></span></p>
<h2 id="JVM-内存分配方式"><a href="#JVM-内存分配方式" class="headerlink" title="JVM 内存分配方式"></a>JVM 内存分配方式</h2><p>在 Java 虚拟机需要一种方式来分配对象，就像操作系统需要管理内存一样，Java 虚拟机也需要一种方式来管理 Java 虚拟机的内存，现在管理 Java 虚拟机的方式主要有两种，一种被称为<code>指针碰撞(Bump the Point)</code>，另外一种被称为<code>空闲列表(Free List)</code>.</p>
<h3 id="指针碰撞"><a href="#指针碰撞" class="headerlink" title="指针碰撞"></a>指针碰撞</h3><p>这种内存分配方式需要配合拥有内存整理过程的垃圾收集器。简单来说，这种方式的内存管理方式就是在空闲内存和已用内存之间设置一个指针，如果有新的内存被分配，就需要将指针向空闲区域移动对应的大小。然而由于这种方式简单粗暴，就必须要对应的垃圾收集器能够将存活的对象占用的内存整理在指针的一侧。使用这种分配算法的垃圾收集器是<code>Serial</code>,<code>ParNew</code>等带有内存整理的垃圾收集器。</p>
<h3 id="空闲列表"><a href="#空闲列表" class="headerlink" title="空闲列表"></a>空闲列表</h3><p>通过在虚拟机中维护一个列表来记录那些内存空间是能够使用的。如果需要分配新的对象，将会在空闲的列表中寻找一个足够大的空闲内存来分配对象，然后更新这个空闲列表。采用<code>标记-清除</code>算法的垃圾收集器（eg：CMS）会采用这种分配方式。但是这种分配方式很容易造成很多外部碎片，在一定程度上会造成内存的浪费。</p>
<h2 id="如何定位需要访问的对象"><a href="#如何定位需要访问的对象" class="headerlink" title="如何定位需要访问的对象"></a>如何定位需要访问的对象</h2><p>如何通过变量表引用到需要用到的对象，通过引用我们要找到具体的对象，然后才能够对对象做一些列的操作。引用方式主要有一下两种方式</p>
<h3 id="句柄方式"><a href="#句柄方式" class="headerlink" title="句柄方式"></a>句柄方式</h3><p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcly1fndxgjtgb8j31kw0phkjl.jpg" alt="句柄方式访问对象"><br>句柄方式通过在 Java 堆中维护一个句柄池，池中的维护了对象实例和对象类型的引用，然后在根据这两个信息分别找到对应的存储信息，但是这样做的一个确定是实例对象的类型和数据都是通过两次寻址的方式来找到的。</p>
<h3 id="直接引用"><a href="#直接引用" class="headerlink" title="直接引用"></a>直接引用</h3><p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcly1fndxohdm1rj31kw0qqb29.jpg" alt="直接引用方式访问对象"><br>直接引用的方式相较于句柄的方式，在第一次寻址的时候就能够找到对象的实例数据，对象类型数据的指针和对象实例放到了一起，从而一次寻址就能够找到对象的实例数据。</p>
<h2 id="垃圾回收算法"><a href="#垃圾回收算法" class="headerlink" title="垃圾回收算法"></a>垃圾回收算法</h2><p>介绍常用的垃圾回收算法，针对<code>HotSpot</code>虚拟机做一些深入的介绍。</p>
<h3 id="Mark-Sweep算法"><a href="#Mark-Sweep算法" class="headerlink" title="Mark-Sweep算法"></a>Mark-Sweep算法</h3><p><code>标记-清除（Mark-Sweep）</code>算法回收前后内存占用情况如下：</p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcly1fndzgovb5ij311s0hsjs7.jpg" alt="标记-清除算法图示"><br><code>标记-清除（Mark-Sweep）</code>算法是通过可以回收的内存进行标记后做清除操作，这样做的缺点就是会产生很多的内存碎片，可能的结果就是空闲内存总量是能够进行新对象的分配的，但是由于这些空闲的空间都不连续，一个对象放不下。就必须进行另外一次垃圾回收，垃圾回收又会引起服务的暂时停顿。</p>
<h3 id="Mark-Compact-算法"><a href="#Mark-Compact-算法" class="headerlink" title="Mark-Compact 算法"></a>Mark-Compact 算法</h3><p><code>标记-整理(Mark-Compact)</code>算法回收前后内存占用情况如下：<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1fnnglisy2mj310u0ik754.jpg" alt="标记-整理算法图示"></p>
<p><code>标记-整理(Mark-Compact)</code>算法解决了<code>标记-清除（Mark-Sweep）</code>算法会产生很多不连续的空闲空间做出了改进，具体的方法就是在进行标记之后不是将对象直接清理掉，而是将存活的对象进行整理，使得存活的对象占用一块连续的内存空间，因为存活对象和可回收空间有明显的分割，所以可以直接对边界之外的内存进行直接的清理。这样的清理无疑更加有效率，虽然增加了移动对象的开支，但是整体上会比<code>标记-清除（Mark-Sweep）</code>算法更有效率。</p>
<h3 id="Copying-算法"><a href="#Copying-算法" class="headerlink" title="Copying 算法"></a>Copying 算法</h3><p><code>复制(Copying)</code>算法回收前后内存占用情况如下：</p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1fnngl3n0qxj31120kimy3.jpg" alt="复制算法图示"></p>
<p><code>复制(Copying)</code>算法将内存区域分为大小相同的两个区域，当需要内存回收的时候，只需要将存活的对象复制到另外一块空闲区域，然后将原来的区域一次清理干净，什么都不留。相较于<code>标记-清除（Mark-Sweep）</code>算法的优势就是在解决了在标记和清除阶段的低效率和内存碎片问题，相较于<code>标记-整理(Mark-Compact)</code>算法的优势则是有着固定的内存分割线，而不是动态的调整，同时由于有两块相同大小的内存区域，存活的对象的复制和内存的回收会更有效率。缺点也十分明显，就是需要的内存空间是可用空间的2倍，内存的使用率永远不会超过50%。（现代虚拟机根据经验优化了这个比例，在后面介绍收集器的时候会介绍）</p>
<h2 id="聊聊HotSpot"><a href="#聊聊HotSpot" class="headerlink" title="聊聊HotSpot"></a>聊聊HotSpot</h2><p><code>HotSpot</code> 是现在最广泛使用的虚拟机，拥有出色的性能。</p>
<h3 id="判断对象是否存活"><a href="#判断对象是否存活" class="headerlink" title="判断对象是否存活"></a>判断对象是否存活</h3><p>目前针对对象的判断生存状态的的算法主要有引用计数法和可达性分析两种。在<code>Object-C</code>等语言中使用的是引用计数法，在<code>HotSpot</code>中，是使用可达性分析来判断的。</p>
<h4 id="引用计数法-Reference-Counting"><a href="#引用计数法-Reference-Counting" class="headerlink" title="引用计数法(Reference Counting)"></a>引用计数法(Reference Counting)</h4><p>引用计数法是想要解决引用问题最容易想到的一种算法，既然需要知道对象是否存活，那就在对象上加个计数器来表示自己被引用的次数呗，每有一次新的引用，这个计数器就加1，每少一次引用，计数器就减1。这样看来，事情似乎出奇的简单，因为当该对象的计数器为0的时候，这就是这个对象的死期了。这种方式十分高效，而且简单。</p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcly1fnhj99v7b8j31hm0c4dh6.jpg" alt="引用计数方法对象的的数据结构"></p>
<center>*引用计数方法对象的的数据结构*</center>

<p>事情到此看起来都很美好。然而，却有一种循环引用的问题让想使用这种算法的 GC 退缩。那便是<code>循环引用</code>问题，像下图这种，两个对象相互引用，但是没有其他对象引用这两个对象的情况，实际上这两个对象已经都没有利用价值了，应该让 GC 回收。可是由于两个对象互相都持有对方的一个引用，让 GC 以为他们都还需要继续生存以服务另外的对象。长期下去会造成很严重的内存泄露。</p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcly1fnhjjr6atzj31ko0f4q5k.jpg" alt="循环引用对象"></p>
<center>*循环引用对象*</center>

<p>除了上面这种最简单的引用计数法，还有<code>延迟引用计数法</code>和<code>Sticky引用计数法</code>。这里不再深入讨论，可以参考 <a href="http://kklin.farbox.com/post/cao-zuo-xi-tong/xu-ni-ji/2-gcyin-yong-ji-shu-suan-fa">GC引用计数算法</a> ，或者直接阅读<a href="https://www.amazon.cn/dp/B01JZS0AO8/ref=sr_1_1?ie=UTF8&amp;qid=1516017889&amp;sr=8-1&amp;keywords=%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6%E7%9A%84%E7%AE%97%E6%B3%95%E4%B8%8E%E5%AE%9E%E7%8E%B0">《垃圾回收的算法与实现》</a>。</p>
<h4 id="可达性分析（Reachablity-Analysis）"><a href="#可达性分析（Reachablity-Analysis）" class="headerlink" title="可达性分析（Reachablity Analysis）"></a>可达性分析（Reachablity Analysis）</h4><p>与<code>引用计数法</code>相比，可达性分析就显得更加复杂了。可达性分析是通过<code>GC Root</code>开始搜索整个对象池，如果某些对象无法搜索到，在图论中我们称之为不可达，那么这些不可达的对象就会成为<code>GC</code>的刀下鬼了。通过这种方式就能够实现回收循环引用的对象。</p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcly1fnhkmkwjq0j31kw15fwt3.jpg" alt=""></p>
<center>*可达性分析判断对象是否可回收*</center>

<p>在<code>HotSpot</code>中<code>GC Root</code>主要有</p>
<ul>
<li>虚拟机栈（栈帧中的本地变量表）中引用的对象。</li>
<li>方法区中类静态属性引用的对象。</li>
<li>方法区中常量引用的对象</li>
<li>本地方法栈中<code>JNI</code>（即一般说的<code>Native</code>方法）引用的对象</li>
</ul>
<h5 id="HotSpot-的可达性分析实现"><a href="#HotSpot-的可达性分析实现" class="headerlink" title="HotSpot 的可达性分析实现"></a>HotSpot 的可达性分析实现</h5><p>在使用<code>可达性分析</code>算法的时候，不可避免地会引起<code>GC 停顿</code>，因为必须保证在进行可达性分析的时候对象之间的引用关系是不变的，我们无法针对一个在不断变化的图分析他的可达性。因此在进行可达性分析的时候难免要停止所有的用户线程。我们通常称之为<code>STW(Stop The Word)</code>. 有些收集器通过一些手段可以减少<code>STW</code>的时间（例如<code>CMS</code>），但是无法跳过这个过程。</p>
<p>目前主流的 Java 虚拟机使用的都是<code>准确式GC</code>，就是说当<code>STW</code>发生的时候，并不需要检查全部的变量从而确定那些变量是引用类型，而是在类加载完成之后会将引用类型的变量存在一个数据结构中，在<code>HotSpot</code>虚拟机中使用<code>OopMap</code>来记录。</p>
<p><code>准确式GC</code>解决了引用类型的定位问题，但是引用类型之间的关系错综复杂，如果为每一条指令都生成对应<code>OopMap</code>，那么<code>GC</code>的空间成本就会很大。为了解决这问题，引入了<code>安全点（Safepoint）</code>和<code>安全区域（Safe Region）</code>两个概念。<code>HotSpot</code>只有在<code>安全点（Safepoint）</code>和<code>安全区域（Safe Region）</code>这两个位置才会生成相应的<code>OopMap</code>，这两个区域的选择标准是能够让程序长时间执行（也就是这段时间内不会发生引用关系的改变）的指令。</p>
<p><code>安全点</code>一般设置在循环的末尾（防止大循环的时候一直不进入safepoint，而其他线程在等待它进入safepoint)、方法返回前、调用方法的call之后、抛出异常的位置。如何让程序在安全点上停下里等待GC也有两种方式，分别是<code>抢先式中断(Preemptive Suspension)</code>和<code>主动式中断(Voluntary Suspension)</code>. 抢先式中断不需要程序配合，当 GC 发生时会中断所有的线程，然后找出不在安全点上的线程让其继续执行到最近的安全点；主动式中断需要程序进行配合，设置一个标识位，当GC开始后这个标识位置位，所有的进程到达安全点之后会主动的去查询这个标识位，如果为真时就会自动中断挂起自己。</p>
<p><code>安全区域</code>是配合安全点的一种机制，因为有些指令程序是“不执行”的，例如线程处在 Sleep 状态或者 Blocked 状态，如果在 GC 的时候一直等待进入安全点，会使 GC 停顿变得十分长。 当线程执行到安全区域的开始位置的时候，会标识自己已经进入安全区域了，而在离开的时候会主动检查系统是否已经完成了根结点的枚举，如果没有就继续等待，如果完成当前线程才可以继续执行后面的代码。</p>
<h2 id="垃圾收集器简介"><a href="#垃圾收集器简介" class="headerlink" title="垃圾收集器简介"></a>垃圾收集器简介</h2><p>按照使用场景介绍常用的垃圾收集器，介绍他们的适用场景。让读者能够根据自己的应用场景来选择合适的收集器。</p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcly1fnjuas1qofj30f70aeaar.jpg" alt="垃圾收集器组合"></p>
<center>*垃圾收集器组合和分类*</center>

<p>上图中展示了各种收集器是新生代收集器还是老年代收集器，如果上面两种收集器时间有连线，则表示可以配合使用。具体的GC配置参数可以参照 <a href="/experiment-on-JVM.html">常用垃圾收集器参数</a></p>
<p>首先说明两个名词的含义，以免在下面的阅读中产生歧义：</p>
<ul>
<li><code>并行（Parallel）</code>：指多条垃圾收集线程并行工作，但此时用户线程仍然处于等待状态</li>
<li><code>并发(Concurrent)</code>：用户线程与垃圾收集线程同时执行（并行执行，或者交替执行）</li>
</ul>
<h3 id="新生代收集器"><a href="#新生代收集器" class="headerlink" title="新生代收集器"></a>新生代收集器</h3><p>本文介绍的新生代收集器都是使用的<code>复制算法</code></p>
<h4 id="Serial-收集器"><a href="#Serial-收集器" class="headerlink" title="Serial 收集器"></a>Serial 收集器</h4><p>串行垃圾收集器，采用复制算法，Serial 说明他是单线程工作的，他只会使用一个CPU，使用一个线程去完成垃圾回收工作，同时这也意味着当<code>Serial GC</code>进行垃圾回收的时候，用户线程也不得不暂停（STW）。<code>Serial 收集器</code>+<code>Serial Old 收集器</code>垃圾回收的时间线如下图所示：<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcly1fnjuxtkhfmj30ja06xdgi.jpg" alt="Serial/Serial Old"><br>这样的用户线程对于现在的服务来说基本是不可接受的，减少 GC 停顿也一直是很多垃圾收集器的努力目标。看似十分“无能”的<code>Serial GC</code>只能在客户端中发挥作用，因为一般客户端应用需要的内存并不是很大，因此停顿几十毫秒就可以完成内存回收，这样也是可以接受了，同时省去了并行 GC 切换线程的开销。</p>
<h4 id="ParNew-收集器"><a href="#ParNew-收集器" class="headerlink" title="ParNew 收集器"></a>ParNew 收集器</h4><p><code>ParNew 收集器</code>基本上就是<code>Serial 收集器</code>的升级版本，在新生代中收集的时候采用了并行的方案。其他部分与<code>Serial 收集器</code>基本一样。该收集器在单 CPU 的环境下并不会有优于<code>Serial</code>的表现，，反而会由于线程的频繁切换而降低性能，而对于多核CPU就会有较好的表现了。因此比较适合服务端程序。<code>ParNew 收集器</code>+<code>Serial Old 收集器</code>的 GC 过程如下图所示：<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcly1fnjv10ohmwj30j206vaas.jpg" alt=""><br>虽然<code>ParNew 收集器</code>只是针对<code>Serial 收集器</code>的简单升级，但是有着十分广泛的使用，原因就是能和<code>CMS收集器</code>搭配使用的只有<code>Serial 收集器</code>和<code>ParNew 收集器</code>两种。<code>CMS</code>的广泛使用就带来了<code>ParNew</code>的广泛使用。</p>
<h4 id="Parallel-Scavenge-收集器"><a href="#Parallel-Scavenge-收集器" class="headerlink" title="Parallel Scavenge 收集器"></a>Parallel Scavenge 收集器</h4><p><code>Parallel Scavenge 收集器</code>也是一个新生代收集器，他也是并行的、使用复制算法的。和<code>ParNew收集器</code>非常相似，GC过程图也与<code>ParNew收集器</code>相同。然而该收集器的关注点与其他收集器不同，其他收集器关注点都是如何缩短GC停顿时间，而<code>Parallel Scavenge 收集器</code>关注的确实如何保持系统一个较高的吞吐量（吞吐量 = 运行用户代码的时间/（运行用户代码时间+垃圾收集时间））。因此该收集器主要适合那些交互较少，运算较多的服务。该收集器允许用户设置极少的参数就能保证一个较高的吞吐量。</p>
<p>该收集器提供两个参数用于精确控制吞吐量<code>-XX:MaxGCPauseMillis</code>和<code>-XX:GCTimeRatio</code>两个参数，含义分别是最大GC停顿时间和直接设置吞吐量。GC的处理优先级是<code>MaxGCPauseMillis</code>最高，<code>GCTimeRatio</code>次之，其他的空间大小配置优先级最低。除了上面的两个参数之外，还有一个参数<code>-XX:+UseAdaptiveSizePolicy</code>，当设置了这个参数之后，就不需要再配置新生代的大小、Eden 和 Survivor 区域的比例、晋升老年代对象年龄等参数（设置了也没用），虚拟机会根据当前系统运行状况来动态调整上面的几个参数。</p>
<p><code>Parallel Scavenge</code>（-XX:+UseParallelGC）框架下，默认是在要触发full GC前先执行一次young GC，并且两次GC之间能让应用程序稍微运行一小下，以期降低full GC的暂停时间（因为young GC会尽量清理了young gen的死对象，减少了full GC的工作量）。控制这个行为的VM参数是<code>-XX:+ScavengeBeforeFullGC</code></p>
<h3 id="老年代收集器"><a href="#老年代收集器" class="headerlink" title="老年代收集器"></a>老年代收集器</h3><h4 id="Serial-Old-收集器"><a href="#Serial-Old-收集器" class="headerlink" title="Serial Old 收集器"></a>Serial Old 收集器</h4><p><code>Serial Old 收集器</code>就是<code>Serial 收集器</code>的老年代版本，主要也是给Client模式下的虚拟机使用。在 Server 应用中用于和 PS 收集器搭配使用，还有就是在 CMS 失败后，作为后备方案来进行垃圾收集。<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcly1fnjuxtkhfmj30ja06xdgi.jpg" alt="Serial/Serial Old"></p>
<h4 id="Parallel-Old-收集器"><a href="#Parallel-Old-收集器" class="headerlink" title="Parallel Old 收集器"></a>Parallel Old 收集器</h4><p><code>Parallel Old 收集器</code>是在<code>JDK1.6</code>中推出的，在此之前<code>Parallel Scavenge 收集器</code>只能和<code>Serial Old 收集器</code>搭配使用，由于<code>Serial Old 收集器</code>的拖累，导致高吞吐量的优势无法体现出来，这种组合的吞度量甚至不如<code>ParNew 收集器</code>+<code>CMS收集器</code>的组合。而<code>Parallel Old 收集器</code>的正式推出表示在需要关注高吞吐量的服务中可以使用<code>Parallel Scavenge 收集器</code>+<code>Parallel Old 收集器</code>的组合。这种组合的 GC 回收过程线程运行情况如下图：<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcly1fnjuy6qgsrj30j606ugmd.jpg" alt=""></p>
<h4 id="Concurrent-Mark-Sweep-收集器"><a href="#Concurrent-Mark-Sweep-收集器" class="headerlink" title="Concurrent Mark Sweep 收集器"></a>Concurrent Mark Sweep 收集器</h4><p><code>CMS 收集器</code>收集器是以最短的停顿时间为目标的收集器，这样的目标可以保证服务的高可用性，因此也成为了各种以交互为目的的服务的首选收集器。该收集器采用的是<code>标记-清除算法</code>，而前面介绍的收集器都是采用的<code>标记-整理算法</code>。该收集器 GC 过程相对复杂一些。分为以下四个步骤：</p>
<ul>
<li>初始标记(CMS initial mark)</li>
<li>并发标记(CMS concurrent mark)</li>
<li>重新标记(CMS final remark)</li>
<li>并发清除(CMS concurrent sweep)</li>
<li>重置线程(CMS concurrent reset）</li>
</ul>
<p>这里截取一段 GC 日志<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">2017-11-07T16:20:41.582+0800: 2.119: [GC (CMS Initial Mark) [1 CMS-initial-mark: 0K(3670016K)] 26854K(4141888K), 0.0036737 secs] [Times: user&#x3D;0.01 sys&#x3D;0.00, real&#x3D;0.01 secs]</span><br><span class="line">2017-11-07T16:20:41.585+0800: 2.123: [CMS-concurrent-mark-start]</span><br><span class="line">2017-11-07T16:20:41.587+0800: 2.125: [CMS-concurrent-mark: 0.002&#x2F;0.002 secs] [Times: user&#x3D;0.01 sys&#x3D;0.00, real&#x3D;0.00 secs]</span><br><span class="line">2017-11-07T16:20:41.587+0800: 2.125: [CMS-concurrent-preclean-start]</span><br><span class="line">2017-11-07T16:20:41.595+0800: 2.133: [CMS-concurrent-preclean: 0.008&#x2F;0.008 secs] [Times: user&#x3D;0.05 sys&#x3D;0.00, real&#x3D;0.01 secs]</span><br><span class="line">2017-11-07T16:20:41.595+0800: 2.133: [CMS-concurrent-abortable-preclean-start]</span><br><span class="line">2017-11-07T16:20:43.996+0800: 4.534: [CMS-concurrent-abortable-preclean: 1.514&#x2F;2.401 secs] [Times: user&#x3D;5.75 sys&#x3D;0.28, real&#x3D;2.40 secs]</span><br><span class="line">2017-11-07T16:20:43.996+0800: 4.534: [GC (CMS Final Remark) [YG occupancy: 241272 K (471872 K)]4.534: [Rescan (parallel) , 0.0123556 secs]4.546: [weak refs processing, 0.0000203 secs]4.546: [class unloading, 0.0059852 secs]4.552: [scrub symbol table, 0.0055144 secs]4.558: [scrub string table, 0.0006434 secs][1 CMS-remark: 0K(3670016K)] 241272K(4141888K), 0.0260691 secs] [Times: user&#x3D;0.11 sys&#x3D;0.00, real&#x3D;0.02 secs]</span><br><span class="line">2017-11-07T16:20:44.023+0800: 4.560: [CMS-concurrent-sweep-start]</span><br><span class="line">2017-11-07T16:20:44.023+0800: 4.560: [CMS-concurrent-sweep: 0.000&#x2F;0.000 secs] [Times: user&#x3D;0.00 sys&#x3D;0.00, real&#x3D;0.00 secs]</span><br><span class="line">2017-11-07T16:20:44.023+0800: 4.560: [CMS-concurrent-reset-start]</span><br><span class="line">2017-11-07T16:20:44.041+0800: 4.578: [CMS-concurrent-reset: 0.018&#x2F;0.018 secs] [Times: user&#x3D;0.07 sys&#x3D;0.02, real&#x3D;0.02 secs]</span><br></pre></td></tr></table></figure></p>
<blockquote>
<p>real是程序的实际运行时间，sys是内核态的时间，user是用户态的时间，单核情况，real远远大于user和sys之和。real，从程序开始到程序执行结束时所消耗的时间，包括CPU的用时和所有延迟程序执行的因素的总和。CPU用时被划分为user和sys两块。user表示程序本身，以及它所调用的库中的子例程使用的时间。sys是由程序直接或间接调用的系统调用执行的时间。<code>real=cpu用时+其他因素时间</code>,<code>cpu 用时=user+sys</code>,所以： real&gt; user + sys （单核情况）</p>
</blockquote>
<p>GC 过程中各个阶段 GC 线程和用户线程的运行关系如下图：<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcly1fnjuyc1e48j30jj06hjsa.jpg" alt=""></p>
<p>初始标记、重新标记这两个步骤需要 STW, 初始标记仅仅是标记以下 <code>GC Roots</code> 能够直接关联到的对象，速度很快，并发标记阶段就是进行<code>GC RootsTracing</code>的过程。重新标记是为了修正并发标记期间由于用户线程还在继续执行而产生变动的那一部分对象，这个阶段相比初始标记要长一点，但是远比并发标记短。整体来看用时最多的几个阶段：并发标记、并发清理、重置线程都会都是并发的，这样可以让应用程序尽可能的减少停顿。</p>
<blockquote>
<p>【关于CMS-concurrent-abortable-preclean】：从日志中我们还发现了一个细节叫做CMS-concurrent-abortable-preclean，这就要从Concurrent precleaning阶段说起了。Concurrent precleaning阶段的实际行为是：针对新生代做抽样，等待新生代在某个时间段（默认5秒，可以通过CMSMaxAbortablePrecleanTime参数设置）执行一次Minor GC，如果这个时间段内GC没有发生，那么就继续进行下一阶段（Remark）；如果时间段内触发了Minor GC，则可能会执行一些优化（具体可以参考<a href="https://blogs.oracle.com/jonthecollector/entry/did_you_know">https://blogs.oracle.com/jonthecollector/entry/did_you_know</a> ）</p>
<p>并发可中止的预清理阶段。这个阶段其实跟上一个阶段做的东西一样，也是为了减少下一个STW重新标记阶段的工作量。增加这一阶段是为了让我们可以控制这个阶段的结束时机，比如扫描多长时间（默认5秒）或者Eden区使用占比达到期望比例（默认50%）就结束本阶段。</p>
</blockquote>
<p><code>CMS 收集器</code>也有明显的几个缺点：一是 CMS 默认启动的回收线程数是<code>（CPU数量+3）/4</code>，也就是当 CPU 在 4 个以上时，并发垃圾回收时 GC线程占用不少于 25% 的 CPU 资源，当 CPU 不足 4 个时，情况就变得更加严峻了。第二个缺点就是 CMS 无法处理<code>浮动垃圾(Floating Garbage)</code>，浮动垃圾是指在并发清理阶段新产生的垃圾，由于 CMS 垃圾回收线程要和用户线程并发，因此必须要保留一部分内存在回收期间供用户线程使用，增额比例通过参数<code>CMSInitiatingOccupancyFraction</code>设置，表示老年代空间占用比例达到多少的时候会出发CMS GC，这个值在<code>JDK1.6</code>中默认值是<code>68</code>，在<code>JDK1.7</code>和<code>JDK1.8</code>中都是<code>92</code>。如果在 CMS GC 期间剩余的老年代内存空间不足已支持程序继续执行，就是触发GC降级，也就是<code>Concurrent Mode Failure</code>,这个时候就需要使用<code>Serial Old收集器</code>来完成老年代回收的任务，效率可想而知。最后一个缺点就是<code>标记-清除算法</code>带来的内存碎片问题，这个问题可以通过参数<code>-XX：+UseCMSCompactAtFullCollection</code>来缓解（默认开启），用于在CMS进行Full GC的时候进行碎片的合并整理，但是内存整理并不是并发的，会造成的应用程序的停顿，所以通常配合<code>-XX：CMSFullGCsBeforeCompaction</code>参数一起使用，该参数表示在允许连续几次的不整理碎片的CMS GC。</p>
<h3 id="G1收集器"><a href="#G1收集器" class="headerlink" title="G1收集器"></a>G1收集器</h3><p><code>Garbage First 收集器</code>应该是当今最前沿的垃圾收集器了，它的优点是：</p>
<blockquote>
<p>并行与并发：G1能充分利用多CPU、多核环境下的硬件优势，使用多个CPU（CPU或者CPU核心）来缩短Stop-The-World停顿的时间，部分其他收集器原本需要停顿Java线程执行的GC动作，G1收集器仍然可以通过并发的方式让Java程序继续执行。</p>
<p>分代收集：与其他收集器一样，分代概念在G1中依然得以保留。虽然G1可以不需要其他收集器配合就能独立管理整个GC堆，但它能够采用不同的方式去处理新创建的对象和已经存活了一段时间、熬过多次GC的旧对象以获取更好的收集效果。</p>
<p>空间整合：与CMS的“标记—清理”算法不同，G1从整体来看是基于“标记—整理”算法实现的收集器，从局部（两个Region之间）上来看是基于“复制”算法实现的，但无论如何，这两种算法都意味着G1运作期间不会产生内存空间碎片，收集后能提供规整的可用内存。这种特性有利于程序长时间运行,分配大对象时不会因为无法找到连续内存空间而提前触发下一次GC。</p>
<p>可预测的停顿：这是G1相对于CMS的另一大优势，降低停顿时间是G1和CMS共同的关注点，但G1除了追求低停顿外，还能建立可预测的停顿时间模型，能让使用者明确指定在一个长度为M毫秒的时间片段内，消耗在垃圾收集上的时间不得超过N毫秒，这几乎已经是实时Java（RTSJ）的垃圾收集器的特征了。</p>
<p>摘录来自: 周志明. “深入理解Java虚拟机：JVM高级特性与最佳实践（第2版）”。 iBooks.</p>
</blockquote>
<p>G1 收集器虽然还有老年代和新生代的感念，但是内存布局上却没有为其划定单独的物理隔离的区域，而是分给它们不同数量（无需连续）的<code>Region</code>。G1 能够预测停顿的功能就是依赖于 Region 这个东西实现的，因为将内存分割成多个大小相等的区域，然后在后台维护一个垃圾回收价值列表，每次根据允许的回收时间（使用参数<code>-XX:MaxGCPauseMillis</code>设定，默认值为<code>200</code>）来确定那些 Region 进行回收。理解起来很简单的 Region 回收，实现起来却很困难，其中一个主要的原因就是 Region 之间的对象相互引用，如果到回收时才进行可达性分析要扫描整个Java堆才能完成分析，同样的问题也存在于其他分代收集器新生代和老年代相互引用的关系中。虚拟机使用<code>Remembered Set</code>来避免扫描全堆，程序在对 Reference 类型进行写操作的时候会检测引用的对象是否存在于不同的 Region（或者是不同年代）中，如果是就将应用信息记录到 <strong>被引用对象</strong> 的 Remembered Set 中，在内存回收时，将 Remembered Set 加入 GC Roots 即可避免全堆扫描。</p>
<p>G1 收集器的运作大致可以分为以下几个步骤：</p>
<ol>
<li>初始标记（Initial Marking）：仅仅标记 GC Roots直接关联的对象</li>
<li>并发标记（Concurrent Marking）：从 GC Roots开始做可达性分析</li>
<li>最终标记（Final Marking）：修正在并发标记阶段变动的标记</li>
<li>筛选回收 （Live Data Counting and Evacuation）：根据回收机制和成本排序，根据用户期望的停顿时间制定回收计划</li>
</ol>
<p>线程运行情况如下图：<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tKfTcly1fnjuyko6aij30jj06ddgp.jpg" alt=""></p>
<p>更多可参考Oracle关于G1调优的官方文档：<a href="http://www.oracle.com/technetwork/cn/articles/java/g1gc-1984535.html">Garbage First Garbage Collector Tuning
</a></p>
<h2 id="参考内容"><a href="#参考内容" class="headerlink" title="参考内容"></a>参考内容</h2><ol>
<li><a href="http://kklin.farbox.com/post/cao-zuo-xi-tong/xu-ni-ji/2-gcyin-yong-ji-shu-suan-fa">GC引用计数算法</a></li>
<li><a href="https://www.amazon.cn/dp/B01JZS0AO8/">《垃圾回收的算法与实现》</a></li>
<li><a href="https://www.amazon.cn/dp/B00D2ID4PK/">《深入理解Java虚拟机:JVM高级特性与最佳实践(第2版)》</a></li>
<li>部分图片来源：<a href="https://crowhawk.github.io/2017/08/15/jvm_3/">深入理解JVM(3)——7种垃圾收集器</a></li>
<li><a href="https://docs.oracle.com/javase/8/docs/technotes/guides/vm/gctuning/cms.html">Concurrent Mark Sweep (CMS) Collector</a></li>
</ol>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>JVM</tag>
        <tag>tech</tag>
      </tags>
  </entry>
  <entry>
    <title>JVM-探究（四）：虚拟机监控和分析工具</title>
    <url>/tools-of-jvm.html</url>
    <content><![CDATA[<p>文章用来介绍常用的虚拟机内存监控和虚拟机相关的故障排除工具。 包括<code>JDK</code>自带的命令行工具、和一些可视化工具，如<code>jvisualvm</code>。 <span id="more"></span></p>
<h1 id="命令行工具"><a href="#命令行工具" class="headerlink" title="命令行工具"></a>命令行工具</h1><p><code>JDK</code>一些常用的命令行工具，能够让你不安装、不需要在服务器输出文件拷贝到本地的在进行分析，从而能够快速地查看虚拟机参数，了解虚拟机当前的运行状况，十分实用。本问介绍的命令都需要在 JDK8 或以上的环境运行，如果在低于 JDK8 可能会有部分指标乱码。</p>
<p>命令一览</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>名称</th>
<th>主要作用</th>
</tr>
</thead>
<tbody>
<tr>
<td>jps</td>
<td>JVM Process Status Tool， 显示系统内所有的 HotSpot 进程。 <strong>注意</strong>：只显示当前用户的</td>
</tr>
<tr>
<td>jstat</td>
<td>JVM Statistics Monitoring Tool， 用于收集 HotSpot 虚拟机各方面的参数</td>
</tr>
<tr>
<td>jinfo</td>
<td>Configuration Info for Java， 显示虚拟机配置信息</td>
</tr>
<tr>
<td>jmap</td>
<td>Memory Map for Java，生成虚拟机的内存转储快照片</td>
</tr>
<tr>
<td>jhat</td>
<td>JVM Heap Dump Browser，用于分析heapdump文件，他会建立一个 HTTP/HTML 服务器，让用户可以在榴浏览器上查看分析结果</td>
</tr>
<tr>
<td>jstack</td>
<td>Stack Trace for Java， 显示虚拟机的线程快照</td>
</tr>
</tbody>
</table>
</div>
<h2 id="jps"><a href="#jps" class="headerlink" title="jps"></a>jps</h2><p>全名：Java Virtual Machine Process Status Tool</p>
<h3 id="主要选项"><a href="#主要选项" class="headerlink" title="主要选项"></a>主要选项</h3><div class="table-container">
<table>
<thead>
<tr>
<th>选项</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>-q</td>
<td>忽略主类名称，只输出 LVMID（一般和进程号相同</td>
</tr>
<tr>
<td>-m</td>
<td>打印被传入到 main() 方法的参数</td>
</tr>
<tr>
<td>-l</td>
<td>打印 main 方法的包名，或者运行的 JAR 包的全路径名</td>
</tr>
<tr>
<td>-v</td>
<td>打印传递给 JVM 的参数</td>
</tr>
<tr>
<td>-V</td>
<td>打印通过 flags file 传递给 JVM 的参数</td>
</tr>
<tr>
<td>-Joption</td>
<td>因为jps也是运行在 JVM 之上的，这就是传递给运行jps的JVM的参数。eg:<code>-J-Xms48m</code></td>
</tr>
</tbody>
</table>
</div>
<h3 id="使用示例"><a href="#使用示例" class="headerlink" title="使用示例"></a>使用示例</h3><p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1fo167dq18uj30rs058gm3.jpg" alt=""></p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1fo168vo9vfj30x80sodnb.jpg" alt=""></p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1fo169qe498j30xc05emxy.jpg" alt=""></p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1fo16a52f55j30x6146tl3.jpg" alt=""></p>
<p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1fo16alsly8j30x605adgc.jpg" alt=""></p>
<h2 id="jstat"><a href="#jstat" class="headerlink" title="jstat"></a>jstat</h2><p>全名：Java Virtual Machine statistics monitoring tool</p>
<h3 id="主要选项-1"><a href="#主要选项-1" class="headerlink" title="主要选项"></a>主要选项</h3><div class="table-container">
<table>
<thead>
<tr>
<th>选项</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td>-class</td>
<td>监视类装载、卸载数量、总空间以及类装载所耗费的时间</td>
</tr>
<tr>
<td>-gc</td>
<td>监视Java堆状况，包括Eden区域、两个survivor区、老年代、元空间等的容量、已用空间、GC 时间合计等信息</td>
</tr>
<tr>
<td>-compiler</td>
<td>输出 JIT 编译器编译过的方法、耗时等信息</td>
</tr>
<tr>
<td>-gccapacity</td>
<td>监视内容与-gc基本相同，但输出主要关注Java堆各个区域使用到的最大、最小空间</td>
</tr>
<tr>
<td>-gcutil</td>
<td>监视内容与-gc基本相同，但输出主要关注已使用空间占总空间百分比</td>
</tr>
<tr>
<td>-gccause</td>
<td>与 -gcutil 功能一样，但是会额外输出导致上一次GC产生的原因</td>
</tr>
<tr>
<td>-gcnew</td>
<td>监视新生代 GC 状况</td>
</tr>
<tr>
<td>-gcnewcapacity</td>
<td>监视内容与 -gcnew 基本相同，输出主要关注使用到的最大、最小空间</td>
</tr>
<tr>
<td>-gcold</td>
<td>监视老年代 GC 情况</td>
</tr>
<tr>
<td>-gcoldcapacity</td>
<td>监视内容与 -gcold 基本相同，输出主要关注使用到的最大、最小空间</td>
</tr>
<tr>
<td>-gcpermcapacity</td>
<td>输出永久代使用到的最大、最小空间（JDK8中已经没有了，并且暂时没有找到查看metaspace的大小的命令，可以直接使用-gc）</td>
</tr>
<tr>
<td>-printcompilation</td>
<td>输出已经被 JIT 编译过的方法</td>
</tr>
</tbody>
</table>
</div>
<h3 id="使用示例-1"><a href="#使用示例-1" class="headerlink" title="使用示例"></a>使用示例</h3><h4 id="jstat-class"><a href="#jstat-class" class="headerlink" title="jstat -class"></a>jstat -class</h4><p><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNc79ly1fo282a6klrj30xc03a3yy.jpg" alt=""></p>
<p>从左到右的含义分别是：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>选项</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>Loaded</td>
<td>已经加载的类数量</td>
</tr>
<tr>
<td>Bytes</td>
<td>已经加载类大小</td>
</tr>
<tr>
<td>Unloaded</td>
<td>卸载的类数量</td>
</tr>
<tr>
<td>Bytes</td>
<td>卸载的类大小</td>
</tr>
<tr>
<td>Time</td>
<td>类加载和卸载操作消耗的时间</td>
</tr>
</tbody>
</table>
</div>
<h4 id="jstat-compiler"><a href="#jstat-compiler" class="headerlink" title="jstat -compiler"></a>jstat -compiler</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">~ jstat -compiler  50674</span><br><span class="line">Compiled Failed Invalid   Time   FailedType FailedMethod</span><br><span class="line">   1248      0       0     1.36          0</span><br></pre></td></tr></table></figure>
<p>从左到右的含义分别是：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>选项</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>Compiled</td>
<td>JIT 编译任务的数量</td>
</tr>
<tr>
<td>Failed</td>
<td>JIT 失败的编译任务</td>
</tr>
<tr>
<td>Invalid</td>
<td>无效的 JIT 编译任务</td>
</tr>
<tr>
<td>Time</td>
<td>编译需要的时间</td>
</tr>
<tr>
<td>FailedType</td>
<td>最后一个编译失败的原因</td>
</tr>
<tr>
<td>FailedMethod</td>
<td>最后一个编译失败的类名和方法</td>
</tr>
</tbody>
</table>
</div>
<h4 id="jstat-gc"><a href="#jstat-gc" class="headerlink" title="jstat -gc"></a>jstat -gc</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">jacob@JacobMBPlocal~ jstat -gc  50674</span><br><span class="line"> S0C    S1C    S0U    S1U      EC       EU        OC         OU       MC     MU    CCSC   CCSU   YGC     YGCT    FGC    FGCT     GCT</span><br><span class="line">10752.0 10752.0  0.0   5396.8 65536.0  24964.9   175104.0     80.0    17536.0 17112.7 2176.0 2040.8      1    0.006   0      0.000    0.006</span><br></pre></td></tr></table></figure>
<p>从左到右的含义分别是：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>选项</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>SOC</td>
<td>S0 区域总大小</td>
</tr>
<tr>
<td>S1C</td>
<td>S1 区域总大小</td>
</tr>
<tr>
<td>S0U</td>
<td>S0 区域使用大小</td>
</tr>
<tr>
<td>S1U</td>
<td>S1 区域使用大小</td>
</tr>
<tr>
<td>EC</td>
<td>Eden 总大小</td>
</tr>
<tr>
<td>EU</td>
<td>Eden 区域使用大小</td>
</tr>
<tr>
<td>OC</td>
<td>当前 Old Space 总大小</td>
</tr>
<tr>
<td>OU</td>
<td>Old Space 使用大小</td>
</tr>
<tr>
<td>PC</td>
<td>当前 Permanent Space 总大小</td>
</tr>
<tr>
<td>PU</td>
<td>Permanent Space 使用大小</td>
</tr>
<tr>
<td>YCG</td>
<td>新生代 GC 发生次数</td>
</tr>
<tr>
<td>YGCT</td>
<td>新生代 GC 时间</td>
</tr>
<tr>
<td>FGC</td>
<td>full GC 次数q</td>
</tr>
<tr>
<td>FGCT</td>
<td>full GC 时间</td>
</tr>
<tr>
<td>GCT</td>
<td>垃圾回收时间总和</td>
</tr>
</tbody>
</table>
</div>
<h4 id="jstat-gccapacity"><a href="#jstat-gccapacity" class="headerlink" title="jstat -gccapacity"></a>jstat -gccapacity</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">~ jstat -gccapacity  50674</span><br><span class="line">NGCMN    NGCMX     NGC     S0C   S1C       EC      OGCMN      OGCMX       OGC         OC       MCMN     MCMX      MC     CCSMN    CCSMX     CCSC    YGC    FGC</span><br><span class="line">87040.0 238592.0  87040.0 10752.0 10752.0  65536.0   175104.0   478208.0   175104.0   175104.0      0.0 1064960.0  17536.0      0.0 1048576.0   2176.0      1     0</span><br></pre></td></tr></table></figure>
<p>从左到右的含义分别是：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>选项</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>NGCMN</td>
<td>新生代使用到的最小空间。 Minimum new generation capacity (KB).</td>
</tr>
<tr>
<td>NGCMX</td>
<td>新生代使用到的最大空间。Maximum new generation capacity (KB).</td>
</tr>
<tr>
<td>NGC</td>
<td>当前新生代大小。Current new generation capacity (KB).</td>
</tr>
<tr>
<td>S0C</td>
<td>当前 S0 区域大小。Current survivor space 0 capacity (KB).</td>
</tr>
<tr>
<td>S1C</td>
<td>当前 S1 区域大小。Current survivor space 1 capacity (KB).</td>
</tr>
<tr>
<td>EC</td>
<td>当前 Eden 区域大小。Current eden space capacity (KB).</td>
</tr>
<tr>
<td>OGCMN</td>
<td>老年代使用到的最小空间大小。Minimum old generation capacity (KB).</td>
</tr>
<tr>
<td>OGCMX</td>
<td>老年代使用到的最大空间大小。Maximum old generation capacity (KB).</td>
</tr>
<tr>
<td>OGC</td>
<td>当前老年代大小。Current old generation capacity (KB).</td>
</tr>
<tr>
<td>OC</td>
<td>当前老年代空间大小。Current old space capacity (KB).</td>
</tr>
<tr>
<td>PGCMN</td>
<td>永久代使用到的最小空间。Minimum permanent generation capacity (KB).</td>
</tr>
<tr>
<td>PGCMX</td>
<td>永久代使用到的最大空间。Maximum Permanent generation capacity (KB).</td>
</tr>
<tr>
<td>PGC</td>
<td>当前永久代大小。Current Permanent generation capacity (KB).</td>
</tr>
<tr>
<td>PC</td>
<td>当前永久空间大小。Current Permanent space capacity (KB).</td>
</tr>
<tr>
<td>VGC</td>
<td>Young GC 次数。 Number of Young generation GC Events.</td>
</tr>
<tr>
<td>FGC</td>
<td>Full GC 次数。Number of Full GC Events.</td>
</tr>
</tbody>
</table>
</div>
<blockquote>
<p>如果读者对于 <code>Current old generation capacity</code> 和 <code>Current old space capacity</code> 有疑问，可以参考<code>StackOverFlow</code>上面的这个提问：<a href="https://stackoverflow.com/questions/11253285/jstat-difference-between-ogc-oc-pgc-pc">jstat: difference between OGC &amp; OC, PGC &amp; PC</a> ,简单点描述就是 一个<strong>代</strong>包含的空间不一定只有一个，只是 HotSpot 恰好只有一个。 <code>OGC = sum(all OC)</code></p>
</blockquote>
<h4 id="jstat-gcutil"><a href="#jstat-gcutil" class="headerlink" title="jstat -gcutil"></a>jstat -gcutil</h4><p><code>gcutil</code> 和 <code>gccapacity</code> 展现的数据基本相同，<code>gccapacity</code> 只是一个是精确的数据，<code>gcutil</code> 是更加直观的百分比。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ jstat -gcutil 17931</span><br><span class="line">  S0     S1     E      O      M     CCS    YGC     YGCT    FGC    FGCT     GCT</span><br><span class="line">  0.00   7.25   9.64   3.52  98.53  96.94    385    6.771     6    0.517    7.288</span><br></pre></td></tr></table></figure>
<p>从左到右的含义分别是：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>选项</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>S0</td>
<td>S0 区空间占用百分比。Survivor space 0 utilization as a percentage of the space’s current capacity.</td>
</tr>
<tr>
<td>S1</td>
<td>S1 区空间占用百分比。Survivor space 1 utilization as a percentage of the space’s current capacity.</td>
</tr>
<tr>
<td>E</td>
<td>Eden 区空间占用百分比。Eden space utilization as a percentage of thespace’s current capacity.</td>
</tr>
<tr>
<td>O</td>
<td>老年代空间占用百分比。Old space utilization as a percentage of the space’s current capacity.</td>
</tr>
<tr>
<td>P</td>
<td>永久代空间占用百分比。Permanent space utilization as a percentage ofthe space’s current capacity.</td>
</tr>
<tr>
<td>YGC</td>
<td>Young GC 次数。Number of young generation GC events.</td>
</tr>
<tr>
<td>YGCT</td>
<td>Young GC 时间。Young generation garbage collection time.</td>
</tr>
<tr>
<td>FGC</td>
<td>Full GC 次数。Number of Full GC events.</td>
</tr>
<tr>
<td>FGCT</td>
<td>Full GC 时间。Full garbage collection time.</td>
</tr>
<tr>
<td>GCT</td>
<td>总的 GC 时间。Total garbage collection time.</td>
</tr>
</tbody>
</table>
</div>
<h4 id="jstat-gccause"><a href="#jstat-gccause" class="headerlink" title="jstat -gccause"></a>jstat -gccause</h4><p><code>gccause</code>和<code>gccause</code>基本相同，多出了两个参数，分别表示上一次 GC 的原因，和本次 GC 的原因。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ jstat -gccause 17931</span><br><span class="line">  S0     S1     E      O      M     CCS    YGC     YGCT    FGC    FGCT     GCT    LGCC                 GCC</span><br><span class="line">  5.30   0.00  40.79   3.82  98.34  95.90    438    7.501     6    0.517    8.018 Allocation Failure   No GC</span><br></pre></td></tr></table></figure>
<p>新增的两个参数：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>选项</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>LGCC</td>
<td>上次 GC 的原因。Cause of last Garbage Collection.</td>
</tr>
<tr>
<td>GCC</td>
<td>本次 GC 的原因。Cause of current Garbage Collection.</td>
</tr>
</tbody>
</table>
</div>
<h4 id="jstat-gcnew"><a href="#jstat-gcnew" class="headerlink" title="jstat -gcnew"></a>jstat -gcnew</h4><p>新生代统计</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ jstat -gcnew 17931</span><br><span class="line"> S0C    S1C    S0U    S1U   TT MTT  DSS      EC       EU     YGC     YGCT</span><br><span class="line">52416.0 52416.0    0.0 2820.6 15  15 26208.0 419456.0 321595.5    453    7.662</span><br></pre></td></tr></table></figure>
<div class="table-container">
<table>
<thead>
<tr>
<th>选项</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>SOC</td>
<td>当前 S0 区域大小。Current survivor space 0 capacity (KB).</td>
</tr>
<tr>
<td>S1C</td>
<td>当前 S1 区域大小。Current survivor space 1 capacity (KB).</td>
</tr>
<tr>
<td>S0U</td>
<td>S0 占用大小。Survivor space 0 utilization (KB).</td>
</tr>
<tr>
<td>S1U</td>
<td>S1 占用大小。Survivor space 1 utilization (KB).</td>
</tr>
<tr>
<td>TT</td>
<td>Tenuring 阈值。Tenuring threshold.</td>
</tr>
<tr>
<td>MTT</td>
<td>最大 Tenuring 阈值。Maximum tenuring threshold.</td>
</tr>
<tr>
<td>DSS</td>
<td>需要的S区域大小。Desired survivor size (KB).</td>
</tr>
<tr>
<td>EC</td>
<td>Current eden space capacity (KB).</td>
</tr>
<tr>
<td>EU</td>
<td>Eden space utilization (KB).</td>
</tr>
<tr>
<td>VGC</td>
<td>Number of young generation GC events.</td>
</tr>
<tr>
<td>VGCT</td>
<td>Young generation garbage collection time.</td>
</tr>
</tbody>
</table>
</div>
<blockquote>
<p>Tenuring 阈值是动态变化的，最大 Tenuring 阈值可以通过 JVM 参数设置。具体可以参考我的另外一篇博客 <a href="/experiment-on-jvm.html#%E5%AE%9E%E9%AA%8C%E5%85%AD%EF%BC%9ATenuringThreshold-%E6%98%AF%E5%8A%A8%E6%80%81%E5%8F%98%E5%8C%96%E7%9A%84">Tenuring Threshold 是动态变化的</a></p>
<p><code>DSS</code> 为需要的 S 空间的大小，如果实际空间不足，新生代的对象会提前进入老年代。<a href="/experiment-on-jvm.html#%E5%AE%9E%E9%AA%8C%E4%B8%80%EF%BC%9A%E4%BD%BF%E7%94%A8ParNew-Serial-Old%E7%9A%84%E7%BB%84%E5%90%88%E8%BF%9B%E8%A1%8C%E5%86%85%E5%AD%98%E5%9B%9E%E6%94%B6">提前转移实验</a></p>
</blockquote>
<h4 id="jstat-gcnewcapacity"><a href="#jstat-gcnewcapacity" class="headerlink" title="jstat -gcnewcapacity"></a>jstat -gcnewcapacity</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ jstat -gcnewcapacity 17931</span><br><span class="line">  NGCMN      NGCMX       NGC      S0CMX     S0C     S1CMX     S1C       ECMX        EC      YGC   FGC</span><br><span class="line">  524288.0   524288.0   524288.0  52416.0  52416.0  52416.0  52416.0   419456.0   419456.0   486     6</span><br></pre></td></tr></table></figure>
<div class="table-container">
<table>
<thead>
<tr>
<th>选项</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>NGCMN</td>
<td>新生代最大空间大小。Minimum new generation capacity (KB).</td>
</tr>
<tr>
<td>NGCMX</td>
<td>新生代最小空间大小。Maximum new generation capacity (KB).</td>
</tr>
<tr>
<td>NGC</td>
<td>当前新生代空间大小。Current new generation capacity (KB).</td>
</tr>
<tr>
<td>S0CMX</td>
<td>S0 区域最大空间大小。Maximum survivor space 0 capacity (KB).</td>
</tr>
<tr>
<td>S0C</td>
<td>S0 区域当前空间大小。Current survivor space 0 capacity (KB).</td>
</tr>
<tr>
<td>S1CMX</td>
<td>S1 区域最大空间大小。Maximum survivor space 1 capacity (KB).</td>
</tr>
<tr>
<td>S1C</td>
<td>S1 区域当前空间大小。Current survivor space 1 capacity (KB).</td>
</tr>
<tr>
<td>ECMX</td>
<td>Eden 区域最大空间大小。Maximum eden space capacity (KB).</td>
</tr>
<tr>
<td>EC</td>
<td>Eden 区域当前空间大小。Current eden space capacity (KB).</td>
</tr>
<tr>
<td>YGC</td>
<td>Young GC 次数。Number of young generation GC events.</td>
</tr>
<tr>
<td>FGC</td>
<td>Full GC 次数。Number of Full GC Events.</td>
</tr>
</tbody>
</table>
</div>
<h4 id="jstat-gcold"><a href="#jstat-gcold" class="headerlink" title="jstat -gcold"></a>jstat -gcold</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ jstat -gcold 17931</span><br><span class="line">   MC       MU      CCSC     CCSU       OC          OU       YGC    FGC    FGCT     GCT</span><br><span class="line">102248.0 100603.7  10324.0   9900.6   1572864.0     63154.7    497     6    0.517    8.744</span><br></pre></td></tr></table></figure>
<div class="table-container">
<table>
<thead>
<tr>
<th>选项</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>PC</td>
<td>当前永久代大小。Current permanent space capacity (KB).</td>
</tr>
<tr>
<td>PU</td>
<td>当前永久代占用。Permanent space utilization (KB).</td>
</tr>
<tr>
<td>OC</td>
<td>当前老年代大小。Current old space capacity (KB).</td>
</tr>
<tr>
<td>OU</td>
<td>当前老年代占用。Old space utilization (KB).</td>
</tr>
<tr>
<td>YGC</td>
<td>Young GC 次数。Number of young generation GC events.</td>
</tr>
<tr>
<td>FGC</td>
<td>Full GC 次数。Number of Full GC events.</td>
</tr>
<tr>
<td>FGCT</td>
<td>Full GC 时间。Full garbage collection time.</td>
</tr>
<tr>
<td>GCT</td>
<td>所有 GC 时间。Total garbage collection time.</td>
</tr>
</tbody>
</table>
</div>
<h4 id="jstat-gcoldcapacity"><a href="#jstat-gcoldcapacity" class="headerlink" title="jstat -gcoldcapacity"></a>jstat -gcoldcapacity</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ jstat -gcoldcapacity 17931</span><br><span class="line">   OGCMN       OGCMX        OGC         OC       YGC   FGC    FGCT     GCT</span><br><span class="line">  1572864.0   1572864.0   1572864.0   1572864.0   508     6    0.517    8.923</span><br></pre></td></tr></table></figure>
<div class="table-container">
<table>
<thead>
<tr>
<th>选项</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>OGCMN</td>
<td>最小老年代大小。Minimum old generation capacity (KB).</td>
</tr>
<tr>
<td>OGCMV</td>
<td>最大老年代大小。Maximum old generation capacity (KB).</td>
</tr>
<tr>
<td>OGC</td>
<td>当前老年代大小。Current old generation capacity (KB).</td>
</tr>
<tr>
<td>OC</td>
<td>当前老年空间大小。Current old space capacity (KB).</td>
</tr>
<tr>
<td>YGC</td>
<td>Young GC 次数。Number of young generation GC events.</td>
</tr>
<tr>
<td>FGC</td>
<td>Full GC 次数。Number of Full GC events.</td>
</tr>
<tr>
<td>FGCT</td>
<td>Full GC 时间。Full garbage collection time.</td>
</tr>
<tr>
<td>GCT</td>
<td>所有 GC 时间。Total garbage collection time.</td>
</tr>
</tbody>
</table>
</div>
<h4 id="jstat-printcompilation"><a href="#jstat-printcompilation" class="headerlink" title="jstat -printcompilation"></a>jstat -printcompilation</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ jstat -printcompilation 17931</span><br><span class="line">Compiled  Size  Type Method</span><br><span class="line">   21778   2719    1 com&#x2F;sankuai&#x2F;octo&#x2F;config&#x2F;model&#x2F;file_param_t hashCode</span><br></pre></td></tr></table></figure>
<div class="table-container">
<table>
<thead>
<tr>
<th>选项</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>Compiled</td>
<td>Number of compilation tasks performed.</td>
</tr>
<tr>
<td>Size</td>
<td>Number of bytes of bytecode for the method.</td>
</tr>
<tr>
<td>Type</td>
<td>Compilation type.</td>
</tr>
<tr>
<td>Method</td>
<td>Class name and method name identifying the compiled method. Class name uses “/“ instead of “.” as namespace separator. Method name is the method within the given class. The format for these two fields is consistent with the HotSpot -XX:+PrintComplation option.</td>
</tr>
</tbody>
</table>
</div>
<h4 id="jstat-的额外参数"><a href="#jstat-的额外参数" class="headerlink" title="jstat 的额外参数"></a>jstat 的额外参数</h4><p>上述的命令都可以使用类似下面的参数</p>
<p><code>jstat -gcutil -t 17931 500 100</code> 其中<code>-t</code>表示打印时间戳、<code>17931</code>为 lvmid、<code>500</code>表示间隔 500ms 来输出信息、 <code>100</code>表示信息条目显示d的数量。</p>
<p>譬如，下面显示了10行，显示时间间隔为500ms</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ jstat -gcutil 17931 500 10</span><br><span class="line">  S0     S1     E      O      M     CCS    YGC     YGCT    FGC    FGCT     GCT</span><br><span class="line">  9.55   0.00  69.10   4.26  98.34  95.92    552    8.950     6    0.517    9.466</span><br><span class="line">  9.55   0.00  71.22   4.26  98.34  95.92    552    8.950     6    0.517    9.466</span><br><span class="line">  9.55   0.00  72.88   4.26  98.34  95.92    552    8.950     6    0.517    9.466</span><br><span class="line">  9.55   0.00  74.90   4.26  98.34  95.92    552    8.950     6    0.517    9.466</span><br><span class="line">  9.55   0.00  77.01   4.26  98.34  95.92    552    8.950     6    0.517    9.466</span><br><span class="line">  9.55   0.00  78.98   4.26  98.34  95.92    552    8.950     6    0.517    9.466</span><br><span class="line">  9.55   0.00  80.78   4.26  98.34  95.92    552    8.950     6    0.517    9.466</span><br><span class="line">  9.55   0.00  82.90   4.26  98.34  95.92    552    8.950     6    0.517    9.466</span><br><span class="line">  9.55   0.00  84.71   4.26  98.34  95.92    552    8.950     6    0.517    9.466</span><br><span class="line">  9.55   0.00  86.85   4.26  98.34  95.92    552    8.950     6    0.517    9.466</span><br></pre></td></tr></table></figure>
<h2 id="jinfo"><a href="#jinfo" class="headerlink" title="jinfo"></a>jinfo</h2><p>输出 Java 配置信息</p>
<p>下面是 Intellj IDEA 的部分输出信息</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">jinfo 88747</span><br><span class="line">Attaching to process ID 88747, please wait...</span><br><span class="line">Debugger attached successfully.</span><br><span class="line">Server compiler detected.</span><br><span class="line">JVM version is 25.131-b11</span><br><span class="line">Java System Properties:</span><br><span class="line"></span><br><span class="line">java.vendor &#x3D; Oracle Corporation</span><br><span class="line">preload.project.path &#x3D; &#x2F;Users&#x2F;jacob&#x2F;Developer&#x2F;IDEA&#x2F;campaignmanage</span><br><span class="line">sun.java.launcher &#x3D; SUN_STANDARD</span><br><span class="line">idea.config.path &#x3D; &#x2F;Users&#x2F;jacob&#x2F;Library&#x2F;Preferences&#x2F;IntelliJIdea2017.3</span><br><span class="line">sun.management.compiler &#x3D; HotSpot 64-Bit Tiered Compilers</span><br><span class="line">sun.nio.ch.bugLevel &#x3D;</span><br><span class="line">idea.paths.selector &#x3D; IntelliJIdea2017.3</span><br><span class="line">kotlin.daemon.client.alive.path &#x3D; &quot;&#x2F;var&#x2F;folders&#x2F;hw&#x2F;_d9gvls95316wtq_crffgz3w0000gn&#x2F;T&#x2F;kotlin-idea-4717993236071674726-is-running&quot;</span><br><span class="line">jnidispatch.path &#x3D; &#x2F;Users&#x2F;jacob&#x2F;Library&#x2F;Caches&#x2F;IntelliJIdea2017.3&#x2F;compile-server&#x2F;campaignmanage_7408d958&#x2F;_temp_&#x2F;jna-100881631&#x2F;jna5884254379081076550.tmp</span><br><span class="line">os.name &#x3D; Mac OS X</span><br><span class="line">sun.boot.class.path &#x3D; &#x2F;Library&#x2F;Java&#x2F;JavaVirtualMachines&#x2F;jdk1.8.0_131.jdk&#x2F;Contents&#x2F;Home&#x2F;jre&#x2F;lib&#x2F;resources.jar:&#x2F;Library&#x2F;Java&#x2F;JavaVirtualMachines&#x2F;jdk1.8.0_131.jdk&#x2F;Contents&#x2F;Home&#x2F;jre&#x2F;lib&#x2F;rt.jar:&#x2F;Library&#x2F;Java&#x2F;JavaVirtualMachines&#x2F;jdk1.8.0_131.jdk&#x2F;Contents&#x2F;Home&#x2F;jre&#x2F;lib&#x2F;sunrsasign.jar:&#x2F;Library&#x2F;Java&#x2F;JavaVirtualMachines&#x2F;jdk1.8.0_131.jdk&#x2F;Contents&#x2F;Home&#x2F;jre&#x2F;lib&#x2F;jsse.jar:&#x2F;Library&#x2F;Java&#x2F;JavaVirtualMachines&#x2F;jdk1.8.0_131.jdk&#x2F;Contents&#x2F;Home&#x2F;jre&#x2F;lib&#x2F;jce.jar:&#x2F;Library&#x2F;Java&#x2F;JavaVirtualMachines&#x2F;jdk1.8.0_131.jdk&#x2F;Contents&#x2F;Home&#x2F;jre&#x2F;lib&#x2F;charsets.jar:&#x2F;Library&#x2F;Java&#x2F;JavaVirtualMachines&#x2F;jdk1.8.0_131.jdk&#x2F;Contents&#x2F;Home&#x2F;jre&#x2F;lib&#x2F;jfr.jar:&#x2F;Library&#x2F;Java&#x2F;JavaVirtualMachines&#x2F;jdk1.8.0_131.jdk&#x2F;Contents&#x2F;Home&#x2F;jre&#x2F;classes</span><br><span class="line">idea.plugins.path &#x3D; &#x2F;Users&#x2F;jacob&#x2F;Library&#x2F;Application Support&#x2F;IntelliJIdea2017.3</span><br><span class="line">java.vm.specification.vendor &#x3D; Oracle Corporation</span><br><span class="line">java.runtime.version &#x3D; 1.8.0_131-b11</span><br><span class="line">jna.loaded &#x3D; true</span><br><span class="line">io.netty.serviceThreadPrefix &#x3D; Netty</span><br><span class="line">user.name &#x3D; jacob</span><br><span class="line">kotlin.incremental.compilation &#x3D; true</span><br><span class="line">idea.home.path &#x3D; &#x2F;Applications&#x2F;IntelliJ IDEA.app&#x2F;Contents</span><br><span class="line">user.language &#x3D; zh</span><br><span class="line">jdt.compiler.useSingleThread &#x3D; true</span><br><span class="line">sun.boot.library.path &#x3D; &#x2F;Library&#x2F;Java&#x2F;JavaVirtualMachines&#x2F;jdk1.8.0_131.jdk&#x2F;Contents&#x2F;Home&#x2F;jre&#x2F;lib</span><br><span class="line">java.version &#x3D; 1.8.0_131</span><br><span class="line">user.timezone &#x3D; Asia&#x2F;Shanghai</span><br><span class="line">java.net.preferIPv4Stack &#x3D; true</span><br><span class="line">kotlin.daemon.enabled &#x3D;</span><br><span class="line">sun.arch.data.model &#x3D; 64</span><br><span class="line"></span><br><span class="line">java.vm.name &#x3D; Java HotSpot(TM) 64-Bit Server VM</span><br><span class="line">file.encoding &#x3D; UTF-8</span><br><span class="line">java.specification.version &#x3D; 1.8</span><br><span class="line">compile.parallel &#x3D; false</span><br><span class="line">io.netty.noUnsafe &#x3D; true</span><br><span class="line"></span><br><span class="line">VM Flags:</span><br><span class="line">Non-default VM flags: -XX:CICompilerCount&#x3D;4 -XX:InitialHeapSize&#x3D;268435456 -XX:MaxHeapSize&#x3D;734003200 -XX:MaxNewSize&#x3D;244318208 -XX:MinHeapDeltaBytes&#x3D;524288 -XX:NewSize&#x3D;89128960 -XX:OldSize&#x3D;179306496 -XX:+UseCompressedClassPointers -XX:+UseCompressedOops -XX:+UseFastUnorderedTimeStamps -XX:+UseParallelGC</span><br><span class="line">Command line:  -Xmx700m -Djava.awt.headless&#x3D;true -Djava.endorsed.dirs&#x3D;&quot;&quot; -Djdt.compiler.useSingleThread&#x3D;true -Dpreload.project.path&#x3D;&#x2F;Users&#x2F;jacob&#x2F;Developer&#x2F;IDEA&#x2F;campaignmanage -Dpreload.config.path&#x3D;&#x2F;Users&#x2F;jacob&#x2F;Library&#x2F;Preferences&#x2F;IntelliJIdea2017.3&#x2F;options -Dcompile.parallel&#x3D;false</span><br></pre></td></tr></table></figure>
<div class="table-container">
<table>
<thead>
<tr>
<th>选项</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>参数为空</td>
<td>打印命令行标记和系统属性</td>
</tr>
<tr>
<td>-flags</td>
<td>打印命令行标记</td>
</tr>
<tr>
<td>-sysprops</td>
<td>打印Java系统属性</td>
</tr>
</tbody>
</table>
</div>
<h2 id="jmap"><a href="#jmap" class="headerlink" title="jmap"></a>jmap</h2><p>下面的工具介绍不再提供例子，读者感兴趣可以自己尝试</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>选 项</th>
<th>作 用</th>
</tr>
</thead>
<tbody>
<tr>
<td>-dump</td>
<td>生成 Java 堆转储快照。格式为：<code>-dump:[live,]format=b,file=&lt;filename&gt;</code>，其中 live 子参数说明是否只 dump 出存活的对象</td>
</tr>
<tr>
<td>-finalizerinfo</td>
<td>显示在 F-Queue 中等待 Finalizer 想成执行 finalize 方法的对象。只在 Linux/Solaris 平台下有效。在JDK9 Mac 平台失败，JDK8 可以</td>
</tr>
<tr>
<td>-heap</td>
<td>显示 Java 堆详细信息，如使用哪种回收器、参数配置、分代状况等。只在 Linux/Solaris 平台下有效。在JDK9 Mac 平台失败，JDK8可以</td>
</tr>
<tr>
<td>-histo</td>
<td>显示堆中对象的统计信息，包括类、实例数量、合计容量</td>
</tr>
<tr>
<td>permstat</td>
<td>以ClassLoader为统计口径显示永久代内存状态，只在 Linux/Solaris 平台下有效。 JDK8以上版本已经废弃</td>
</tr>
<tr>
<td>-F</td>
<td>当虚拟机进程对 -dump 选项没有响应时，可使用这个选项强制生成 dump 快照。只在 Linux/Solaris 平台下有效。</td>
</tr>
</tbody>
</table>
</div>
<h2 id="jhat"><a href="#jhat" class="headerlink" title="jhat"></a>jhat</h2><p>虚拟机堆转储快照分析工具,不建议使用, 建议使用 <code>visualVM</code> 等工具进行分析</p>
<h2 id="jstack"><a href="#jstack" class="headerlink" title="jstack"></a>jstack</h2><p>Java堆栈跟踪工具</p>
<blockquote>
<p>“线程快照就是当前虚拟机内每一条线程正在执行的方法堆栈的集合，生成线程快照的主要目的是定位线程出现长时间停顿的原因，如线程间死锁、死循环、请求外部资源导致的长时间等待等都是导致线程长时间停顿的常见原因。线程出现停顿的时候通过jstack来查看各个线程的调用堆栈，就可以知道没有响应的线程到底在后台做些什么事情，或者等待着什么资源。”</p>
<p>摘录来自: 周志明. “深入理解Java虚拟机：JVM高级特性与最佳实践（第2版）”。 iBooks.</p>
</blockquote>
<div class="table-container">
<table>
<thead>
<tr>
<th>选项</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td>-F</td>
<td>当正常输出的请求不被响应时，强制输出线程堆栈</td>
</tr>
<tr>
<td>-l</td>
<td>除堆栈外，显示关于锁的附加信息</td>
</tr>
<tr>
<td>-m</td>
<td>如果调用到本地方法的话，可以显示C/C++ 的堆栈.(JDK9失败）</td>
</tr>
</tbody>
</table>
</div>
<h1 id="visualVM"><a href="#visualVM" class="headerlink" title="visualVM"></a>visualVM</h1><p>visualVM 通过可视化的方式来和命令行的命令进行互补，来分析一些内存占用的趋势，一些参数的动态变化比命令行工具有着先天的优势，由于官方文档比较全面，不再赘述。</p>
<p>visualVM 官方提供了中文文档：</p>
<ul>
<li>QuickStart：<a href="https://visualvm.github.io/gettingstarted.html?Java_VisualVM">https://visualvm.github.io/gettingstarted.html?Java_VisualVM</a></li>
<li>Document： <a href="https://visualvm.github.io/documentation.html">https://visualvm.github.io/documentation.html</a></li>
<li>GitHub 主页：<a href="https://github.com/oracle/visualvm">https://github.com/oracle/visualvm</a></li>
</ul>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>JVM</tag>
        <tag>tech</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL 中的锁</title>
    <url>/lock-of-mysql.html</url>
    <content><![CDATA[<p>什么是事务？什么是隔离级别？<br>数据库中使用了多少种锁？这些锁之间有什么关系？<br>数据库如何在并发度和各种并发问题中平衡？<br><span id="more"></span></p>
<h1 id="事务与隔离级别"><a href="#事务与隔离级别" class="headerlink" title="事务与隔离级别"></a>事务与隔离级别</h1><h2 id="事务四要素"><a href="#事务四要素" class="headerlink" title="事务四要素"></a>事务四要素</h2><ul>
<li>原子性（Atomicity）：要么全部完成，要么全部不完成；</li>
<li>一致性（Consistency）：一个事务单元需要提交之后才会被其他事务可见；</li>
<li>隔离性（Isolation）：并发事务之间不会互相影响，设立了不同程度的隔离级别，通过适度的破坏一致性，得以提高性能；</li>
<li>持久性（Durability）：事务提交后即持久化到磁盘不会丢失。</li>
</ul>
<h2 id="并发问题名词解释"><a href="#并发问题名词解释" class="headerlink" title="并发问题名词解释"></a>并发问题名词解释</h2><p>以下表为例子<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNbRwgy1fvfvewig2kj30ep04f0t0.jpg" alt="table"></p>
<h3 id="脏读-dirty-read"><a href="#脏读-dirty-read" class="headerlink" title="脏读 dirty read"></a>脏读 dirty read</h3><p>没有提交的事务被其他事务读取到了，这叫做<strong>脏读</strong>。<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNbRwgy1fvfvg5ajgyj30r60gv0u7.jpg" alt="dirty read"><br>事务开始之前两个人各有1000元<br>事务一执行了转账操作，事务二统计两个人一共有多少钱。事务2的到的结果明显是错误的。</p>
<h3 id="不可重复读-unrepeatable-read"><a href="#不可重复读-unrepeatable-read" class="headerlink" title="不可重复读 unrepeatable read"></a>不可重复读 unrepeatable read</h3><p>同一个事务对同一条记录读取两遍，两次读出来的结果不一样，称为<strong>不可重复读</strong><br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNbRwgy1fvfvib1im1j30r60gft9y.jpg" alt="unrepeatabel read"><br>事务2中两次执行同一条SQL得到的结果却不同。不可重复读与脏读的区别在于，不可重复读读到的是其他事务已经提交的修改，而脏读是读到了其他事务还未提交的修改。</p>
<h3 id="幻读-phantom-read"><a href="#幻读-phantom-read" class="headerlink" title="幻读 phantom read"></a>幻读 phantom read</h3><p>同样的条件，第一次和第二次读出来的记录数不一样，称为<strong>幻读</strong><br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNbRwgy1fvg0m43z6oj30r60fwwfo.jpg" alt="phantom read"><br>同样的条件，第一次和第二次读出来的记录集合不一样。不可重复读是因为其他事务进行了 UPDATE 操作，幻读是因为其他事务进行了 INSERT 或者 DELETE 操作。<br>事务 2 的两次查询，第一次查出 2 条记录，第二次却查出 3 条记录，多出来的这条记录，正如 phantom（幽灵，幻影，错觉） 的意思，就像幽灵一样。</p>
<h3 id="丢失更新-lost-update"><a href="#丢失更新-lost-update" class="headerlink" title="丢失更新 lost update"></a>丢失更新 lost update</h3><p>两个事务都是写操作，某种情况下有些修改被提交后又被覆盖了，称为<strong>丢失更新</strong><br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNbRwgy1fvg0h3ckcrj30r60buabh.jpg" alt="lost update"><br>上图中事务2的提交覆盖了事务1的提交。<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNbRwgy1fvg0k0v4vbj30r60aqq45.jpg" alt="rollback"><br>上图中事务1的回滚直接忽视了事务2的 UPDATE 操作</p>
<h2 id="隔离级别"><a href="#隔离级别" class="headerlink" title="隔离级别"></a>隔离级别</h2><p>为了有效的保证数据一致性和数据库并发性能，便有了四种不同的数据库隔离级别</p>
<ul>
<li>读未提交（Read Uncommitted）：可以读取未提交的记录，会出现脏读，幻读，不可重复读，所有并发问题都可能遇到；</li>
<li>读已提交（Read Committed）：事务中只能看到已提交的修改，不会出现脏读现象，但是会出现幻读，不可重复读；（大多数数据库的默认隔离级别都是 RC，但是 MySQL InnoDb 默认是 RR）</li>
<li>可重复读（Repeatable Read）：MySQL InnoDb 默认的隔离级别，解决了不可重复读问题，但是任然存在幻读问题；（MySQL 的实现有差异，后面介绍）</li>
<li>序列化（Serializable）：最高隔离级别，啥并发问题都没有。</li>
</ul>
<p>针对这四种隔离级别，应该根据具体的业务来取舍，如果某个系统的业务里根本就不会出现重复读的场景，完全可以将数据库的隔离级别设置为 RC，这样可以最大程度的提高数据库的并发性。不同的隔离级别和可能发生的并发现象如下表：<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNbRwgy1fvg0rg5l09j30oh05h0tz.jpg" alt=""></p>
<blockquote>
<p>RR级别下会出现提交覆盖吗？答案文末揭晓。</p>
</blockquote>
<h2 id="隔离级别的实现"><a href="#隔离级别的实现" class="headerlink" title="隔离级别的实现"></a>隔离级别的实现</h2><p>上面所说的都是事务和隔离级别的概念，是 SQL 标准中通用的概念，不同的数据库产品有不同的实现。</p>
<h3 id="传统的隔离级别"><a href="#传统的隔离级别" class="headerlink" title="传统的隔离级别"></a>传统的隔离级别</h3><p>传统的隔离级别是基于锁实现的，这种方式叫做 基于锁的并发控制（Lock-Based Concurrent Control，简写 LBCC）<br>通过对读写操作加不同的锁，以及对释放锁的时机进行不同的控制，就可以实现四种隔离级别。传统的锁有两种：读操作通常加共享锁（Share locks，S锁，又叫读锁），写操作加排它锁（Exclusive locks，X锁，又叫写锁）；加了共享锁的记录，其他事务也可以读，但不能写；加了排它锁的记录，其他事务既不能读，也不能写。另外，对于锁的粒度，又分为行锁和表锁，行锁只锁某行记录，对其他行的操作不受影响，表锁会锁住整张表，所有对这个表的操作都受影响。</p>
<p>归纳起来，四种隔离级别的加锁策略如下：</p>
<ul>
<li>读未提交（Read Uncommitted）：事务读不阻塞其他事务读和写，事务写阻塞其他事务写但不阻塞读；通过对写操作加 “持续X锁”，对读操作不加锁 实现；</li>
<li>读已提交（Read Committed）：事务读不会阻塞其他事务读和写，事务写会阻塞其他事务读和写；通过对写操作加 “持续X锁”，对读操作加 “临时S锁” 实现；不会出现脏读；</li>
<li>可重复读（Repeatable Read）：事务读会阻塞其他事务事务写但不阻塞读，事务写会阻塞其他事务读和写；通过对写操作加 “持续X锁”，对读操作加 “持续S锁” 实现；</li>
<li>序列化（Serializable）：为了解决幻读问题，行级锁做不到，需使用表级锁。</li>
</ul>
<p>通过对<strong>锁的类型（读锁还是写锁），锁的粒度（行锁还是表锁），持有锁的时间（临时锁还是持续锁）</strong>合理的进行组合，就可以实现四种不同的隔离级别。这四种不同的加锁策略实际上又称为 <strong>封锁协议（Locking Protocol）</strong>，所谓协议，就是说不论加锁还是释放锁都得按照特定的规则来。读未提交 的加锁策略又称为 <strong>一级封锁协议</strong>，后面的分别是二级，三级，序列化 的加锁策略又称为 <strong>四级封锁协议</strong>。</p>
<p>其中三级封锁协议在事务的过程中为写操作加持续 X 锁，为读操作加持续 S 锁，并且在事务结束时才对锁进行释放，像这种加锁和解锁明确的分成两个阶段我们把它称作 <strong>两段锁协议（2-phase locking，简称 2PL）</strong>。在两段锁协议中规定，加锁阶段只允许加锁，不允许解锁；而解锁阶段只允许解锁，不允许加锁。这种方式虽然无法避免死锁，但是两段锁协议可以保证事务的并发调度是串行化的（关于串行化是一个非常重要的概念，尤其是在数据恢复和备份的时候）。在两段锁协议中，还有一种特殊的形式，叫 <strong>一次封锁</strong>，意思是指在事务开始的时候，将事务可能遇到的数据全部一次锁住，再在事务结束时全部一次释放，这种方式可以有效的避免死锁发生。</p>
<h3 id="MySQL的隔离级别"><a href="#MySQL的隔离级别" class="headerlink" title="MySQL的隔离级别"></a>MySQL的隔离级别</h3><p>虽然数据库的四种隔离级别通过 LBCC 技术都可以实现，但是它最大的问题是它只实现了并发的读读，对于并发的读写还是冲突的，写时不能读，读时不能写，当读写操作都很频繁时，数据库的并发性将大大降低，针对这种场景，MVCC 技术应运而生。MVCC 的全称叫做 <strong>Multi-Version Concurrent Control（多版本并发控制）</strong>。<br>InnoDb 会为每一行记录增加几个隐含的“辅助字段”，（实际上是 3 个字段：一个隐式的 ID 字段，一个事务 ID，还有一个回滚指针），事务在写一条记录时会将其拷贝一份生成这条记录的一个原始拷贝，写操作同样还是会对原记录加锁，但是读操作会读取未加锁的新记录，这就保证了读写并行。要注意的是，生成的新版本其实就是 undo log，它也是实现事务回滚的关键技术。<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNbRwgy1fvg3oyq9u1j30fm080dgk.jpg" alt=""></p>
<p>在 read uncommit 隔离级别下，每次都是读取最新版本的数据行，所以不能用 MVCC 的多版本，而 serializable 隔离级别每次读取操作都会为记录加上读锁，也和 MVCC 不兼容，所以只有 RC 和 RR 这两个隔离级别才有 MVCC。</p>
<p>RR 和 RC 隔离级别都实现了 MVCC 来满足读写并行，但是读的实现方式是不一样的：RC 总是读取记录的最新版本，如果该记录被锁住，则读取该记录最新的一次快照，而 RR 是读取该记录事务开始时的那个版本。虽然这两种读取方式不一样，但是它们读取的都是快照数据，并不会被写操作阻塞，所以这种读操作称为 <strong>快照读（Snapshot Read）</strong>，有时候也叫做 <strong>非阻塞读（Nonlocking Read）</strong></p>
<p>除了 快照读 ，MySQL 还提供了另一种读取方式：当前读（Current Read），有时候又叫做 加锁读（Locking Read） 或者 阻塞读（Blocking Read），这种读操作读的不再是数据的快照版本，而是数据的最新版本，并会对数据加锁，根据加锁的不同，又分成两类：</p>
<ul>
<li>SELECT … LOCK IN SHARE MODE：加 S 锁</li>
<li>SELECT … FOR UPDATE：加 X 锁</li>
<li>INSERT / UPDATE / DELETE：加 X 锁</li>
</ul>
<p>当前读在 RR 和 RC 两种隔离级别下的实现也是不一样的：RC 只加记录锁，RR 除了加记录锁，还会加间隙锁，用于解决幻读问题。</p>
<p>不同隔离级别下InnoDB的读操作<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNbRwgy1fvg3stivqyj30ow06rq4o.jpg" alt=""></p>
<h2 id="答案"><a href="#答案" class="headerlink" title="答案"></a>答案</h2><p>MySQL 的实现和 ANSI-SQL 标准之间的差异，在标准的传统实现中，RR 隔离级别是使用持续的 X 锁和持续的 S 锁来实现的（参看下面的 “隔离级别的实现” 一节），由于是持续的 S 锁，所以避免了其他事务有写操作，也就不存在提交覆盖问题。但是 MySQL 在 RR 隔离级别下，普通的 SELECT 语句只是快照读，没有任何的加锁，和标准的 RR 是不一样的。如果要让 MySQL 在 RR 隔离级别下不发生提交覆盖，可以使用 SELECT … LOCK IN SHARE MODE 或者 SELECT … FOR UPDATE 。</p>
<h1 id="常见锁类型"><a href="#常见锁类型" class="headerlink" title="常见锁类型"></a>常见锁类型</h1><h2 id="表锁-amp-行锁"><a href="#表锁-amp-行锁" class="headerlink" title="表锁 &amp; 行锁"></a>表锁 &amp; 行锁</h2><p>在 MySQL 中锁的种类有很多，但是最基本的还是表锁和行锁：表锁指的是对一整张表加锁，一般是 DDL 处理时使用，也可以自己在 SQL 中指定；而行锁指的是锁定某一行数据或某几行，或行和行之间的间隙。行锁的加锁方法比较复杂，但是由于只锁住有限的数据，对于其它数据不加限制，所以并发能力强，通常都是用行锁来处理并发事务。表锁由 MySQL 服务器实现，行锁由存储引擎实现，常见的就是 InnoDb，所以通常我们在讨论行锁时，隐含的一层意义就是数据库的存储引擎为 InnoDb ，而 MyISAM 存储引擎只能使用表锁。</p>
<h3 id="表锁"><a href="#表锁" class="headerlink" title="表锁"></a>表锁</h3><p>表锁由 MySQL 服务器实现，所以无论你的存储引擎是什么，都可以使用。一般在执行 DDL 语句时，譬如 ALTER TABLE 就会对整个表进行加锁。在执行 SQL 语句时，也可以明确对某个表加锁，譬如下面的例子<br><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">mysql&gt;</span><span class="bash"> lock table products <span class="built_in">read</span>;</span></span><br><span class="line">Query OK, 0 rows affected (0.00 sec)</span><br><span class="line"></span><br><span class="line"><span class="meta">mysql&gt;</span><span class="bash"> select * from products <span class="built_in">where</span> id = 100;</span></span><br><span class="line"></span><br><span class="line"><span class="meta">mysql&gt;</span><span class="bash"> unlock tables;</span></span><br><span class="line">Query OK, 0 rows affected (0.00 sec)</span><br></pre></td></tr></table></figure><br>上面的 SQL 首先对 products 表加一个表锁，然后执行查询语句，最后释放表锁。<br>表锁可以细分成两种：读锁和写锁，如果是加写锁，则是 <code>lock table products write</code> 。</p>
<p>关于表锁，我们要了解它的加锁和解锁原则，要注意的是它使用的是 <strong>一次封锁</strong> 技术，也就是说，我们会在会话开始的地方使用 lock 命令将后面所有要用到的表加上锁，在锁释放之前，我们只能访问这些加锁的表，不能访问其他的表，最后通过 unlock tables 释放所有表锁。这样的好处是，不会发生死锁！所以我们在 MyISAM 存储引擎中，是不可能看到死锁场景的。对多个表加锁的例子如下：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">mysql&gt;</span><span class="bash"> lock table products <span class="built_in">read</span>, orders <span class="built_in">read</span>;</span></span><br><span class="line">Query OK, 0 rows affected (0.00 sec)</span><br><span class="line"></span><br><span class="line"><span class="meta">mysql&gt;</span><span class="bash"> select * from products <span class="built_in">where</span> id = 100;</span></span><br><span class="line"></span><br><span class="line"><span class="meta">mysql&gt;</span><span class="bash"> select * from orders <span class="built_in">where</span> id = 200;</span></span><br><span class="line"></span><br><span class="line"><span class="meta">mysql&gt;</span><span class="bash"> select * from users <span class="built_in">where</span> id = 300;</span></span><br><span class="line">ERROR 1100 (HY000): Table &#x27;users&#x27; was not locked with LOCK TABLES</span><br><span class="line"></span><br><span class="line"><span class="meta">mysql&gt;</span><span class="bash"> update orders <span class="built_in">set</span> price = 5000 <span class="built_in">where</span> id = 200;</span></span><br><span class="line">ERROR 1099 (HY000): Table &#x27;orders&#x27; was locked with a READ lock and can&#x27;t be updated</span><br><span class="line"></span><br><span class="line"><span class="meta">mysql&gt;</span><span class="bash"> unlock tables;</span></span><br><span class="line">Query OK, 0 rows affected (0.00 sec)</span><br></pre></td></tr></table></figure>
<p>可以看到由于没有对 users 表加锁，在持有表锁的情况下是不能读取的，另外，由于加的是读锁，所以后面也不能对 orders 表进行更新。MySQL 表锁的加锁规则如下：</p>
<ul>
<li>对于读锁<ul>
<li>持有读锁的会话可以读表，但不能写表；</li>
<li>允许多个会话同时持有读锁；</li>
<li>其他会话就算没有给表加读锁，也是可以读表的，但是不能写表；</li>
<li>其他会话申请该表写锁时会阻塞，直到锁释放。</li>
</ul>
</li>
<li>对于写锁<ul>
<li>持有写锁的会话既可以读表，也可以写表；</li>
<li>只有持有写锁的会话才可以访问该表，其他会话访问该表会被阻塞，直到锁释放；</li>
<li>其他会话无论申请该表的读锁或写锁，都会阻塞，直到锁释放。</li>
</ul>
</li>
</ul>
<p>锁的释放规则如下：</p>
<ul>
<li>使用 UNLOCK TABLES 语句可以显示释放表锁；</li>
<li>如果会话在持有表锁的情况下执行 LOCK TABLES 语句，将会释放该会话之前持有的锁；</li>
<li>如果会话在持有表锁的情况下执行 START TRANSACTION 或 BEGIN 开启一个事务，将会释放该会话之前持有的锁；</li>
<li>如果会话连接断开，将会释放该会话所有的锁。</li>
</ul>
<h3 id="行锁"><a href="#行锁" class="headerlink" title="行锁"></a>行锁</h3><p>表锁不仅实现和使用都很简单，而且占用的系统资源少，所以在很多存储引擎中使用，如 MyISAM、MEMORY、MERGE 等，MyISAM 存储引擎几乎完全依赖 MySQL 服务器提供的表锁机制，查询自动加表级读锁，更新自动加表级写锁，以此来解决可能的并发问题。但是表锁的粒度太粗，导致数据库的并发性能降低，为了提高数据库的并发能力，InnoDb 引入了行锁的概念。行锁和表锁对比如下：</p>
<ul>
<li>表锁：开销小，加锁快；不会出现死锁；锁定粒度大，发生锁冲突的概率最高，并发度最低；</li>
<li>行锁：开销大，加锁慢；会出现死锁；锁定粒度最小，发生锁冲突的概率最低，并发度也最高。</li>
</ul>
<p>行锁和表锁一样，也分成两种类型：读锁和写锁。常见的增删改（INSERT、DELETE、UPDATE）语句会自动对操作的数据行加写锁，查询的时候也可以明确指定锁的类型，SELECT … LOCK IN SHARE MODE 语句加的是读锁，SELECT … FOR UPDATE 语句加的是写锁。</p>
<p>行锁这个名字听起来像是这个锁加在某个数据行上，实际上这里要指出的是：在 MySQL 中，行锁是加在索引上的。</p>
<h4 id="MySQL-加锁流程"><a href="#MySQL-加锁流程" class="headerlink" title="MySQL 加锁流程"></a>MySQL 加锁流程</h4><p>当执行下面的 SQL 时（id 为 students 表的主键），我们要知道，InnoDb 存储引擎会在 id = 49 这个主键索引上加一把 X 锁。<br><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">mysql&gt;</span><span class="bash"> update students <span class="built_in">set</span> score = 100 <span class="built_in">where</span> id = 49;</span></span><br></pre></td></tr></table></figure><br>当执行下面的 SQL 时（name 为 students 表的二级索引），InnoDb 存储引擎会在 name = ‘Tom’ 这个索引上加一把 X 锁，同时会通过 name = ‘Tom’ 这个二级索引定位到 id = 49 这个主键索引，并在 id = 49 这个主键索引上加一把 X 锁。<br><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">mysql&gt;</span><span class="bash"> update students <span class="built_in">set</span> score = 100 <span class="built_in">where</span> name = <span class="string">&#x27;Tom&#x27;</span>;</span></span><br></pre></td></tr></table></figure><br>加锁过程如下图所示：<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNbRwgy1fvg7zp5m7uj30ir0f7jsk.jpg" alt=""></p>
<p>多条记录的加锁流程：<br><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">mysql&gt;</span><span class="bash"> update students <span class="built_in">set</span> level = 3 <span class="built_in">where</span> score &gt;= 60;</span></span><br></pre></td></tr></table></figure><br>下图展示了当用户执行这条 SQL 时，MySQL Server 和 InnoDb 之间的执行流程：<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNbRwgy1fvg7yyf7rhj30i50dp0tl.jpg" alt=""><br>MySQL 在操作多条记录时 InnoDB 与 MySQL Server 的交互是一条一条进行的，加锁也是一条一条依次进行的，先对一条满足条件的记录加锁，返回给 MySQL Server，做一些 DML 操作，然后在读取下一条加锁，直至读取完毕。</p>
<h4 id="行锁种类"><a href="#行锁种类" class="headerlink" title="行锁种类"></a>行锁种类</h4><p>根据锁的粒度可以把锁细分为表锁和行锁，行锁根据场景的不同又可以进一步细分，在 MySQL 的源码里，定义了四种类型的行锁，如下：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LOCK_TABLE  16  <span class="comment">/* table lock */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LOCK_REC    32  <span class="comment">/* record lock */</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* Precise modes */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LOCK_ORDINARY   0   </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LOCK_GAP    512</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LOCK_REC_NOT_GAP 1024   </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LOCK_INSERT_INTENTION 2048</span></span><br></pre></td></tr></table></figure></p>
<ul>
<li>LOCK_ORDINARY：也称为 Next-Key Lock，锁一条记录及其之前的间隙，这是 RR 隔离级别用的最多的锁，从名字也能看出来；</li>
<li>LOCK_GAP：间隙锁，锁两个记录之间的 GAP，防止记录插入；</li>
<li>LOCK_REC_NOT_GAP：只锁记录；</li>
<li>LOCK_INSERT_INTENSION：插入意向 GAP 锁，插入记录时使用，是 LOCK_GAP 的一种特例。</li>
</ul>
<h2 id="读锁-amp-写锁"><a href="#读锁-amp-写锁" class="headerlink" title="读锁 &amp; 写锁"></a>读锁 &amp; 写锁</h2><p>MySQL 将锁分成两类：锁类型（lock_type）和锁模式（lock_mode）。锁类型就是上文中介绍的表锁和行锁两种类型，当然行锁还可以细分成记录锁和间隙锁等更细的类型，锁类型描述的锁的粒度，也可以说是把锁具体加在什么地方；而锁模式描述的是到底加的是什么锁，譬如读锁或写锁。锁模式通常是和锁类型结合使用的，锁模式在 MySQL 的源码中定义如下：<br><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* Basic lock modes */</span></span><br><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">lock_mode</span> &#123;</span></span><br><span class="line">    LOCK_IS = <span class="number">0</span>, <span class="comment">/* intention shared */</span></span><br><span class="line">    LOCK_IX,    <span class="comment">/* intention exclusive */</span></span><br><span class="line">    LOCK_S,     <span class="comment">/* shared */</span></span><br><span class="line">    LOCK_X,     <span class="comment">/* exclusive */</span></span><br><span class="line">    LOCK_AUTO_INC,  <span class="comment">/* locks the auto-inc counter of a table in an exclusive mode*/</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<ul>
<li>LOCK_IS：读意向锁；</li>
<li>LOCK_IX：写意向锁；</li>
<li>LOCK_S：读锁；</li>
<li>LOCK_X：写锁；</li>
<li>LOCK_AUTO_INC：自增锁；</li>
</ul>
<p>将锁分为读锁和写锁主要是为了提高读的并发，如果不区分读写锁，那么数据库将没办法并发读，并发性将大大降低。而 IS（读意向）、IX（写意向）只会应用在表锁上，方便表锁和行锁之间的冲突检测。LOCK_AUTO_INC 是一种特殊的表锁。下面依次进行介绍。</p>
<h3 id="读写锁"><a href="#读写锁" class="headerlink" title="读写锁"></a>读写锁</h3><p>读锁和写锁都是最基本的锁模式，它们的概念也比较容易理解。读锁，又称共享锁（Share locks，简称 S 锁），加了读锁的记录，所有的事务都可以读取，但是不能修改，并且可同时有多个事务对记录加读锁。写锁，又称排他锁（Exclusive locks，简称 X 锁），或独占锁，对记录加了排他锁之后，只有拥有该锁的事务可以读取和修改，其他事务都不可以读取和修改，并且同一时间只能有一个事务加写锁。（注意：这里说的读都是当前读，快照读是无需加锁的，记录上无论有没有锁，都可以快照读）</p>
<h3 id="读写意向锁"><a href="#读写意向锁" class="headerlink" title="读写意向锁"></a>读写意向锁</h3><p>表锁锁定了整张表，而行锁是锁定表中的某条记录，它们俩锁定的范围有交集，因此表锁和行锁之间是有冲突的。譬如某个表有 10000 条记录，其中有一条记录加了 X 锁，如果这个时候系统需要对该表加表锁，为了判断是否能加这个表锁，系统需要遍历表中的所有 10000 条记录，看看是不是某条记录被加锁，如果有锁，则不允许加表锁，显然这是很低效的一种方法，为了方便检测表锁和行锁的冲突，从而引入了意向锁。</p>
<p>意向锁为表级锁，也可分为读意向锁（IS 锁）和写意向锁（IX 锁）。当事务试图读或写某一条记录时，会先在表上加上意向锁，然后才在要操作的记录上加上读锁或写锁。这样判断表中是否有记录加锁就很简单了，只要看下表上是否有意向锁就行了。意向锁之间是不会产生冲突的，也不和 AUTO_INC 表锁冲突，它只会阻塞表级读锁或表级写锁，另外，意向锁也不会和行锁冲突，行锁只会和行锁冲突。</p>
<p>下面是各个表锁之间的兼容矩阵：<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNbRwgy1fvg896mbu6j30l206q3z4.jpg" alt=""></p>
<h3 id="AUTO-INC-锁"><a href="#AUTO-INC-锁" class="headerlink" title="AUTO_INC 锁"></a>AUTO_INC 锁</h3><p>AUTO_INC 锁又叫自增锁（一般简写成 AI 锁），它是一种特殊类型的表锁，当插入的表中有自增列（AUTO_INCREMENT）的时候可能会遇到。当插入表中有自增列时，数据库需要自动生成自增值，在生成之前，它会先为该表加 AUTO_INC 表锁，其他事务的插入操作阻塞，这样保证生成的自增值肯定是唯一的。AUTO_INC 锁具有如下特点：</p>
<ul>
<li>AUTO_INC 锁互不兼容，也就是说同一张表同时只允许有一个自增锁；</li>
<li>自增锁不遵循二段锁协议，它并不是事务结束时释放，而是在 INSERT 语句执行结束时释放，这样可以提高并发插入的性能。</li>
<li>自增值一旦分配了就会 +1，如果事务回滚，自增值也不会减回去，所以自增值可能会出现中断的情况。</li>
</ul>
<p>显然，AUTO_INC 表锁会导致并发插入的效率降低，为了提高插入的并发性，MySQL 从 5.1.22 版本开始，引入了一种可选的轻量级锁（mutex）机制来代替 AUTO_INC 锁，我们可以通过参数 innodb_autoinc_lock_mode 控制分配自增值时的并发策略。参数 innodb_autoinc_lock_mode 可以取下列值：</p>
<ul>
<li><p>innodb_autoinc_lock_mode = 0 （traditional lock mode）</p>
<ul>
<li>使用传统的 AUTO_INC 表锁，并发性比较差。</li>
</ul>
</li>
<li><p>innodb_autoinc_lock_mode = 1 （consecutive lock mode）</p>
<ul>
<li>MySQL 默认采用这种方式，是一种比较折中的方法。</li>
<li>MySQL 将插入语句分成三类：<code>Simple inserts</code>、<code>Bulk inserts</code>、<code>Mixed-mode inserts</code>。通过分析 INSERT 语句可以明确知道插入数量的叫做 <code>Simple inserts</code>，譬如最经常使用的 INSERT INTO table VALUE(1,2) 或 INSERT INTO table VALUES(1,2), (3,4)；通过分析 INSERT 语句无法确定插入数量的叫做 Bulk inserts，譬如 INSERT INTO table SELECT 或 LOAD DATA 等；还有一种是不确定是否需要分配自增值的，譬如 INSERT INTO table VALUES(1,’a’), (NULL,’b’), (5, ‘C’), (NULL, ‘d’) 或 INSERT … ON DUPLICATE KEY UPDATE，这种叫做 <code>Mixed-mode inserts</code>。</li>
<li>Bulk inserts 不能确定插入数使用表锁；Simple inserts 和 Mixed-mode inserts 使用轻量级锁 mutex，只锁住预分配自增值的过程，不锁整张表。Mixed-mode inserts 会直接分析语句，获得最坏情况下需要插入的数量，一次性分配足够的自增值，缺点是会分配过多，导致浪费和空洞。</li>
<li>这种模式的好处是既平衡了并发性，又能保证同一条 INSERT 语句分配的自增值是连续的。</li>
</ul>
</li>
<li><p>innodb_autoinc_lock_mode = 2 （interleaved lock mode）</p>
<ul>
<li>全部都用轻量级锁 mutex，并发性能最高，按顺序依次分配自增值，不会预分配。</li>
<li>缺点是不能保证同一条 INSERT 语句内的自增值是连续的，这样在复制（replication）时，如果 binlog_format 为 statement-based（基于语句的复制）就会存在问题，因为是来一个分配一个，同一条 INSERT 语句内获得的自增值可能不连续，主从数据集会出现数据不一致。所以在做数据库同步时要特别注意这个配置。</li>
</ul>
</li>
</ul>
<h2 id="细说行锁"><a href="#细说行锁" class="headerlink" title="细说行锁"></a>细说行锁</h2><p>前面在讲行锁时有提到，在 MySQL 的源码中定义了四种类型的行锁，我们这一节将学习这四种锁。</p>
<h3 id="记录锁-Record-Locks"><a href="#记录锁-Record-Locks" class="headerlink" title="记录锁 Record Locks"></a>记录锁 Record Locks</h3><p>记录锁 是最简单的行锁。<br><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">mysql&gt;</span><span class="bash"> UPDATE accounts SET level = 100 WHERE id = 5;</span></span><br></pre></td></tr></table></figure><br>这条 SQL 语句就会在 id = 5 这条记录上加上记录锁，防止其他事务对 id = 5 这条记录进行修改或删除。记录锁永远都是加在索引上的，就算一个表没有建索引，数据库也会隐式的创建一个索引。如果 WHERE 条件中指定的列是个二级索引，那么记录锁不仅会加在这个二级索引上，还会加在这个二级索引所对应的聚簇索引上（参考上面的加锁流程一节）。</p>
<p>注意，如果 SQL 语句无法使用索引时会走主索引实现全表扫描，这个时候 MySQL 会给整张表的所有数据行加记录锁。如果一个 WHERE 条件无法通过索引快速过滤，存储引擎层面就会将所有记录加锁后返回，再由 MySQL Server 层进行过滤。不过在实际使用过程中，MySQL 做了一些改进，在 MySQL Server 层进行过滤的时候，如果发现不满足，会调用 unlock_row 方法，把不满足条件的记录释放锁（显然这违背了两段锁协议）。这样做，保证了最后只会持有满足条件记录上的锁，但是每条记录的加锁操作还是不能省略的。可见在没有索引时，不仅会消耗大量的锁资源，增加数据库的开销，而且极大的降低了数据库的并发性能，所以说，更新操作一定要记得走索引。</p>
<h3 id="间隙锁-Gap-Locks"><a href="#间隙锁-Gap-Locks" class="headerlink" title="间隙锁 Gap Locks"></a>间隙锁 Gap Locks</h3><p>还是看上面的那个例子，如果 id = 5 这条记录不存在，这个 SQL 语句还会加锁吗？<br>这个 SQL 语句在 RC 隔离级别不会加任何锁，在 RR 隔离级别为了避免幻读会在 id = 5 前后两个索引之间加上间隙锁。<br>间隙锁和间隙锁之间是互不冲突的，间隙锁唯一的作用就是为了防止其他事务的插入。</p>
<h3 id="Next-Key-Locks"><a href="#Next-Key-Locks" class="headerlink" title="Next-Key Locks"></a>Next-Key Locks</h3><p>Next-key 锁 是记录锁和间隙锁的组合，它指的是加在某条记录以及这条记录前面间隙上的锁。假设一个索引包含10、11、13 和 20 这几个值，可能的 Next-key 锁如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(-∞, 10]</span><br><span class="line">(10, 11]</span><br><span class="line">(11, 13]</span><br><span class="line">(13, 20]</span><br><span class="line">(20, +∞)</span><br></pre></td></tr></table></figure>
<p>通常我们都用这种左开右闭区间来表示 Next-key 锁，前面四个都是 Next-key 锁，最后一个为间隙锁。<br>和间隙锁一样，在 RC 隔离级别下没有 Next-key 锁，只有 RR 隔离级别才有。继续拿上面的 SQL 例子来说，如果 id 不是主键，而是二级索引，且不是唯一索引，那么这个 SQL 在 RR 隔离级别下会加什么锁呢？答案就是 Next-key 锁，如下：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(a, 5]</span><br><span class="line">(5, b)</span><br></pre></td></tr></table></figure><br>其中，a 和 b 是 id = 5 前后两个索引，我们假设 a = 1、b = 10，那么此时如果插入一条 id = 3 的记录将会阻塞住。之所以要把 id = 5 前后的间隙都锁住，仍然是为了解决幻读问题，因为 id 是非唯一索引，所以 id = 5 可能会有多条记录，为了防止再插入一条 id = 5 的记录，必须将下面标记 <code>^</code> 的位置都锁住，因为这些位置都可能再插入一条 id = 5 的记录：<br><code>1 ^ 5 ^ 5 ^ 5 ^ 10 11 13 15</code><br>可以看出来，Next-key 锁确实可以避免幻读，但是带来的副作用是连插入 id = 3 这样的记录也被阻塞了。</p>
<p>关于 Next-key 锁，有一个比较有意思的问题，比如下面这个 orders 表（id 为主键，order_id 为二级非唯一索引）：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">+-----+----------+</span><br><span class="line">|  id | order_id |</span><br><span class="line">+-----+----------+</span><br><span class="line">|   1 |        1 |</span><br><span class="line">|   3 |        2 |</span><br><span class="line">|   5 |        5 |</span><br><span class="line">|   7 |        5 |</span><br><span class="line">|  10 |        9 |</span><br><span class="line">+-----+----------+</span><br></pre></td></tr></table></figure>
<p>这个时候不仅 order_id = 5 这条记录会加上 X 记录锁，而且这条记录前后的间隙也会加上锁，加锁位置如下：<br><code>1 2 ^ 5 ^ 5 ^ 9</code><br>可以看到 (2, 9) 这个区间都被锁住了，这个时候如果插入 order_id = 4 或者 order_id = 8 这样的记录肯定会被阻塞，这没什么问题，那么现在问题来了，如果插入一条记录 order_id = 2 或者 order_id = 9 会被阻塞吗？答案是可能阻塞，也可能不阻塞，这取决于插入记录主键的值，感兴趣的可以参考<a href="http://blog.sina.com.cn/s/blog_a1e9c7910102vnrj.html">这篇博客</a>。</p>
<h3 id="插入意向锁-Insert-Intention-Locks"><a href="#插入意向锁-Insert-Intention-Locks" class="headerlink" title="插入意向锁 Insert Intention Locks"></a>插入意向锁 Insert Intention Locks</h3><p><a href="https://dev.mysql.com/doc/refman/5.7/en/innodb-locking.html#innodb-insert-intention-locks">插入意向锁</a> 是一种特殊的间隙锁（所以有的地方把它简写成 II GAP），这个锁表示插入的意向，只有在 INSERT 的时候才会有这个锁。注意，这个锁虽然也叫意向锁，但是和上面介绍的表级意向锁是两个完全不同的概念，不要搞混淆了。插入意向锁和插入意向锁之间互不冲突，所以可以在同一个间隙中有多个事务同时插入不同索引的记录。譬如在上面的例子中，id = 1 和 id = 5 之间如果有两个事务要同时分别插入 id = 2 和 id = 3 是没问题的，虽然两个事务都会在 id = 1 和 id = 5 之间加上插入意向锁，但是不会冲突。</p>
<p>插入意向锁只会和间隙锁或 Next-key 锁冲突，正如上面所说，间隙锁唯一的作用就是防止其他事务插入记录造成幻读，那么间隙锁是如何防止幻读的呢？正是由于在执行 INSERT 语句时需要加插入意向锁，而插入意向锁和间隙锁冲突，从而阻止了插入操作的执行。</p>
<h3 id="行锁的兼容矩阵"><a href="#行锁的兼容矩阵" class="headerlink" title="行锁的兼容矩阵"></a>行锁的兼容矩阵</h3><p>下面我们对这四种行锁做一个总结，它们之间的兼容矩阵如下图所示：<br><img src="https://image-hosting-1251780645.cos.ap-beijing.myqcloud.com/fromSina/006tNbRwgy1fvg952ozxrj30oo05r3z8.jpg" alt=""><br>其中，<strong>行表示已有的锁，列表示要加的锁。</strong></p>
<h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><ol>
<li><a href="http://www.aneasystone.com/archives/2017/10/solving-dead-locks-one.html">学习事务与隔离级别</a></li>
<li><a href="http://www.aneasystone.com/archives/2017/11/solving-dead-locks-two.html">了解常见锁类型</a></li>
<li><a href="http://www.imooc.com/article/17290">MySQL数据库事务各隔离级别加锁情况—read committed &amp;&amp; MVCC</a></li>
</ol>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>tech</tag>
        <tag>SQL</tag>
      </tags>
  </entry>
  <entry>
    <title>【译】Python Curses 编程</title>
    <url>/curses-programming-with-python.html</url>
    <content><![CDATA[<p>本文介绍如何使用 <a href="https://docs.python.org/3.6/library/curses.html#module-curses">curses</a> 扩展去控制命令行模式的显示。</p>
<blockquote>
<p>原文链接：<a href="https://docs.python.org/3.6/howto/curses.html">https://docs.python.org/3.6/howto/curses.html</a><br>原文作者: A.M. Kuchling, Eric S. Raymond<br>版本：Release-2.04</p>
</blockquote>
<span id="more"></span>
<h2 id="什么是-curses"><a href="#什么是-curses" class="headerlink" title="什么是 curses?"></a>什么是 curses?</h2><p>curses 是为文本终端提供一个界面绘图和键盘输入响应的库。这些终端包括 VT100s、Linux终端，和不同程序提供的模拟终端。终端支持使用不同的控制代码来实现相同的操作，类似移动光标、滚动屏幕、擦除屏幕区域等。不同的终端的控制代码大部分都不相同，并且有着自己的特殊操作习惯和技巧。</p>
<p>也许有人会问，在这个图形化显示的时代，为什么还需要这种终端操作库，基于字符的终端显示确实是一个过时的技术，但是在很多有价值的场景下还是能够使用终端显示做出一些十分迷人的产物。其中一个场景就是在并不具备图形显示的便携式和嵌入式 Linux 中，另外还有就是在安装系统或者配置内核的时候，这些操作都不得不在图像界面启动之前操作。</p>
<p>Curses 提供的基本功能，是为程序员提供一个不重复的文本窗口。窗口的内容可以通过不同的方式改变：添加文本、删除文本、修改外观等等。curses 会屏蔽掉底层终端命令的差异性，计算出你需要执行的命令。curses 并不提供类似按钮、复选框，或者对话这种用户界面。如果你需要这些元素可以使用一些类似 <a href="https://pypi.python.org/pypi/urwid/">Urwid</a> 的用户界面库。</p>
<p>Curses 一开始是为了 BSD Unix写的；后来的 AT&amp;T 的 System V 版本对原有功能做了增强，同时添加了许多新的功能。 BSD curses 就不再维护了，而是被 ncurses 替代了。ncurses 是一个 AT&amp;T 接口的一个开源实现。如果你正在使用一个开源的 Unix，类似Linux或者FreeBSD，你的系统应该已经包含了ncurses。因为现在大多数的商业 Unix 版本都是基于 System V 的代码开发的，这里描述的功能理论上也会存在于上述 Unix 版本中。尽管如此，一些老版本 unix 对 curses 可能并不会有很好的支持。</p>
<p>Windows 版本的 Python 并不包含 curses 模块。而是有一个类似的替代版本 <a href="https://pypi.python.org/pypi/UniCurses">UniCurses</a>。你也可以尝试使用 Fredrik Lundh 写的<a href="http://effbot.org/zone/console-index.htm">the Console module</a>，虽然和 curses 的 API 不一样，但是也可以提供基于光标的输出，并且为鼠标和键盘提供全方位支持。</p>
<h2 id="Python-curses-模块"><a href="#Python-curses-模块" class="headerlink" title="Python curses 模块"></a>Python curses 模块</h2><p>这个 python 模块是针对 C 语言版本 curses 的简单封装，如果你已经熟悉了 C 语言的 curses 编程，在 Python 中应用这些知识也会变得非常简单。最大的不同就是，由于合并了一些C语言中的不同函数 Python 接口会比 C 语言函数更加简单。比如 <code>addstr()</code>,<code>mvaddstr()</code>, 和<code>mvwaddstr()</code>被合并成了一个函数<a href="https://docs.python.org/3.6/library/curses.html#curses.window.addstr"><code>addstr()</code></a>。后面你会看到更多这样的例子。</p>
<p>这篇教程是使用 curses 和 Python 编写终端文本程序的介绍，并不尝试成为一个 curses API 的复杂手册。查看 Python curses 手册和 C 语言的 ncurses 手册可以得到更详细的 API 介绍。</p>
<h2 id="一个-curses-程序的开始和结束"><a href="#一个-curses-程序的开始和结束" class="headerlink" title="一个 curses 程序的开始和结束"></a>一个 curses 程序的开始和结束</h2><p>在开始之前，Curses 必须先经过初始化。通过调用<a href="https://docs.python.org/3.6/library/curses.html#curses.initscr"><code>initscr()</code></a>来实现。这个函数会判断终端类型，并发送一些启动需要的指令给终端、创建内部的数据结构。如果执行成功，<code>initscr()</code> 返回一个代表整个屏幕的窗口类；这沿袭 C 语言中的变量名<code>stdscr</code>.</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> Curses</span><br><span class="line">stdscr = curses.initscr()</span><br></pre></td></tr></table></figure>
<p>通常 curses 会关闭屏幕回显，保证只在特定条件下才会读取键盘输入并显示。这需要调用 <a href="https://docs.python.org/3.6/library/curses.html#curses.noecho"><code>noecho()</code></a>方法。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">curses.noecho()</span><br></pre></td></tr></table></figure>
<p>应用程序通常需要对键盘输入立即做出响应，而不需要特意的按下回车键；这叫做 cbreak 模式，与之对应的是常用的缓冲输入模式。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">curses.cbreak()</span><br></pre></td></tr></table></figure>
<p>终端通常返回特殊键作为多字节转义序列，比如光标键、Home键、Page Up等， curses 可以让你的程序根据转义序列执行相应的代码。让 curses  可以响应这些特殊值，需要开启 keypad 模式。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">stdscr.keypad(<span class="literal">True</span>)</span><br></pre></td></tr></table></figure>
<p>结束一个 curses 应用比启动简单多了。只需要执行下面的方法：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">curses.nocbreak()</span><br><span class="line">stdscr.keypad(<span class="literal">False</span>)</span><br><span class="line">curses.echo()</span><br></pre></td></tr></table></figure>
<p>为了恢复 curses 的终端设置。调用 endwin() 方法重置为原来的操作模式</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">curses.endwin()</span><br></pre></td></tr></table></figure>
<p>在你调试你的程序的时候，一个经常出现的问题就是你会把终端搞得一团糟，通常是因为你的代码产生了 bug 并且引发了一个没有捕获的异常。例如：键盘输入不会在回显在屏幕上，这回让终端使用起来很困难。</p>
<p>在 Python 中你可以使用 <a href="https://docs.python.org/3.6/library/curses.html#curses.wrapper"><code>curses.wrapper()</code></a>来避免这种问题，让调试变得简单。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> curses <span class="keyword">import</span> wrapper</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span>(<span class="params">stdscr</span>):</span></span><br><span class="line">  <span class="comment"># Clear screen</span></span><br><span class="line">  stdscr.clear()</span><br><span class="line">  <span class="comment"># This raises ZeroDivisionError when i == 10.</span></span><br><span class="line">  <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="number">11</span>):</span><br><span class="line">      v = i-<span class="number">10</span></span><br><span class="line">      stdscr.addstr(i, <span class="number">0</span>, <span class="string">&#x27;10 divided by &#123;&#125; is &#123;&#125;&#x27;</span>.<span class="built_in">format</span>(v, <span class="number">10</span>/v))</span><br><span class="line"></span><br><span class="line">  stdscr.refresh()</span><br><span class="line">  stdscr.getkey()</span><br><span class="line"></span><br><span class="line">wrapper(main)</span><br></pre></td></tr></table></figure>
<p><a href="https://docs.python.org/3.6/library/curses.html#curses.wrapper"><code>wrapper()</code></a>函数接收一个可调用对象并且执行上文描述的初始化过程。如果支持颜色配置，同时会初始化颜色配置。然后会运行你的代码。一旦代码 return，<code>wrapper()</code> 会重置终端一开始的状态，并且代码会放在<code>try</code><br>…<code>except</code>中执行，如果异常抛出会将终端重置为原始状态然后将异常抛出。因此，在有异常抛出的时候，你的终端不会处在一个可笑的状态，并且能够根据异常信息定位问题。</p>
<h2 id="窗口和-Pad"><a href="#窗口和-Pad" class="headerlink" title="窗口和 Pad"></a>窗口和 Pad</h2><p>窗口是 curses 中最基本的元素。一个窗口代表着屏幕中的一块矩形区域，支持展示文本，删除文本，用户输入等等。</p>
<p><code>initscr()</code>函数返回的<code>stdscr</code>对象就是一个覆盖了整个屏幕的窗口对象。对于许多程序来说一个窗口就足够了，但是有时候也需要将屏幕分割为不同的窗口，以便于分别重绘和清除这些窗口。<code>newwin()</code>函数创建一个给定大小的新窗口，并返回这个窗口类。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">begin_x = <span class="number">20</span>; begin_y = <span class="number">7</span></span><br><span class="line">height = <span class="number">5</span>; width = <span class="number">40</span></span><br><span class="line">win = curses.newwin(height, width, begin_y, begin_x)</span><br></pre></td></tr></table></figure>
<p>注意到 curses 的坐标系统是不同寻常的。坐标通常是以 y，x 的格式，坐标的原点在窗口的左上角。这和通常程序中处理坐标时以 x 开头的方式是不同的。虽然这样令人有些不舒服，但是这是 curses 诞生的时候的设置，现在修改为时已晚。</p>
<p>你可以通过<code>curses.LINES</code>和<code>curses.COLS</code>来获取屏幕的大小。从<code>(0,0)</code>到<code>(curses.LINES - 1, curses.COLS - 1)</code>就是都是可以使用的坐标。</p>
<p>当你调用函数去展示或者擦除文本，效果并不会立即展现在屏幕上。你必须调用窗口实例的<code>refresh()</code>方法才能更新屏幕显示。</p>
<p>这是因为 curses 终端连接的带宽比较小，减少屏幕重绘时间变得十分有必要。因此，Curses 会积累修改，当你调用<code>refresh()</code>方法的时候，以最有效率的方式来重绘窗口。举例说明：如果你在一个窗口添加了一些文本，然后又清除了这个窗口的内容，这样添加文本的操作就变得没有必要了，因为你不会看到被添加的文本。</p>
<p>实际上，显式的通知 curses 来刷新窗口并不会对编程增加很多的复杂性。大部分程序都是在经历一系列的活动之后等待用户的操作，只需要在等待用户输入之前调用<code>stdscr.refresh()</code>或者<code>refresh()</code>方法。</p>
<p>Pad 是窗口的特殊形式，它可以比屏幕面积更大，并且可以每次只展示 pad 的一部分。创建 pad 需要制定 pad 的高和宽，刷新需要给定pad的在屏幕上显示部分的坐标。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">pad = curses.newpad(<span class="number">100</span>, <span class="number">100</span>)</span><br><span class="line"><span class="comment"># These loops fill the pad with letters; addch() is</span></span><br><span class="line"><span class="comment"># explained in the next section</span></span><br><span class="line"><span class="keyword">for</span> y <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="number">99</span>):</span><br><span class="line">    <span class="keyword">for</span> x <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="number">99</span>):</span><br><span class="line">        pad.addch(y,x, <span class="built_in">ord</span>(<span class="string">&#x27;a&#x27;</span>) + (x*x+y*y) % <span class="number">26</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Displays a section of the pad in the middle of the screen.</span></span><br><span class="line"><span class="comment"># (0,0) : coordinate of upper-left corner of pad area to display.</span></span><br><span class="line"><span class="comment"># (5,5) : coordinate of upper-left corner of window area to be filled</span></span><br><span class="line"><span class="comment">#         with pad content.</span></span><br><span class="line"><span class="comment"># (20, 75) : coordinate of lower-right corner of window area to be</span></span><br><span class="line"><span class="comment">#          : filled with pad content.</span></span><br><span class="line">pad.refresh( <span class="number">0</span>,<span class="number">0</span>, <span class="number">5</span>,<span class="number">5</span>, <span class="number">20</span>,<span class="number">75</span>)</span><br></pre></td></tr></table></figure>
<p>这个<code>refresh()</code>方法会在屏幕的<code>(5,5)</code>到<code>(10,75)</code>的部分显示pad的一部分；显示部分左上角的pad坐标是<code>(0,0)</code>。除此之外，pad和窗口的使用都是相同的，并且有相同的方法。</p>
<p>如果你需要多个窗口和pad协作的话，有一个更加高效的方式来刷新屏幕并避免每个部分更新时候烦人的闪烁。<code>refresh()</code>实际上做了两件事：</p>
<ul>
<li>调用每个窗口的<code>noutrefresh()</code>方法更新屏幕显示的底层数据结构，并不会刷新屏幕。</li>
<li>调用 <code>doupdate()</code>方法来将上面的数据结构物理的刷新到屏幕上。</li>
</ul>
<p>所以，你可以在一些窗口调用<code>noutrefresh()</code>方法更新数据结构，然后调用<code>doupdate()</code>方法来刷新屏幕。</p>
<h2 id="展示文本"><a href="#展示文本" class="headerlink" title="展示文本"></a>展示文本</h2><p>C 语言程序员也许会觉得 cursers 的方法像迷宫一样错综复杂，方法之间只有着很细微的区别。例如：<code>addstr()</code> 会在当前 <code>stdscr</code> 窗口的光标处展示文字，而<code>mvaddstr()</code>则是在指定的坐标展示文字。<code>waddstr()</code>功能和<code>addstr()</code>类似，但是允许指定窗口而不是默认的<code>stdscr</code>，<code>mvwaddstr</code>允许同时指定窗口和坐标。</p>
<p>幸运的是，Python 的接口隐藏了这些细节。<code>stdscr</code>是一个和其他相同的窗口类，类似<code>addstr()</code>的方法可以接收不同的参数类型。<br>通常包含以下四种参数类型。</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>参数类型</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>str 或者 ch</td>
<td>在当前位置展示 str 或者 ch</td>
</tr>
<tr>
<td>str 或者 ch，attr</td>
<td>在当前位置使用属性 attr 展示str或者ch</td>
</tr>
<tr>
<td>y,x str 或者 ch</td>
<td>在坐标 y,x 展示 str 或者 ch</td>
</tr>
<tr>
<td>y,x str 或者 ch, attr</td>
<td>在坐标 y,x 使用attr 属性显示 str 或者 ch</td>
</tr>
</tbody>
</table>
</div>
<p>属性(attributes) 可以突出显示文本，类似粗体、下划线、负片显示，或者为文本着色。这会在下面的部分详细解释。</p>
<p><code>addstr()</code>函数在终端显示一个字符串或者字节串。字节串直接发送给终端，字符串通过窗口属性编码为字节发送给终端，默认的系统编码可以通过<code>locale.getpreferredcoding()</code>获得。</p>
<p><code>addch()</code>函数可以接收一个字符，可以是一个长度为1的字符串，或者长度为1的字节串，或者一个整型数。</p>
<p>针对扩展的字符提供了一些常量，这些常量都大于255。例如：<code>ACS_PLMINUS</code> 代表 <code>+/-</code> 符号，<code>ACS_ULCORNER</code> 代表一个 box（处理绘制边框） 的左上角。你还可以使用其他合适 Unicode 字符。</p>
<p>窗口会自动记住上次操作之后光标的位置，如果你不使用坐标，所有的的操作都会开始于上次结束的地方。你也可以通过 <code>move(y,x)</code> 移动光标。因为有些终端光标是默认闪烁的，将光标移动到一些不是那么烦人的位置是很有必要的。</p>
<p>如果你的应用不需要闪烁的光标，你可以调用<code>curs_set(False)</code>将光标设置为不可见。为了和旧版本的 curses 版本兼容，<code>leaveok(bool)</code>和<code>curs_set</code>有着相同的功能。当 bool 是 true 的时候，光标就会变得不可见，你也不必担心光标会在一些奇怪的地方闪烁。</p>
<h2 id="属性和颜色"><a href="#属性和颜色" class="headerlink" title="属性和颜色"></a>属性和颜色</h2><p>字符可以用不同的方式展示。基于文本的应用程序通常使用负片显示状态。文本编辑器往往需要高亮一些特定的单词。 curses 支持通过属性来为每个字符进行配置以支持上面的描述。</p>
<p>一个属性是一个整数，每一个 bit 都代表着不同的属性。你可以尝试设置不同的bit位来达到不同的效果，不过 curses 不保证所有的组合都是可用的，也不保证不同的组合就一定是不同的显示。这取决于被使用的终端，所以使用大部分终端都会支持的属性是最明智的。列表如下</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>属性</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>A_BLINK</td>
<td>字符闪烁</td>
</tr>
<tr>
<td>A_BOLD</td>
<td>高亮或者加粗字符</td>
</tr>
<tr>
<td>A_DIM</td>
<td>半高亮字符</td>
</tr>
<tr>
<td>A_REVERSE</td>
<td>负片显示字符</td>
</tr>
<tr>
<td>A_UNDERLINE</td>
<td>为字符添加下划线</td>
</tr>
</tbody>
</table>
</div>
<p>所以，想要在屏幕顶端显示负片字符状态，可以使用下面的代码：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">stdscr.addstr(<span class="number">0</span>, <span class="number">0</span>, <span class="string">&quot;Current mode: Typing mode&quot;</span>, curses.A_REVERSE)</span><br><span class="line">stdscr.refresh()</span><br></pre></td></tr></table></figure>
<p>curses 同样支持为支持颜色显示的终端文字添加颜色。</p>
<p>如果想使用颜色功能，必须要在执行玩<code>initscr()</code>之后执行<code>start_color()</code>函数（<code>curses.wrapper()</code>会自动执行）来初始化颜色管理，如果终端支持颜色显示，那么调用 <code>has_colors()</code>函数会返回 TRUE。</p>
<p>curses 维护了有限的颜色搭配，包括前景色（文字颜色）和背景色，可以通过使用<code>color_pair()</code>函数来设置字体颜色，类似 A_REVERSE， 也是按照bit位的属性设置的，同样的，不保证所有的组合都能够在所有的终端上正确显示。</p>
<p>使用颜色组合1来显示文字的例子：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">stdscr.addstr(<span class="string">&quot;Pretty text&quot;</span>, curses.color_pair(<span class="number">1</span>))</span><br><span class="line">stdscr.refresh()</span><br></pre></td></tr></table></figure>
<p>像上文说的那样，每个颜色模式分为前景色和背景色。<code>init_pair(n, f, b)</code> 方法会修改颜色模式n的前景色为f，背景色为b。颜色模式0表示黑底白字，不可以修改。</p>
<p>颜色是被编号的，<code>start_color()</code>方法会初始化八种基本颜色，分别是：0:黑色，1:红色，2:绿色，3:黄色，4:蓝色，5:洋红色，6:青色，7:白色。curses 同样也为这些颜色设置了常量值，curses.COLOR_BLACK, curses.COLOR_RED, curses.COLOR_GREEN, 等等。</p>
<p>来实际应用一下，将1号颜色模式修改为白底红字：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">curses.init_pair(<span class="number">1</span>, curses.COLOR_RED, curses.COLOR_WHITE)</span><br></pre></td></tr></table></figure>
<p>当你修改一个颜色模式的时候，所有使用了这个颜色模式的文字都会刷新为新的颜色。<br>你也可以通过下面的方式来添加文字</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">stdscr.addstr(<span class="number">0</span>, <span class="number">0</span>, <span class="string">&quot;RED ALERT!&quot;</span>, curs_set.color_pair(<span class="number">1</span>))</span><br></pre></td></tr></table></figure>
<p>很多终端支持通过给定的 RGB 值来修改颜色。这可以将颜色模式修改为任何你想要的颜色。不幸的是，Linux 标准终端并不支持，所以我无法演示，或给出例子。你可以通过调用<code>can_change_color()</code>函数来确定你的终端是否支持这个功能。如果你的终端恰好返回的是 True，表示支持，可以查阅 man 手册来获取更多的信息。</p>
<h2 id="用户输入"><a href="#用户输入" class="headerlink" title="用户输入"></a>用户输入</h2><p>C 语言的 curses 提供一个很简单的输入方式。python 的 curses 增加了一些基本的输入技巧（其他类似 Urwid 的库提供了更多种类的输入技巧）。</p>
<p>在一个窗口上获取输入有两种方式：</p>
<ul>
<li><code>getch()</code>刷新屏幕并等待用户输入一个按键，如果<code>echo()</code>在这之前被调用，输入的按键会同步显示在屏幕上。你可以指定坐标控制显示的位置。</li>
<li><code>getkey()</code>和上面的函数做了同样的事情，不同的是将整数转换成了字符串。单字符返回一个单字符串，特殊按键会返回一个长串，类似<code>KEY_UP</code>或者<code>^G</code></li>
</ul>
<p>通过调用<code>nodelay()</code>函数可以实现不等待用户的输入，如果设置<code>nodelay(True)</code>，<code>getch()</code>和 <code>getkey()</code>函数不会再等待输入。如果没有输入，<code>getch()</code>会返回 <code>curses.ERR</code>(值为-1),<code>getkey()</code>函数则是会抛出一个异常。<code>getch()</code>还有一个<code>halfdelay()</code>函数可以设定在指定的时间（单位为十分之一秒）内如果没有得到用户输入才会抛出异常。</p>
<p><code>getch()</code>函数返回一个整数。如果在0～255范围内代表的是 ASCII 码，如果大于255，则可能是一些特殊按键类似： Page Up，Home，或者光标按键。你可以通过输入值和一些常量的比较确定输入。常量类似：curses.KEY_PPAGE, curses.KEY_HOME, curses.KEY_LEFT。所以你的代码主循环有可能是这样的：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">  c = stdscr.getch()</span><br><span class="line">  <span class="keyword">if</span> c == <span class="built_in">ord</span>(<span class="string">&#x27;p&#x27;</span>):</span><br><span class="line">    PrintDocunment()</span><br><span class="line">  <span class="keyword">elif</span> c == <span class="built_in">ord</span>(<span class="string">&#x27;q&#x27;</span>):</span><br><span class="line">    <span class="keyword">break</span></span><br><span class="line">  <span class="keyword">elif</span> c == curses.KEY_HOME:</span><br><span class="line">    x = y = <span class="number">0</span></span><br></pre></td></tr></table></figure>
<p><code>curses.ascii</code>模块提供了 ASCII 处理函数，参数为一个整数或者单字符。对于书写更加可读的代码很有用。同样提供接收一个整数或者字符的对话函数。例如：<code>curses.ascii.ctrl()</code>根据参数返回控制字符。</p>
<p>还有一个可以获得整个字符串的函数，就是<code>getstr()</code>,由于功能限制很少使用。仅有的编辑按键就是 backspace 和 Enter 按键。 <code>getstr()</code>可以用于获取指定长度的字符串。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">curses.echo()            <span class="comment"># Enable echoing of characters</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Get a 15-character string, with the cursor on the top line</span></span><br><span class="line">s = stdscr.getstr(<span class="number">0</span>,<span class="number">0</span>, <span class="number">15</span>)</span><br></pre></td></tr></table></figure>
<p><code>curses.textpad</code>模块提供一个支持类似 Emacs 键盘快捷键的文本框。<code>Textbox</code>的不同方法支持输入编辑和聚合编辑结果，无论是不是有多余的空格。<br>下面是例子</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> curses</span><br><span class="line"><span class="keyword">from</span> curses.textpad <span class="keyword">import</span> Textbox, rectangle</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span>(<span class="params">stdscr</span>):</span></span><br><span class="line">    stdscr.addstr(<span class="number">0</span>, <span class="number">0</span>, <span class="string">&quot;Enter IM message: (hit Ctrl-G to send)&quot;</span>)</span><br><span class="line"></span><br><span class="line">    editwin = curses.newwin(<span class="number">5</span>,<span class="number">30</span>, <span class="number">2</span>,<span class="number">1</span>)</span><br><span class="line">    rectangle(stdscr, <span class="number">1</span>,<span class="number">0</span>, <span class="number">1</span>+<span class="number">5</span>+<span class="number">1</span>, <span class="number">1</span>+<span class="number">30</span>+<span class="number">1</span>)</span><br><span class="line">    stdscr.refresh()</span><br><span class="line"></span><br><span class="line">    box = Textbox(editwin)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># Let the user edit until Ctrl-G is struck.</span></span><br><span class="line">    box.edit()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># Get resulting contents</span></span><br><span class="line">    message = box.gather()</span><br></pre></td></tr></table></figure>
<p>更多内容参考<a href="https://docs.python.org/3.6/library/curses.html#module-curses.textpad"><code>curses.textpad</code></a>的文档。</p>
<h2 id="更多信息"><a href="#更多信息" class="headerlink" title="更多信息"></a>更多信息</h2><p>这篇教程并没有包含一些高级主题，例如读取屏幕的信息，捕获鼠标的动作。但是 Python 的 <a href="https://docs.python.org/3.6/library/curses.html#module-curses">curses</a> 模块的文档现在已经完成了，下一步你应该阅读他们。</p>
<p>如果你还对 curses 函数的一些行为细节只有怀疑，查询你的 curses 实现的文档吧，无论是 ncurses 或者其他 Unix 实现。手册中会记录各种小技巧，并提供完整的函数列表、属性，还有那些 ACS_* 字符可用。</p>
<p>因为 curses 的 API 非常繁杂，因此有一些函数并没有得到 Python 的支持。这通常不是因为这些函数很难实现，而是因为这些函数已经没人需要了。同样，Python 也不支持 ncurses 的菜单库。欢迎大家是实现这些没有实现的功能，阅读<a href="https://devguide.python.org/">Python Developer’s Guide</a>学习如何为 Python 提交代码。</p>
<ul>
<li><a href="http://invisible-island.net/ncurses/ncurses-intro.html">Writing Programs with NCURSES</a> 针对C程序员的详细教程</li>
<li><a href="http://linux.die.net/man/3/ncurses">The ncurses man page</a></li>
<li><a href="http://invisible-island.net/ncurses/ncurses.faq.html">The ncurses FAQ</a></li>
<li><a href="https://www.youtube.com/watch?v=eN1eZtjLEnU">“Use curses… don’t swear”</a> PyCon 2013 中关于使用 curses 或者 Urwid 控制终端显示的视频</li>
<li><a href="http://www.pyvideo.org/video/1568/console-applications-with-urwid">“Console Applications with Urwid”</a> PyCon 2012 中使用 Urwid 编程的示范视频</li>
</ul>
]]></content>
      <categories>
        <category>翻译</category>
      </categories>
      <tags>
        <tag>translate</tag>
      </tags>
  </entry>
</search>
